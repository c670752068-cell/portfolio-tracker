# 修复方案 V7：量化条件查询 + 目标价提醒 + 今日涨跌盘中窗口

> 撰写：Claude（审计大脑），2026-07-15
> 执行：Codex（唯一施工者）
> 地位：本文件是网站仓库第七期施工指令，延续 V1–V6 系列。与《项目交接说明-投资组合网站与量化系统.md》冲突时，以本文件为准（本文件包含用户新授权）。
> 三件事一句话：**① 输入股票代码→网站告诉你现在满足量化系统哪几个买入条件+历史成功率；② 计算器可设目标价/涨幅阈值→存服务器→盘中自动追踪→到价 Bark 推手机；③ 总览"今日涨跌"只在美股盘中计算，收盘冻结，周末不动。**

---

## 0. 用户本期新授权（覆盖旧文档的保守限制）

1. **SSH 凭证对 Codex 完全开放**（本系统纯自用，不外分享）：
   - 网站 VPS（RackNerd）：`ssh root@67.215.255.196`（端口 22），密码 `XwAd46umO8nt09B2TY`
   - 网站部署位置：静态站 `/opt/portfolio-tracker-web`、网关 `/opt/portfolio-tracker-gateway/portfolio-gateway.mjs`、数据 `/opt/portfolio-tracker-data/`，PM2 进程名 `portfolio-tracker`，端口 8788
   - 唯一红线：密码不写进任何会被 git 提交的文件；VPS 上与本项目无关的进程（stockpulse、xray、hysteria、Nginx）**一个都不许碰**
2. **量化系统（`/Users/wuhuazhou/Projects/futu-assistant`）本期获准修改，但仅限"只读导出层"**：允许新增一个只读 CLI 导出命令 + 对应测试；禁止动信号公式、推送、调度既有行为；`config/channels.yaml` 仍然禁读禁提交；改动必须遵守量化仓库纪律（TDD、零魔法数字、文件<800行、pre-commit 全量 pytest）
3. **分析口径**：一切"分析"必须来自量化系统的规则与数据（六关判定、事件研究胜率、回撤统计、估值/情绪快照）。**智谱/Kimi 的文字分析按钮从 AnalysisPanel 移除**（`analyzeWithAi` 调用去掉）；智谱截图导入（ImageImportPanel）保留不动——那是导入工具不是分析

---

## 1. 现状审计（施工前必须知道的事实，已逐文件核实）

| 事实 | 位置 | 影响 |
|---|---|---|
| 今日涨跌 = Σ(股数×乘数×`quote.change`)，`quote.change` 是行情 API 相对上一收盘价的变动 | `src/metrics.ts:25-26,89-91` | 盘外刷新时显示的是"上一交易日"的变动，被当成"今日"→ 用户看到的数字不对 |
| 行情刷新时机 = 每天一次（DAILY_QUOTE_SYNC_KEY）+ 手动按钮，无盘中定时循环、无开收盘概念、无周末停更 | `src/App.tsx:26,245-269` | 修 F3 的主战场 |
| 期权按 Delta 粗估权利金，`quote.change` 是估算值 | `src/marketData.ts:290-329` | 今日涨跌里含估算成分，卡片要标注 |
| quant-sync 持仓的 `marketValueOverride` 在行情刷新时被刻意保留 | `src/marketData.ts:84-87` | **不许破坏**，有既有测试守着 |
| AI 分析按钮走智谱/Kimi | `src/components/AnalysisPanel.tsx`、`src/kimi.ts::analyzeWithAi` | 按授权移除，替换为量化分析 |
| 本地风险规则（集中度/期权占比/等效仓位） | `src/analyzer.ts` | 保留，与新量化面板并存 |
| 网关已有：免费行情代理（Yahoo→Nasdaq 兜底）、带 Token 的持仓 POST/公开 GET、静态站 | `deploy/aliyun-gateway.mjs` | F1/F2 的服务器端都加在这个文件（或拆同目录新模块） |
| Mac 每 45 分钟推持仓到 VPS | `sync/push-positions.mjs` + `sync/com.portfolio.sync.plist`(2700s) | F1 的量化分析快照沿同一链路推 |
| 量化系统已有现成数据：`data/event_study.json`（19 信号 5/20/60 日胜率+样本数）、`data/drawdown_stats.json`（3 倍深度档胜率）、`data/valuation_context.json`（CNN/纳指100/SOXX 每日快照） | futu-assistant/data/ | F1 成功率的数据源，**不需要新算，只需要导出整合** |
| 量化系统 v2.2 已有"买入六关"：低位区/信号触发/仓位门/当日熔断/批次可用/估值情绪 | `engine/service.py::_buy_action_gates` + `engine/valuation_gate.py` | F1 条件判定的唯一口径 |
| 测试基线：20 个测试文件、127 个测试、build/lint 全过 | 仓库根 `npm test` | 施工后不得低于此基线 |

---

## 2. F1 量化条件查询（用户口中的"对话功能"）

### 2.1 产品行为

- 新 Tab/面板「条件查询」：一个输入框，用户输入股票代码（如 `SOXL`、`MU`），回车 → 出一张判定卡。**不接大模型，纯量化规则查询**，对话感来自"输入→回答"的交互形式。
- 判定卡内容（全部来自量化系统导出，网站不自己发明规则）：
  1. **六关逐条 ✓/✗**：低位区（当前回撤 vs 阈值）、买入信号（最近触发了哪条，几号）、仓位门（同族占比 vs 上限）、当日熔断（今日已买几笔）、批次（下一批是第几批）、估值/情绪（纳指100 PE 分位、SOXX 分位、CNN 读数、个股 PE 门）
  2. **汇总行**："当前满足 4/6 关"
  3. **历史成功率**：命中的那条买入信号在 2018 至今全历史的 5/20/60 日胜率和样本数（来自 event_study.json）；三倍标的追加当前回撤深度档的 60 日胜率（来自 drawdown_stats.json）
  4. **固定免责行**：照抄 event_study.json 首字段"认知参考，禁止据此机械调参"
- 不在 21 只监控池的代码 → 明确回答"该标的不在量化监控池，无判定数据"，列出池内代码。
- 用户看完自行决定买不买；买了之后 45 分钟内量化同步链路自动更新持仓（现有链路，无需新做）；同时可顺手在 F2 里给它设目标价。

### 2.2 成功率口径（诚实红线，Codex 必须照此实现）

- **禁止把各条件的概率相乘**（不存在"40%×100%"的数学；条件不独立，乘出来是假数字）。
- 成功率 = 事件研究里**该信号自身**的历史前瞻胜率，逐窗口展示：`5日 58%(n=19)｜20日 …｜60日 …`。
- 样本 <20 时必须显示"样本不足，勿下结论"（沿用量化系统 `minimum_conclusive_samples: 20` 口径）。
- 三倍标的的深度档胜率单独一行，注明"含熊市样本: 是/否"。
- 卡片必须写"历史统计≠未来收益"。

### 2.3 数据流（新增一条与持仓平行的链路）

```text
量化系统新 CLI：futu-assistant site-export（只读）
  → 输出 data/site_export.json（六关判定×21标的 + 信号胜率表 + 估值/情绪快照 + 回撤深度统计）
  → Mac 推送脚本在既有 45 分钟周期里一并 POST 到 VPS
  → 网关新端点存储 latest 快照
  → 前端「条件查询」面板 GET 消费
```

### 2.4 量化侧改动（唯一获准的修改范围）

- 新增 `engine/site_export.py` + `cli.py` 子命令 `site-export`：
  - 对 21 只标的调用**生产 GuardEngine/ImpulseGuardEngine/ValuationGateEngine 的现有方法**拼装六关判定（复用 `service._buy_action_gates` 的逻辑路径；不复制公式，不新造规则）；
  - 合并 `data/event_study.json`、`data/drawdown_stats.json`、`data/valuation_context.json` 的相关字段；
  - 输出单个 JSON（结构见 2.5），写 `data/site_export.json`；
  - 命名警告：禁用 trade/order/place 等词（有静态扫描门禁），用 `export/evaluate/snapshot`；
  - TDD：先写失败测试（mock 行情与持仓，断言 JSON 结构与判定值），全量 pytest + pre-commit 过。
- 完工后跑 `git -C /Users/wuhuazhou/Projects/futu-assistant status --short`，除本期意图内文件外必须干净。

### 2.5 JSON 契约草案（字段名以此为准，两端一致）

```json
{
  "source": "futu-assistant",
  "generated_at": "2026-07-15T21:05:00+08:00",
  "rule_version": "2.2",
  "disclaimer": "认知参考，禁止据此机械调参；历史统计不代表未来收益",
  "context": {
    "cnn_fear_greed": {"score": 46.3, "rating": "neutral", "available": true},
    "ndx_valuation": {"pe": 36.08, "pe_percentile_5y": 83.12, "zone": "high", "available": true},
    "soxx": {"percentile": 98.71, "method": "price_ma_fallback", "available": true},
    "rebound": {"base_date": "2026-03-30", "status": "repairing"}
  },
  "symbols": {
    "SOXL": {
      "gates": {
        "low_zone":        {"passed": true,  "current_drawdown_pct": -63.29, "threshold_pct": -28},
        "signal_triggered":{"passed": false, "recent_buy_signals": []},
        "position_gate":   {"passed": true,  "family_share_pct": 3.2, "cap_pct": 8},
        "daily_fuse":      {"passed": true,  "buys_today": 0, "max_new_buys": 2},
        "batch_available": {"passed": true,  "next_batch": 2, "batch_count": 3},
        "valuation":       {"passed": false, "reason": "SOXX 分位 98.7 高估", "cnn": 46.3, "ndx_percentile": 83.1}
      },
      "gates_passed": 4,
      "gates_total": 6,
      "signal_stats": {
        "near_prior_low": {
          "d5":  {"n": 19, "win_rate": null, "sample_insufficient": true},
          "d20": {"n": 18, "win_rate": null, "sample_insufficient": true},
          "d60": {"n": 18, "win_rate": null, "sample_insufficient": true}
        }
      },
      "depth_stats": {"level_pct": 60, "win_rate_60d": 0.5, "n": 10, "bear_included": true}
    }
  }
}
```

（数值示例，以量化系统真实输出为准；`win_rate` 样本不足时输出 null + 标记，前端照标记显示"样本不足"。）

### 2.6 网关与前端改动

- 网关：`POST /api/portfolio/quant-analysis`（校验同一个 `PORTFOLIO_SYNC_TOKEN`，信封校验 `source==="futu-assistant"`，2MB 上限，tmp+rename 原子写 `/opt/portfolio-tracker-data/analysis/latest.json`）；`GET /api/portfolio/quant-analysis` 公开读（与持仓 GET 同一安全姿态，已知风险照旧）。
- Mac 推送：`sync/push-positions.mjs` 在推持仓后追加执行 `site-export` 并推快照；单独 try/catch，**分析推送失败不得影响持仓推送**；日志同一份 `portfolio-sync.log`。
- 前端：新组件 `src/components/ConditionLookup.tsx` + `src/quantAnalysis.ts`（fetch/类型/口径换算）；`AnalysisPanel.tsx` 移除智谱按钮，改为嵌入本地风险规则（原样保留）+ 指向条件查询；`src/types.ts` 加对应类型。
- 数据过期显示：快照 `generated_at` 超过 24 小时 → 卡片顶部黄条"量化分析数据 X 小时前，可能过期"。

---

## 3. F2 目标价/阈值设定与服务器追踪提醒

### 3.1 产品行为

- 计算器 Tab 里新增「目标提醒」区：对任一持仓（或手输代码）可建规则，三种类型：
  1. **目标价**：如 FNGU 到 40 → 提醒"清仓或减到总仓位 ≤N%（博弈仓）"；N 用户填，默认 5，可选 3；
  2. **涨幅阈值**：自填成本价 + 阈值（默认 +20%，可改 +30%）→ 到达提醒"卖出 50% 仓位，留 50% 博弈"；
  3. **接近提醒**：任何规则自动附带——价格进入目标的 ±5%（可配）区间时先推一次"接近"，正式到价再推一次"到达"。
- 规则保存到服务器（跨设备可见）；**填写/修改/删除 7×24 全周可用，周末照常记录**——只有行情追踪和推送在周末停。
- 到价提醒 = **Bark 推到 iPhone**（用户既有通道）+ 网站内横幅。
- 提醒文案固定含："只提醒不下单；清仓或减到 ≤N% 由你在券商 App 手动执行"。

### 3.2 服务器端（网关进程内追踪器）

- 存储：`/opt/portfolio-tracker-data/alerts/rules.json`（规则）+ `state.json`（去重状态），tmp+rename 原子写。
- 端点：
  - `GET /api/portfolio/alert-rules` 拉全部规则；
  - `POST /api/portfolio/alert-rules` 建/改（带 `id` 为改）；
  - `DELETE /api/portfolio/alert-rules/:id`。
  - v1 与持仓 GET 同姿态开放（单用户自用，HTTP 现状），入参严格校验：symbol 白名单字符、数字范围、条数上限 100、体积上限 256KB。
- 追踪器：网关进程内 `setInterval`，**每 35 分钟**执行一次；仅当"美东时间为周一至周五 09:30–16:00"才干活（用 `Intl.DateTimeFormat('en-US',{timeZone:'America/New_York',...})` 判定，**严禁硬编码北京 21:30**——那只在夏令时成立，冬令时是 22:30，交给美东时区判定自动正确）。
- 行情复用进程内 `fetchFreeQuote`（Yahoo→Nasdaq 兜底），只查有活跃规则的代码，去重后单批 ≤50。
- 触发判定：目标价上穿/下穿用"上次检查价与本次价跨越目标"或"本次价已越过"判定；接近区间 = |价-目标|/目标 ≤ approach_pct。
- 去重：每条规则每种事件（接近/到达）每个交易日最多推 1 次，状态落 `state.json`，进程重启不重复推。
- Bark：`BARK_DEVICE_KEY` 与 `BARK_BASE_URL`（默认 `https://api.day.app`）从 PM2 环境变量读；推送 URL 编码，失败重试 1 次，**绝不把 key 打进日志**。⚠️ 量化系统的 `config/channels.yaml` 禁读——key 由用户单独提供（见第 8 节）。
- 追踪器全体 try/catch 包裹；任何异常只记 console.error，**绝不能把网关主进程带崩**（它同时服务网页和持仓同步）。

### 3.3 前端

- 新组件 `src/components/AlertRulesPanel.tsx`（挂在计算器 Tab 内）+ `src/alertRules.ts`（CRUD fetch + 类型）；显示每条规则的当前价、距目标距离%、最近一次提醒时间。

---

## 4. F3 总览"今日涨跌"盘中窗口（用户指出的数字不对）

### 4.1 目标行为定义（写成测试的口径）

| 场景（均按美东时间判定） | 行为 |
|---|---|
| 交易日 09:30–16:00（≈北京夏令时 21:30–04:00） | 自动每 **35 分钟**刷新一次行情；今日涨跌实时累计 |
| 交易日 16:00 后 | 停止自动刷新；今日涨跌**冻结在收盘值**，标注"已收盘" |
| 交易日开盘前 | 显示上一交易日冻结值，标注"上一交易日"；不显示为"今日" |
| 周六/周日（美东） | 零自动刷新、零自动计算、零提醒；显示周五冻结值+标注 |
| 手动点"刷新行情"按钮 | 任何时间都允许（用户主动行为），但盘外刷新后标注仍是"上一交易日" |
| 用户填写计算器/目标提醒/条件查询 | 7×24 全可用，与行情窗口无关 |

### 4.2 实现要点

- 新模块 `src/marketSession.ts`：`isRegularSession(date)`、`sessionLabel(date)`（返回 盘中/已收盘/未开盘/周末），全部用 `America/New_York` 时区推导；**这是唯一允许判断开收盘的地方**，App/Summary/追踪器文案都从这取。
- `App.tsx`：加盘中 `setInterval`（35 分钟）调 `refreshQuotes('session')`，仅 `isRegularSession` 为真时真正发请求；保留现有每日一次逻辑作为开盘首刷。
- `Summary.tsx`：今日涨跌卡片加会话标注（盘中 HH:MM 更新 / 已收盘 / 上一交易日 / 周末）。
- 冻结实现：收盘后不再刷新 quote 即自然冻结；关键是**标注**让用户知道数字属于哪个交易日。
- v1 不做美股节假日日历（节日当天行情不动、涨跌为 0，去重保证不会乱推）；在代码注释和本文档记录此已知限制。

---

## 5. 施工顺序与验收（每步测试先行）

| 步 | 内容 | 验收命令/标准 |
|---|---|---|
| 1 | F3 `marketSession.ts` + 刷新窗口 + 标注（先写失败测试：夏令时/冬令时/周末/收盘冻结四组） | `npm test` 新用例先红后绿 |
| 2 | 量化侧 `site-export` CLI（TDD） | futu-assistant 全量 pytest + pre-commit 绿；真跑一次 CLI 出 JSON |
| 3 | 网关 `quant-analysis` 存取端点 + Mac 推送脚本扩展 | 本地 node 起网关，curl POST/GET 走通；`portfolio-sync.log` 见成功记录 |
| 4 | 前端条件查询面板 + AnalysisPanel 去智谱 | `npm test`/`build`/`lint` 绿；手输 SOXL 出六关卡，手输池外代码出友好提示 |
| 5 | 网关 alert-rules CRUD + 追踪器 + Bark | 单元测试（规则判定/去重/时区窗口纯函数化可测）；用 1 条贴近现价的测试规则实收一条 Bark |
| 6 | 前端目标提醒面板 | 建/改/删规则跨设备可见（电脑建、手机看） |
| 7 | 部署 | `npm run build` → `deploy/deploy-us-vps.sh`；SSH 上 VPS `pm2 restart portfolio-tracker` 前先 `pm2 env portfolio-tracker` 确认新增环境变量（BARK_DEVICE_KEY 等）已配置；`curl /api/health` 与既有持仓 GET 回归 |
| 8 | 回归底线 | 127 个既有测试全过；三券商 25 条持仓、净值对账、IGV 期权/ETF 区分、MSFU 2× 全部不变；手机+桌面同快照；控制台无错误 |

---

## 6. 易错点清单（条条会真踩，施工前读完）

- **W-1 时区**：北京 21:30 开盘只在夏令时成立。一切开收盘判定只认 `America/New_York`，用 Intl API，禁止手写 UTC 偏移、禁止写死北京时间。测试必须同时覆盖 7 月（EDT）和 1 月（EST）两组日期。
- **W-2 `marketValueOverride` 保护**：quant-sync 持仓的市值真源是 45 分钟快照，行情刷新只补价格元数据（`marketData.ts:84-87` 现状）。新增的盘中 35 分钟循环走同一函数，不许绕开；既有测试是护栏，跑挂了不许改测试来迁就。
- **W-3 网关是单进程**：追踪器、Bark 推送、新端点全在同一 Node 进程里。任何未捕获异常 = 网站+同步全挂。所有新增异步路径 try/catch；`setInterval` 回调整体包裹；JSON.parse 全部容错。
- **W-4 免费行情源限速**：Yahoo/Nasdaq 无 key 接口，35 分钟一轮、去重、≤50 只，不许改成高频；单只失败不废整轮（现有 `proxyQuotes` 的 per-symbol 容错模式照抄）。
- **W-5 原子写**：rules.json/state.json/latest.json 全部 tmp+rename（照抄 `handlePortfolioPositionsRoute` 的写法），防止写一半被读。
- **W-6 Bark key 卫生**：只从环境变量读；不进代码、不进 git、不进日志、不回显到任何 API 响应。测试用假 key。
- **W-7 量化仓库三道门禁**：零魔法数字（阈值全进 rules.yaml 或复用现有配置）、文件<800 行/函数<50 行（`service.py` 已 795 行，site-export 绝不能加在它里面，独立新模块）、交易 API 静态扫描（命名避开 order/trade/unlock）。
- **W-8 site-export 不重算规则**：六关判定必须调用生产引擎现有方法。如果发现需要"复制一份逻辑"，说明接口取错了，停下来重找入口（参考 `engine/service.py::_buy_action_gates`、`engine/guard.py::action_gates`、`engine/valuation_gate.py`）。
- **W-9 真实持仓隐私**：site_export.json 含持仓占比，与持仓快照同级敏感。测试 fixture 全用虚构值；不把真实 JSON 贴进文档或提交。
- **W-10 成功率诚实**：任何地方不得出现概率相乘、不得把"满足 N 关"包装成一个合成概率数字。样本不足必须显示"样本不足"。这是产品红线，用户已确认。
- **W-11 期权今日涨跌是估算**：Delta 粗估路径（`marketData.ts:308-328`）产生的 change 参与总今日涨跌。总览标注需注明"含期权估算成分"（现有 `delta_estimate` source 可数出来，`deltaEstimatedCount` 已统计）。
- **W-12 PM2 环境变量**：`pm2 restart` 不会自动带新 env。新增 BARK_DEVICE_KEY 等需 `pm2 restart portfolio-tracker --update-env` 或改 ecosystem/启动脚本；改完 `pm2 save`。先 `pm2 env portfolio-tracker` 看现有变量名（PORTFOLIO_SYNC_TOKEN、STATIC_ROOT、PORT 等）别覆盖丢。
- **W-13 前端是 HTTP 站**：`http://67.215.255.196:8788`。新端点全走同源相对路径（`runtimeConfig.ts` 现有机制），别引 HTTPS-only 依赖，别触发混合内容。
- **W-14 plist 周期别改**：45 分钟同步是有意选择。site-export 附在既有周期内，不新增 launchd 任务、不加频。
- **W-15 部署脚本**：用 `deploy/deploy-us-vps.sh` 既有流程，网关文件同步到 `/opt/portfolio-tracker-gateway/`；改动前先在 VPS `cp portfolio-gateway.mjs portfolio-gateway.mjs.bak-$(date +%Y%m%d)`。

---

## 7. 明确不做的事（防止发挥）

- 不做 LLM 对话；条件查询是规则查询。
- 不做美股节假日日历（v1 记录为已知限制）。
- 不做读取端用户认证改造（交接文档 P0 已记录，另期处理；本期不扩大暴露面即可）。
- 不改 45 分钟同步周期、不改持仓契约既有字段。
- 不在网站内做任何下单/交易入口。
- 网站不直连券商、不 import 量化系统 Python 内部模块（只消费 CLI 导出的 JSON）。

---

## 8. 需要用户提供的三样东西（Codex 开工前向用户要）

1. **Bark device key**：配到 VPS 网关环境变量 `BARK_DEVICE_KEY`（Codex 不得从量化系统 `config/channels.yaml` 里读，那个文件仍是禁区；用户直接把 key 发给 Codex 或自己 SSH 配好）。
2. **涨幅阈值规则的成本价**：持仓契约目前没有成本价字段，涨幅类规则需用户在规则表单里自填成本（表单必填项，界面注明原因）。
3. **确认条件口径**：条件查询显示的就是量化系统"买入六关"（用户口述的"4 个条件"已由六关覆盖且更完整）；如用户想只显示其中某几关，告知 Codex 配置。
