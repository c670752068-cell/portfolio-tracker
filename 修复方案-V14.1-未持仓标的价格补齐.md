# 修复方案 V14.1：未持仓标的的价格补齐（V14 盲区补充）

日期：2026-07-22
适用仓库：`portfolio-tracker`（网站侧 Codex 执行）
前置：V14 已完成并部署（248 测试全绿，三个 hash 一致 `index-fMVJS00k.js`）。

> **本文件是补充，不是返工。** V14 的公式、UI、定时刷新、量化真值预留全部实现正确，已逐项验证（见第 1 节）。本轮只补一个**方案措辞造成的盲区**——责任在方案，不在执行。

---

## 0. 给执行者（Codex）的硬性规则

1. 只做本文件列出的任务；**不得修改 `src/depthPrice.ts` 的推算公式**（已验证正确）。
2. 先写**会失败的测试并确认它真的失败**（贴失败输出），再改实现变绿。
3. `npm run build` + `npm test` 全绿 → 单独 `git commit` → 留验证报告。
4. 不新增数据源、不新增运行时依赖（用已有的 `/api/quotes` 网关代理）。
5. 契约禁区：不改 `~/Projects/futu-assistant` 任何文件。
6. 完工贴出**三个 hash**（本地 dist / 线上 VPS / 线上 Pages）。

---

## 1. V14 验收结论（已逐项核实，无需返工）

| 项 | 状态 | 证据 |
|---|---|---|
| 推算公式 | ✅ 正确 | 实测 `current_pct=-21.8, threshold_pct=-16.24, price=384.98` → 高点 492.30、阈值价 412.35，与手算一致 |
| 正负号稳健性 | ✅ 优于方案 | 用 `Math.abs()` 处理，量化给 `-21.8` 或 `21.8` 都正确（方案原写法 `1 + pct/100` 假设必为负，Codex 的实现更稳） |
| 除零保护 | ✅ 有 | `ZERO_EPSILON` 判断，无 NaN/Infinity |
| 量化真值优先 | ✅ 已预留 | 读取 `depth.current_price / high_price / threshold_price`，量化侧交付后自动切 `source='quant'` |
| `~` 标注与偏差说明 | ✅ 有 | `derived` 时加 `~`，并显示"由行情代理现价与量化回撤反推，与量化取价时段可能有偏差" |
| 无数据不显示假价 | ✅ 有 | `unavailable` 时整块不渲染，无 0 / NaN |
| 25 分钟定时刷新 | ✅ 有 | `QUANT_ANALYSIS_REFRESH_MS = 25 * 60 * 1000` + `visibilitychange` |
| 测试与部署 | ✅ | 248 测试全绿；本地 = VPS = Pages = `index-fMVJS00k.js` |

---

## 2. 盲区：未持仓标的拿不到价格（本轮要修）

### 现象

`ConditionLookup.tsx` 的 `depthQuotePrice(holdings, symbol)` **只遍历 `holdings`（用户持仓）** 寻找报价：

```ts
function depthQuotePrice(holdings: readonly Holding[], rawSymbol: string): number | null {
  for (const holding of holdings) {
    if (!holding.quote) continue;
    // ...只在持仓里匹配 symbol
  }
  return null;   // 未持有 → null
}
```

于是：
- 用户**持有**的标的（NVDA / MSFT / TQQQ…）→ 有价格 ✅
- 用户**未持有**但在量化监控池里的标的（**AVGO、MU、AAPL…**）→ `null` → `source='unavailable'` → **整块价格不显示** ❌

**而这恰恰是「深度买入窗口」最有价值的场景** —— 它的用途正是发现**还没买**的标的的买入机会。用户截图里的 AVGO 就是未持仓标的，所以他看到的仍然只有百分比、没有价格。

### 实证

```
有报价 → { currentPrice: 384.98, highPrice: 492.30, thresholdPrice: 412.35, source: 'derived' }
无报价 → { currentPrice: null, highPrice: null, thresholdPrice: null, source: 'unavailable' }
```
两个断言均通过，确认无持仓即无价格。

### 责任归属

V14 方案第 2 节原文写的是「数据来源：复用已有的行情代理调用结果（`marketData.ts` 的 quote），**不新增数据源**」，并补了一句"若该标的不在持仓中而无报价，允许显示 `unavailable`"。**Codex 严格执行了这句话** —— 持仓的 quote 确实就是"已有的调用结果"。**盲区在方案措辞，不在执行。**

### 补救路径已实测可行

网站已有的 `/api/quotes` 网关代理**可查询任意标的**，实测：

```
GET http://67.215.255.196:8788/api/quotes?symbols=AVGO,MU,AAPL
→ AVGO $396.81 · MU $959.48 · AAPL $325.89   （全部正常返回）
```

零新增成本、零新增依赖，只需把监控池标的也纳入查询范围。

---

## 3. 任务

### Phase 1：为量化监控池标的批量拉取报价

1. `App.tsx`（或新建 `src/monitoredQuotes.ts` 纯逻辑 + App 调用，择一，保持结构简单）：
   - 当量化快照就绪时，取 `Object.keys(snapshot.symbols)` 作为监控池标的清单；
   - **排除**已在持仓中且已有 `quote` 的标的（避免重复请求）与现金等价物；
   - 用已有的行情代理（`settings.quoteProxyUrl` / `getServerQuoteProxyUrl()`）**一次批量请求**（`/api/quotes?symbols=A,B,C`）；
   - 网关单次上限 50 个标的（网关代码 `.slice(0, 50)`），超出时**分批**请求；
   - 结果存入 state：`Map<symbol, price>`，命名如 `monitoredQuotes`。
2. **请求节流（重要）**：
   - 与量化快照刷新对齐，**每 25 分钟最多一次**；
   - 结果连同时间戳缓存到 localStorage（key `portfolio-tracker:monitored-quotes-v1`），刷新页面时若缓存未过期（< 25 分钟）直接复用，不发请求；
   - 行情源未配置（`quoteProvider === 'none'` 且无同源网关）→ 跳过，不报错。
3. 请求失败 → 保留上次缓存，UI 照常显示（价格会带"数据可能陈旧"含义，沿用现有 `~` 与偏差说明即可，**不新增红色报错**）。

**测试**：
- 监控池 60 个标的 → 分 2 批请求（50 + 10）；
- 已持有且有 quote 的标的不进入请求清单；
- 缓存未过期（24 分钟）→ 不发请求；过期（26 分钟）→ 发请求；
- 请求失败 → 返回上次缓存，不抛异常。

提交：`feat: fetch quotes for monitored symbols outside holdings`

### Phase 2：`depthQuotePrice` 支持持仓外报价

1. 改签名为 `depthQuotePrice(holdings, monitoredQuotes, rawSymbol)`：
   - **优先级 1**：持仓中的报价（用户自己的持仓数据最贴近其实际）；
   - **优先级 2**：`monitoredQuotes` 中的报价；
   - 都没有 → `null`（维持现有 `unavailable` 行为）。
2. `ConditionLookup` 接收新 prop `monitoredQuotes`，`App.tsx` 传入。
3. **不改** `buildDepthPriceView` 与推算公式。

**测试**：持有 NVDA → 取持仓报价；未持有 AVGO 但 monitoredQuotes 有 → 取该报价并算出高点/阈值价；两者都无 → `unavailable`。

提交：`feat: use monitored quotes for non-held depth windows`

### Phase 3：明确"反推高点会漂移"这一固有局限

**背景（重要，用户必须知道）**：真实的「250 日高点」是一个**固定值**，但用现价反推出的高点**会随现价波动而变化**。

举例（同一个 AVGO）：
| 时刻 | 现价 | 量化回撤 | 反推高点 |
|---|---|---|---|
| 12:44 ET | $384.98 | -21.80% | ~$492.30 |
| 20:00 ET | $396.81 | （若仍用 -21.80%）| ~$507.43 |

若量化的 `current_pct` 更新频率低于网站行情，反推高点会明显漂移 —— 用户会困惑"为什么高点一直在变"。

**修法**（纯文案，不改计算）：
- `source === 'derived'` 时，现有偏差说明后追加一句：**「高点为反推值，会随现价与量化回撤的更新时差而小幅变动；量化系统提供真实高点后此处将改为固定值」**；
- `source === 'quant'` 时不显示该句（真值不漂移）。

**测试**：`derived` 时文案包含"会随…变动"；`quant` 时不包含。

提交：`docs: explain derived high price drift`

---

## 4. 部署与验收

1. `npm run build` && `npm test`
2. `bash deploy/deploy-us-vps.sh`
3. `git push origin main`，等 Pages Actions

**总验收清单**：
- [ ] 三个 Phase 各自的失败测试输出 + 通过输出，均贴进报告
- [ ] `npm test` 全绿（新增 ≥ 8 用例）；`npm run build` 通过
- [ ] **本地 dist / 线上 VPS / 线上 Pages 三个 hash 一致**（贴出三个）
- [ ] `/api/health` 正常
- [ ] **线上实测**：打开一个未持仓的监控池标的（如 AVGO 或 MU），深度买入窗口**显示出现价、高点、阈值价**（带 `~` 与两条说明）
- [ ] 持仓标的（如 NVDA）价格仍正常显示，优先用持仓报价
- [ ] 25 分钟内重复打开页面不重复发请求（看 network 或日志）
- [ ] 行情源未配置时不报错、不白屏
- [ ] `git -C ~/Projects/futu-assistant status` 干净
- [ ] 每 Phase 独立 commit；无未提交改动

---

## 5. 明确不做

- 不改 `depthPrice.ts` 的推算公式（已验证正确）。
- 不新增数据源、不新增依赖（只用已有 `/api/quotes`）。
- 不为监控池标的做高频轮询（严格 25 分钟节流，与量化快照对齐）。
- 不自造买入信号（网站只展示价格与回撤位置）。

---

## 6. 后续

量化侧交付 `depth_window` 的价格真值字段（`current_price` / `high_price` / `threshold_price` / `next_level_price`，见 `需求单-量化侧-价格字段与估值序列.md` 需求 1）后：
- `source` 自动切为 `'quant'`，`~` 标记、偏差说明、漂移说明**全部自动消失**；
- 本轮的监控池报价拉取仍可保留作为兜底（量化字段缺失时的降级路径）。
