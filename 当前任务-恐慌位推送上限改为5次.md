# 当前任务：恐慌位推送上限改为 5 次 + 已买入后自助停止

日期：2026-07-23
执行方：**具备量化仓库权限的 Codex**
涉及仓库：**仅量化系统** `~/Projects/futu-assistant`（Python）

> **本文件自包含，是本轮唯一任务书。** 本轮**不改网站**，不涉及 `portfolio-tracker`。
> 共 4 个 Phase。

---

## 0. 用户诉求（原话）

> 「已经买了怎么停止」
> 「我们提醒只提醒 5 次就好了，不要重复提醒了」

用户在 21:42 收到 **4 条内容完全相同**的 TQQQ 恐慌位推送（"刚刚"×2、"2 分钟前"×2），价格、跌幅、金额全部一致：

```
🟢【可买】TQQQ 恐慌位触发 $67.08
TQQQ 符合 2 个购买条件，可以买入 $14,477.03（至目标仓位 20%）～当前跌幅 -4.55%
当前价 $67.08（盘中）
当前回撤 -22.91% / 阈值 -22.06%
3 倍族当前 3.29%
```

---

## 1. 根因（已核实，直接采信，不要重新排查）

### 1.1 上限配置过大：`max_cards: 40`

`config/rules.yaml:547`（`panic_window.notify` 块）：

```yaml
notify:
  first_burst_cards: 2
  first_burst_within_seconds: 60
  rotation_seconds: 20          # ← 每 20 秒轮播一张
  position_refresh_seconds: 60
  panic_first_clip_pct: 10
  max_cards: 40                 # ← 单个恐慌窗口最多 40 张卡
  max_window_minutes: 30
```

配合 `engine/panic_window.py:510`：

```python
if sequence >= int(notify["max_cards"]):
    self._stop_at_limit(now, "max_cards")
```

**即：一个恐慌窗口内，系统会以约 20 秒一张的节奏，最多推 40 张卡，或持续 30 分钟。** 用户看到的连续刷屏完全符合这个设定。

### 1.2 去重键带自增序号，对同一信号不去重（**设计如此，非 bug**）

`engine/panic_window.py:476–510`：

```python
sequence = self.state.card_count + 1                                   # 每推一次自增
symbol = symbols[(sequence - 1) % len(symbols)]                        # 多标的轮播
dedupe = f"panic:{symbol}:{now.date().isoformat()}:seq{sequence}"      # ← key 含 seq，每次都不同
...
self.state.card_count = sequence
```

因为 `dedupe_key` 里带了自增的 `seq{sequence}`，`push/bark.py` 的去重账本（`push_log` 表按 `dedupe_key` 主键去重）**永远拦不住**同一窗口内的重复推送 —— 这是"轮播提醒"的刻意设计，不是缺陷。

> 对照：`engine/stock_depth.py:118` 的键是 `stock-depth:{symbol}:{session}:{day}`，**不含序号**，所以同一标的同一时段一天只推一次。两条路径的设计取向不同。

### 1.3 现有的停止手段

- 推送正文已提示：`如在 IBKR 买入，请手动 mute 30m 停止提醒`；
- CLI 已有 `mute` / `unmute` 命令（`futu-assistant mute 30m` / `unmute`）；
- 但**用户不知道怎么用**，且需要打开终端 —— 手机上收到推送时无法立即操作。

---

## 2. 本轮改动范围（严格限定）

### 2.1 允许

- ✅ 修改 `config/rules.yaml` 中 `panic_window.notify` 的**推送节奏参数**（`max_cards` 等），**这是用户明确授权的产品决策**；
- ✅ 在推送正文中增加**如何停止**的可操作指引；
- ✅ 为上述改动补测试。

### 2.2 绝对禁止

- ❌ 修改任何**开窗/关窗判定逻辑、阈值、信号规则、止盈剧本**（`max_cards` 只影响"推几次"，**不影响"是否该买"的判定**，两者必须严格区分）；
- ❌ 修改 `panic_window` 的**触发条件**（`depth_threshold`、`panic_threshold` 等）；
- ❌ 修改 `scheduler.py` 的定时调度行为；
- ❌ 读取或输出 `config/channels.yaml`（密钥）；
- ❌ 写 `store.sqlite`（只读 `mode=ro` 可以）；
- ❌ 改动 `portfolio-tracker` 网站仓库（本轮不涉及）；
- ❌ 新增下单能力。

### 2.3 通用规则

1. 每个 Phase：**先写会失败的测试并确认它真的失败（贴失败输出）** → 变绿 → 仓库测试全绿 → **单独 commit** → **留验证报告**；
2. 不新增运行时依赖；
3. 密钥绝不进仓库或提交历史；
4. 中途不要停下来问方向；
5. 完工贴出：git log + 完整测试输出 + 一条实际推送样例。

---

## Phase 1：推送上限改为 5 次

1. `config/rules.yaml` 的 `panic_window.notify`：
   ```yaml
   max_cards: 5          # 原 40
   ```
2. **同步检查并调整**相邻参数，使 5 张卡的节奏合理（避免 100 秒内推完就永久静默）：
   - `rotation_seconds: 20` → 建议 **`120`**（2 分钟一张，5 张覆盖约 10 分钟）；
   - `first_burst_cards: 2` / `first_burst_within_seconds: 60` **保持不变**（开窗瞬间先给 2 张，符合"紧急事件要醒目"）；
   - `max_window_minutes: 30` **保持不变**（时间上限仍是 30 分钟兜底）。
   > 若你认为 `rotation_seconds` 有更合适的值，可自行判断，但**必须在报告中说明理由并给出 5 张卡的实际时间跨度**。
3. `limit_body_template` 文案随之生效（"已达到 {max_cards} 张或 {max_minutes} 分钟安全上限"），无需改模板。

**测试**：
- 构造一个持续开窗的恐慌窗口 → 推送第 5 张后触发 `_stop_at_limit(now, "max_cards")`，**第 6 张不再发出**；
- 断言 `state.card_count` 停在 5；
- 断言停止卡（`limit_title`）只发一次；
- 断言 30 分钟时间上限仍独立生效（构造慢节奏场景，卡数未到 5 但超 30 分钟 → 同样停止）。

提交：`fix: cap panic window pushes at five cards`

## Phase 2：推送正文加入「如何立即停止」

用户收到推送时在手机上，需要**一眼可执行**的停止方式。

1. 在 `panic_window.notify` 增加/改写停止指引文案（沿用现有 `ibkr_manual_mute_text` 字段或新增字段，**不新增推送通道**）：
   ```
   已买入？在 Mac 终端执行：futu-assistant mute 30m
   ```
   完整命令路径以实际安装位置为准（`~/Projects/futu-assistant/.venv/bin/futu-assistant mute 30m`）。
2. **卡片序号可见**：在正文中显示当前是第几张，让用户知道还会推几次：
   ```
   提醒 3/5 · 本窗口最多 5 次
   ```
   实现方式：`body_template` 增加占位符（如 `{sequence}` / `{max_cards}`），在 `_send_card` 处传入。
3. **不改**标题模板、不改分级、不改推送通道。

**测试**：
- 推送正文包含 `提醒 N/5` 且 N 随序号递增；
- 正文包含 mute 命令文本；
- 模板占位符缺失时**不抛异常**（降级为不显示序号）。

提交：`feat: show push sequence and stop instruction`

## Phase 3：`mute` 期间行为明确化

1. 核实并在测试中固定以下行为（**若现状已正确则只补测试，不改代码**）：
   - `mute` 生效期间，恐慌位卡片**不发送**；
   - `mute` 期间被抑制的卡片**是否计入 `card_count`**？
     - **要求**：**不计入** —— 静音结束后若窗口仍开启，用户应还能收到剩余额度的提醒，而不是"静音期间额度被悄悄耗尽"；
     - 若现状是计入的，改为不计入，并在报告中说明改动点。
   - `unmute` 后立即恢复推送。
2. **critical 级别推送**（如账户回撤熔断）在 mute 期间的行为**保持现状不变** —— `mute` 命令的帮助文本写的是 "Mute **non-critical** pushes"，不得改变这一语义。

**测试**：
- mute 期间开窗 → 无卡片发出；
- mute 期间 `card_count` 不增长；
- unmute 后窗口仍开 → 继续从剩余额度推送；
- critical 类推送在 mute 期间行为与改动前一致（回归断言）。

提交：`fix: do not consume push quota while muted`

## Phase 4：验证与回归

1. **完整测试套件全绿**（约 212 项）；
2. **判定零变化**（最重要）：本轮只改推送节奏，**不得影响任何买卖判定**。请证明：
   - 同一份输入数据下，恐慌窗口的**开窗/关窗结论**与改动前完全一致；
   - `depth_threshold` / `panic_threshold` / 目标仓位计算等**均未变化**；
   - 贴出改动前后的对照输出。
3. **实际推送验证**：触发一次真实或模拟的恐慌窗口，贴出：
   - 收到的推送条数（应 ≤ 5）；
   - 一条推送的完整正文（含 `提醒 N/5` 与 mute 指引）；
   - 达到上限后的停止卡文案。

提交：无（验证报告）

---

## 3. 总验收清单

- [ ] 各 Phase 失败测试输出 + 通过输出已贴
- [ ] `config/rules.yaml` 中 `max_cards: 5`；`rotation_seconds` 调整值及理由已说明，并给出 5 张卡的实际时间跨度
- [ ] 第 6 张卡不再发出；`card_count` 停在 5；停止卡只发一次
- [ ] 30 分钟时间上限仍独立生效
- [ ] 推送正文含 `提醒 N/5` 与可执行的 mute 命令
- [ ] mute 期间不发卡片、**不消耗额度**；unmute 后可继续
- [ ] critical 推送在 mute 期间行为未变（回归断言）
- [ ] 量化仓库测试套件全绿（约 212 项）
- [ ] **判定零变化**：开窗/关窗结论与阈值计算与改动前完全一致（贴对照输出）
- [ ] 未改 `scheduler.py` 调度；未触碰 `config/channels.yaml`；未写 `store.sqlite`
- [ ] 未改动 `portfolio-tracker` 网站仓库
- [ ] 每 Phase 独立 commit；无未提交改动

---

## 4. 明确不做

- 不改开窗/关窗判定逻辑、阈值、信号规则、止盈剧本
- 不改恐慌位触发条件
- 不改 `scheduler.py` 定时调度行为
- 不改 `stock_depth` 路径的去重逻辑（它本就是一天一次，正常）
- 不新增推送通道、不改推送分级
- 不触碰 `channels.yaml`、不写 `store.sqlite`
- 不改网站仓库

---

## 5. 背景说明（供执行者理解取舍）

`panic_window` 的多卡轮播是**刻意设计**：恐慌位是稍纵即逝的买入窗口，重复提醒是为了避免用户错过。`dedupe_key` 含自增序号正是为此。

但 **40 张 / 30 分钟**对已经完成买入的用户是纯粹的骚扰。用户的诉求是把上限降到 **5 次**，并让"已买入后如何停止"变得显而易见。

**因此本轮只调节奏与可停止性，绝不触碰"该不该买"的判定** —— 这是两件不同的事，必须分开。
