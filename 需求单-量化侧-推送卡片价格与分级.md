# 需求单（给量化系统 Codex）：Bark 推送卡片补价格 + 重要性分级

日期：2026-07-15
提出方：网站侧大脑（Claude），代表最终用户
执行方：**量化系统 `~/Projects/futu-assistant` 的维护 Codex**
> 本文件只是需求单，放在网站仓库仅为便于用户转交。**网站侧 Codex 不执行本文件。**

---

## 0. 前置声明（诚实边界）

提出方**没有读过 futu-assistant 的源码**（按契约无权修改，也未越界阅读推送模块）。因此本文件是**需求规格 + 验收标准**，不指定具体文件与函数；请执行方自行在 `push/`（契约文档标注为「Bark 卡片」）与相关卡片生成处定位。

**本需求只改推送卡片的"呈现"，不改任何判定逻辑、阈值、信号规则。** 若某项要求会牵动判定口径，停下并回报，不要自行调整规则。

---

## 1. 用户实测反馈（原话要点）

> 「它只是给我报的这个距离高点下跌百分之多少，但是它没有给我报价格……如果出现那种可买的，播报可买的，就是单独播报，或者说播报的时候加一些提醒的事项……重要事项的话，它在播报的时候需要重点提醒，要不然用户看了一眼，因为它是一个弹窗，用户看了一眼发现好像不重要，它可能就略过去了。」

对应到实际推送卡片（用户 2026-07-15 截图）：

| 卡片 | 现状 | 问题 |
|---|---|---|
| `MU 深度买入窗口` | `夜盘价 $966.83｜当前回撤 20.32% / 阈值 20.23%｜QQQ -2% 恐慌参考：未触及` | **有价格，格式是对的**，可作为其他卡片的范本 |
| `AVGO / MSFT 深度买入窗口` | 同上，有夜盘价 | 同上 |
| `三倍回撤日报 · 10:00` | `TQQQ 距高点 -18.0%（周期第 33 日）／历史:中位-22.1 均-27.2 最深-81.7／当前档(≥15%)…／下一档 -20% 还差 2.0%` | **通篇没有任何价格**，用户无法判断"现在多少钱、跌到多少才是下一档" |
| 全部卡片 | 标题与正文视觉权重一致，无分级 | 「可买入」这类可行动事件淹没在日报里，一眼扫过就被略过 |

---

## 2. 需求

### 需求 1：所有含"回撤/距高点"的卡片必须同时给出价格

适用：`三倍回撤日报`、`晨间行动汇总`，以及任何只报百分比的卡片。

每个标的一行至少包含：
- **当前价**，并标注取价时段（夜盘/盘前/盘中/盘后）——与深度买入窗口卡片现有写法保持一致；
- **对应的 250 日高点价位**（这样 -18% 才有实感）；
- **下一档位对应的价格**：现有文案是「下一档 -20% 还差 2.0%」，请补成「下一档 -20%（约 $XX.XX）还差 2.0 点」。

范例（目标形态）：
```
TQQQ  $52.30（夜盘）· 距高点 -18.0%（高点 $63.78）· 周期第 33 日
  下一档 -20%（约 $51.02）还差 2.0 点
```

要求：价格与百分比来自**同一次取价**，不得一个用实时、一个用昨收；取不到价时写「价格暂缺」，**不允许留空或用旧价冒充**。

### 需求 2：可行动事件与日常播报分级，且标题自带结论

用户只看一眼弹窗标题，**结论必须在标题里**。

分三级，建议用 Bark 的 `level` / `group` / `sound` 参数实现（**具体参数以你侧 Bark 服务端与 App 版本文档为准**；若不支持某项，用可用手段等效达成，并在回报里说明）：

| 级别 | 适用事件 | 建议呈现 |
|---|---|---|
| **高（可行动）** | 深度买入窗口开启、恐慌位触发、卖出窗口开启 | 标题前缀 `🟢【可买】` / `🟠【可卖】`；`level=timeSensitive`（用户若已授权关键警报可用 `critical`）；独立 `group`（如「行动窗口」）；专用提示音 |
| **中（状态变化）** | 接近阈值（还差 ≤2 点）、窗口关闭、观察期转正式 | 标题前缀 `🟡【接近】`/`⚪【状态】`；`level=active`；同「行动窗口」组或独立组 |
| **低（日常）** | 三倍回撤日报、晨间汇总、无变化的例行播报 | 无前缀或 `📊`；`level=passive`；独立 `group`（如「日报」），不打扰 |

标题范例（**结论 + 关键数字**，不要只写标的名）：
```
🟢【可买】MU 深度位已开 $966.83（回撤 20.3%/阈值 20.2%）
🟡【接近】TQQQ 距下一档 -20% 还差 2.0 点（$52.30）
📊 三倍回撤日报 · 3 只监控中，0 只达标
```

### 需求 3：可行动事件独立播报，不与日报混在一条

用户明确要求「可买的单独播报」。要求：
- 深度位开启 / 恐慌位触发 / 卖出窗口开启，**每个标的单独一条推送**，不得只在日报正文里带一句；
- 日报正文保留这些标的的明细即可，但日报本身维持"低"级别；
- 若同一时刻多个标的同时达标，允许合并为一条「🟢【可买】3 个标的达标：MU / AVGO / MSFT」，但正文必须逐个列出价格与阈值。

### 需求 4：日报开头给一行结论摘要

`三倍回撤日报` 和 `晨间行动汇总` 的**第一行**先给结论，再给明细：
```
今日：0 个可买 · 1 个接近（TQQQ 差 2.0 点）· 0 个可卖
```

---

## 3. 明确不做

- 不改任何阈值、信号规则、开窗判定逻辑（本需求纯呈现层）。
- 不新增下单能力（系统永远只提醒不下单）。
- 不因为要"更醒目"而把非可行动事件也提级 —— 全部标高等于没有分级。
- 不在推送里出现任何密钥、账户号、券商 token。

## 4. 验收标准

- [ ] 三倍回撤日报每个标的都带：当前价（含时段）、高点价、下一档对应价；取不到价显示「价格暂缺」
- [ ] 手动触发一次可买事件 → 收到**独立**推送，标题含 `🟢【可买】`、结论与价格，落在独立分组
- [ ] 日报推送为低打扰级别，标题不含行动前缀，第一行是结论摘要
- [ ] 三个级别在手机上实测可区分（截图对比）
- [ ] 判定逻辑与历史输出未变化：同一份输入数据下，开窗/不开窗的结论与改动前完全一致（用现有测试或对照跑验证）
- [ ] 未泄露任何密钥；未改动 `config/channels.yaml`
