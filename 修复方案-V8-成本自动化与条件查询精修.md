# 修复方案 V8：成本自动化 + 条件查询精修 + 卖出窗口

> 撰写：Claude（审计大脑），2026-07-16
> 执行：Codex（唯一施工者）
> 地位：网站仓库第八期施工指令，延续 V7。凭证与授权沿用 V7 第 0 节（网站 VPS SSH 直接用；量化仓库允许改"只读导出层 + 本期明确点名的 YAML/聚合修正"；channels.yaml 仍禁读）。
> 用户核心不满一句话：**"什么都有但什么都不精明，很潦草"——本期目标是把每个面板的口径、来源、文案、数字格式做到讲得清楚、不用用户猜、不用用户填系统本来就知道的东西。**

---

## 1. 审计发现（逐条带证据，先读完再动工）

### A1（P0·数据正确性）本地风险扫描的浮亏数字离谱：MSFU -75.3%、MSFT -39.1%

- 用户确认这两个数字不对。
- **最可疑根因（Codex 必须先写失败测试证实/证伪）**：`futu-assistant/engine/site_export.py:62 aggregate_holding_costs` 按 `symbol` 分组聚合成本——而 MSFU 同时存在 **2× ETF 正股持仓** 和 **MSFU 期权合约**（期权 cost_basis 是权利金口径、另有 100 乘数），若分组时没有排除 `asset_type == "option"` 或没按资产类型分开，加权平均成本被期权污染 → 网站按这个"成本"算盈亏 → 假浮亏。
- 排查路径：① `aggregate_holding_costs` 是否含期权行；② 网站侧 `holdingCosts` 被应用到持仓 `buyPrice/costOverride` 的位置是否把"同 symbol 的 ETF 行和期权行"都套了同一个成本；③ IBKR/富途/长桥同 symbol 多行（跨券商）合并时的加权是否用了错误数量口径。
- 修复口径：成本聚合**只统计非期权行**；期权成本永远单列（premium 口径）；跨券商同标的按股数加权；任何一行成本未知 → coverage 降级 `partial/unavailable`，绝不编数。
- 验收：修复后 MSFU（ETF 行）与 MSFT 的盈亏% 与券商 App 显示一致（用户人工核对一次并确认）。

### A2（P0·产品）目标提醒逼用户手填系统已知的信息

- 现状：`src/components/AlertRulesPanel.tsx` 股票代码是自由文本输入（截图实证）；涨幅规则要用户确认/填写成本（`costConfirmed`、`form.costBasis`，AlertRulesPanel.tsx:24-48,111）。
- 用户要求：**代码改成"从当前持仓选择"的下拉框，锁定住**（三券商持仓系统一清二楚，期权归到底层标的）；**成本自动带出**（数据链路已通：券商 cost_basis → site_export `holding_costs` → 前端 `props.holdingCosts`），不再让用户填。
- 设计：
  - 下拉选项 = 当前快照全部持仓的底层标的（去重、期权归底层、显示"代码 · 当前市值 · 来源券商"）；不允许自由输入（用户原话"锁定住"）。
  - 成本行改为只读展示："成本 $X（券商加权）/ 成本不可用"。
  - `coverage == unavailable` 的标的：**涨幅类规则选项禁用**并说明"券商未提供成本，仅可用目标价规则"——不回退到手填。
  - 目标价类规则不需要成本，任何持仓标的均可建。

### A3（P0·产品）删除"分析"Tab 与总览"本地风险扫描"

- 用户原话："分析这个位置……把这个关卡给删掉"；"本地风险扫描……去掉吧，不需要这个东西"。
- 现状锚点：`src/App.tsx:3,9,13`（AnalysisPanel/RiskList/analyzePortfolio import）、`App.tsx:342`（findings 计算）、`App.tsx:490`（总览 RiskList）、`App.tsx:512-513`（分析 Tab 内容）。
- 施工：删除"分析"Tab 按钮与路由；删除总览 RiskList 块；随之删除 `AnalysisPanel.tsx(+test)`、`RiskList.tsx`、`analyzer.ts`、`kimi.ts::analyzeWithAi` 及其引用（功能整体移除，允许删文件；智谱**截图导入**链路一根手指都不许碰）。
- 注意：A1 的成本修复不受此删除影响——持仓表的盈亏列仍在用成本，所以 A1 必须修，不能"删了扫描就当没事"。

### A4（P1·口径与文案）条件查询重构：下拉选择 + 分资产类型条件 + 删"当日熔断"

用户三条明确反馈（2026-07-16 口述）：

**A4-1 输入框改下拉，禁止自由搜索。**
- 原话："不要用搜索……我们监控了哪几个就只有那几个，让用户直接下拉框选择就好了，要不然用户可能又想东想西的，想买各种各样的东西。"
- 施工：`ConditionLookup.tsx` 的文本输入 + 查询按钮 → 下拉框，选项 = site_export 快照里的监控池标的（当前 21 只，以快照 `symbols` 键为准动态渲染，**不许在前端写死列表**）；现金类（SGOV 等）过滤掉；不提供任何自由输入路径。监控池增减是量化侧 `symbol-add` 的事，本期不做入口。

**A4-2 个股不用"价格低位区"当条件——它只适用于 3 倍杠杆/ETF。**
- 原话："苹果、微软、特斯拉这种大科技……要跌到 15% 太夸张了，基本只有财报日才可能有；就算当天跌 15%~20% 也不可能当天去买（情绪消化不完，要等第二第三天）。低位区这个讲的应该是三倍杠杆 ETF——TQQQ、SOXL、FNGU 这种。个股应该是没有的，这一个是有问题的。"
- 口径定案：
  - **个股（stock 类型）**：市场条件 = 买入信号 + 估值/情绪，共 2 项（个股的"低位"衡量就是第六关里的 5 年 PE 分位 ≤30，已经覆盖，不需要价格回撤关卡）。"距 250 日高点回撤 X%"降级为**参考信息行**，不参与计数、不打 ✓/✗。
  - **ETF / 杠杆 ETF（etf1x/etf2x/etf3x）**：市场条件 = 低位区 + 买入信号 + 估值/情绪，共 3 项。低位区文案："距最近 250 个交易日高点回撤 X%，达到 -Y%（{1×10%/2×20%/3×28%}口径）进入低位区"。
  - 汇总行按类型显示"市场条件满足 X/2"或"X/3"。
- **量化侧配套（获准范围，放宽类）**：买入行动过滤的 low_zone 关对 stock 类型豁免（新增 YAML 键，如 `notification_delivery.low_zone_gate_types: [etf1x, etf2x, etf3x]`，代码读配置判断适用性；个股买入卡的市场门变为"信号 + 第六关估值情绪"）。这放宽了个股买入卡的触发条件，按 `rule_change` 纪律（24h 冷静 + TDD 先红后绿）执行。site_export 的每个 gate 输出增加 `applicable: true/false` 字段，前端按它渲染，**前端不得自己按资产类型硬编码判断**。
- ⚠️ 范围红线：只动"买入行动过滤门"和展示。`low_zone_entry`/`low_zone_milestone` 低位里程碑**推送**（MU -15%/-20% 档位提醒）和 TQQQ/FNGU/SOXL 的 T15 深回撤里程碑**一概不动**。

**A4-3 "当日熔断"从条件查询卡整体删除。**
- 原话："当日熔断这个应该基本上不会出现，很少……我应该没有说过当日熔断这个事情，这应该是一个 bug，要删掉。"
- 施工：`ConditionLookup.tsx:14-19` 的 gate 列表删除 `daily_fuse` 行（连折叠组都不进）；site_export 可继续输出该字段（向后兼容），前端不渲染。
- ⚠️ 只删**展示**：量化引擎的每日熔断（每日最多 2 笔/新增 ≤5%）是执行方案 v1.9 的三防线之一，**引擎行为保留不动**——它拦的是"一天连买多笔"的手滑，触发时有自己的推送卡。若用户想连引擎一起删，须另行明确指令。

**A4-4 剩余纪律状态组** = 仓位门 + 批次，默认折叠，组标题："纪律闸门（决定允许买多少），不是行情判断"。

**A4-5 数字格式**（截图实证的潦草点）："下一批 1.00 / 共 3.00 批"→"第 1 批 / 共 3 批"；"今日已买 0.00"这类计数一律整数；百分比统一 2 位小数。

### A5（P1·策略口径，量化侧）恐慌开窗阈值：CNN < 30

- 用户口径："恐慌的时候就可以开启窗口了。所谓恐慌，就是 CNN 恐慌贪婪指数在 30 的下方。"估值查询保留。
- 现状：估值第六关的情绪触发用的是 `cnn_fear_greed.fear_extreme_max: 10`（极值口径）。
- 施工（量化仓库，属获准范围）：`config/rules.yaml` 的 `valuation_context.cnn_fear_greed` 新增 `fear_open_max: 30`；`engine/valuation_gate.py` 的第六关 OR 条件改用 `fear_open_max`；**`fear_extreme_max: 10` 保留**，继续供反弹基准锚（rebound 状态机极值入场）使用，两个阈值语义不同，不许合并。
- ⚠️ 这是**放宽类**策略变更：按仓库 `rule_change` 纪律执行（loosen_cooling_hours: 24），TDD 先行，并在提交信息里注明放宽内容；网站条件查询卡的估值/情绪 reason 文案同步显示"CNN {值}（<30 恐慌开窗）"。

### A6（P1·新功能）卖出条件查询（对称于买入）

- 用户要求：条件查询下方加"卖出窗口"查询——从持仓中选一只，显示"现在卖出窗口开没开、依据什么"。
- 口径 = 量化系统 v2.2 卖出体系（不发明新规则）：
  1. **修复期状态**：`rebound_cycle_state` 为 repairing → 显示"卖出窗口未开启：深跌修复期内，耐心持有（基准日 {date}）"；complete → "修复完成，可开始分批减仓：优先减期权与两三倍杠杆，不要一次性减完"。
  2. **知足常乐**：该标的自基准日涨幅 vs QQQ 涨幅（差距≤3 点或已超越 → "建议至少减仓 50%"）。
  3. **补涨收敛**：大科技 ≥5 只追平 QQQ 时显示"市场亢奋·清仓杠杆或调仓 SGOV"。
  4. **止盈阶梯参考**：该标的 playbook 的 sell_steps（盈利 5-7%/7-10%/10%+ 对应减仓档）与 `risk_first_order`。
  5. 近期触发的 sell 向信号（若有）。
- 数据来源：扩展 `engine/site_export.py` 增加 `sell` 区段（每个**持有**的族一条；调用 rebound/playbook 现有引擎方法，禁止复制公式）。前端 `ConditionLookup.tsx` 加"卖出窗口"区，选择器只列当前持仓（同 A2 下拉逻辑复用）。
- 固定尾行："只提醒不下单；由你在券商 App 手动执行。"

### A7（P2·杂项细节，用户"潦草感"的具体来源）

1. 总览"下一步建议补充"出现**两条一模一样的 MSFU 期权增强提示**（截图实证）：`App.tsx:589` 按 issue 逐条渲染，多个 MSFU 合约各产生一条 → 按 `field+reason` 去重，或合并为"MSFU 期权 ×2 需补充"。
2. 目标提醒列表的"当前价"没有时间戳 → 加"@ HH:MM"来源时间；盘外显示"上一交易日收盘"。
3. "创建提醒"按钮无提交中状态 → 提交期间禁用+loading 文案，防双击建重复规则。
4. 条件查询"刷新快照"按钮：点击后无成功/失败反馈 → 显示"快照 {generated_at}，X 分钟前"。
5. 已保存规则卡的"距目标 13.35%"应注明方向（"还需上涨 13.35%"）。
6. 全站百分比/金额小数位统一：金额 2 位、百分比 2 位、计数整数（与 A4 同一批做）。

---

## 2. 施工顺序与验收

| 步 | 任务 | 验收 |
|---|---|---|
| 1 | A1 成本聚合 root-cause：先写失败测试复现期权污染（fixture：MSFU ETF 行 + MSFU 期权行 + 跨券商行） | 量化仓库 pytest 先红后绿；`git status` 干净除意图内文件 |
| 2 | A2 目标提醒下拉锁定 + 成本只读自动 | `npm test` 新用例：期权归底层去重、unavailable 禁用涨幅规则 |
| 3 | A3 删分析 Tab + 风险扫描 | build/lint 绿；总览与 Tab 栏无残留入口；无 dead import |
| 4 | A4 量化侧先行：low_zone 门类型豁免（YAML+gate+applicable 字段导出）+ A5 CNN fear_open_max: 30（两项放宽合并走一次 24h 冷静纪律，TDD） | 量化 pytest 先红后绿；site-export 输出可见 `applicable` 字段与新阈值 |
| 5 | A4 前端：下拉框（监控池）、个股 2 项/ETF 3 项分组、删"当日熔断"行、纪律组折叠、数字整数化 | 快照测试：AAPL 卡无低位区关卡且计数 X/2；SOXL 卡 X/3；卡内无"当日熔断"字样 |
| 6 | A6 卖出窗口（quant sell 区段 → 网关 → 前端） | 真实快照演示：持有的 TQQQ/MSFT 各出一张卖出窗口卡，修复期内显示"未开启" |
| 7 | A7 杂项六条 | 逐条截图对比 |
| 8 | 部署回归 | V7 第 5 节第 7-8 步同款：build → deploy-us-vps.sh → pm2 --update-env → 25 条持仓/净值对账/IGV/MSFU 分类回归；两仓库测试基线不倒退（网站≥V7 基线，量化 242+） |

---

## 3. 易错点（在 V7 W-1~W-15 全部继续有效的基础上新增）

- **X-1 成本聚合改动会牵动纸面组合吗？** `aggregate_holding_costs` 目前只被 site_export 用（先 grep 确认引用面），若还有其他消费者，修改口径前逐个核对，别顺手改了纸面组合的成本逻辑。
- **X-2 期权归底层下拉**：MSFU 期权底层是 MSFU（自身是 2×ETF），再往下才是 MSFT——下拉按"持仓行的直接标的"归组即可（MSFU 一项、MSFT 一项），不要做二级穿透，否则用户找不到 MSFU。
- **X-3 删除组件的连带**：AnalysisPanel 测试、analyzer 测试、`analyzeWithAi` 在 kimi.ts 里的导出与类型全要清干净，`npm run lint` 的 no-unused 是裁判；但 kimi.ts 的截图识别函数一个不许动。
- **X-4 卖出区段只对持仓族生成**：不持有的标的显示"未持有，无卖出窗口可查"；数据来自快照持仓，不要在前端猜。
- **X-5 rebound 影子期状态**：卖出窗口的依据卡（知足常乐/补涨收敛）在量化侧仍处 shadow 未转正期间，网站展示必须带"（观察期，未正式生效）"角标——读 `rules.yaml` 的 `shadow.human_confirmed`，导出时带上该标志。
- **X-6 fear_open_max 只改第六关**：全仓库 grep `fear_extreme_max` 消费点，rebound 状态机入场判定绝不能被顺手改成 30。
- **X-7 下拉选项的现金类过滤**：SGOV 等 cash_like 不该出现在目标提醒/卖出查询/条件查询下拉里，按现有 `assetClass.ts::isCashEquivalent` 过滤。
- **X-8 个股低位区豁免的爆炸半径**：只改两处——买入行动过滤门的适用类型 + 条件查询展示。全仓库 grep `low_zone` 消费点逐一核对：`low_zone_entry`/`low_zone_milestone` 推送、`notification_delivery.actionable_buy_signals` 里的 `low_zone_entry` 信号、T15 三倍里程碑，全部保持原行为。改完跑量化全量 pytest，`tests/test_notification_policy.py` 的既有用例是护栏。
- **X-9 gate 适用性由后端声明**：site_export 每个 gate 加 `applicable` 字段，前端只按字段渲染。禁止前端出现 `if (assetType === 'stock')` 这类口径硬编码——口径只能活在量化系统 YAML 里，否则两边迟早打架（这就是屎山的起点）。
- **X-10 daily_fuse 只删渲染**：前端删行，类型定义里字段保留可选（向后兼容旧快照）；量化引擎、每日熔断推送卡、site_export 输出都不动。防止 Codex "顺手"把引擎防线拆了。
- **X-11 下拉框数据源单一**：条件查询下拉用 site_export `symbols` 键；目标提醒/卖出查询下拉用持仓快照。两个来源不同是有意的（前者=监控池，后者=实际持仓），不要合并成一个共享列表组件里的同一份数据。共享的是"下拉组件"，不是"数据源"。

---

## 4. 已由用户拍板的口径（2026-07-16 口述确认，Codex 不必再问）

1. A3 删除是整个功能删除（组件文件+测试一并删）。
2. A5 的 30 是"开窗"阈值；深跌极值锚（10）保留不动。
3. A4-1 条件查询用下拉框、只列监控池、禁自由搜索。
4. A4-2 个股不用价格低位区当条件（只适用 ETF/杠杆 ETF）；个股低位=PE 分位（第六关已覆盖）。
5. A4-3 "当日熔断"从条件查询卡删除（引擎防线保留——若用户还想删引擎，另行指令）。
6. A2 成本零手填：自动带出或明确"不可用+禁用涨幅规则"，没有第三种状态。

## 5. 仍需用户配合的一件事

- A1 验收：修复后拿券商 App 核对一次 MSFU/MSFT 盈亏%，确认与网站一致。
