# 需求单（给量化系统 Codex）：快照补价格字段 + 导出历史 PE 序列

日期：2026-07-22
提出方：网站侧大脑（Claude），代表最终用户
执行方：**量化系统 `~/Projects/futu-assistant` 的维护 Codex**
依据：《网站接入指南_量化系统持仓数据契约》§4 —— 「需要新字段/新导出：不要自己改 futu-assistant 仓库，把需求提回给大脑，由量化侧 Codex 实现导出契约。」

> 本文件是需求规格 + 验收标准。提出方**没有读过 futu-assistant 源码**（按契约无权修改），故不指定具体文件与函数，请执行方自行定位。
> **本需求只增加导出字段，不改任何阈值、信号规则、开窗判定逻辑。** 若某项要求会牵动判定口径，停下并回报。

---

## 需求 1：分析快照的 `depth_window` 补价格字段（高优先级）

### 现状问题

网站从快照拿到的 `depth_window` 结构如下（网站侧类型定义，与实际导出对应）：

```ts
depth_window: {
  applicable, open,
  current_pct,        // -21.80  当前回撤
  threshold_pct,      // -16.24  开窗阈值
  price_session,      // "盘中"  仅时段标签
  win_rate_60d, n, sample_insufficient, bear_included
}
```

**全结构没有任何价格字段。** 于是网站只能显示「当前回撤 21.80% / 阈值 16.24%」，用户的原话：

> 「价格要给，不然怎么知道是多少，一直在波动……我不知道到哪一个价格的时候它的百分比是跌幅了 20%。」

网站侧已做的临时方案：用自己的行情代理（Yahoo/NASDAQ 延迟价）反推高点与阈值价，UI 标注为推算值。**但这会与量化的取价时段（夜盘/盘中）不同源，产生偏差**，只是过渡方案。

### 要求

`depth_window`（以及恐慌位 `panic` 对应结构，如适用）增加四个字段，**全部使用与 `current_pct` 完全相同的那一次取价**：

| 字段 | 含义 |
|---|---|
| `current_price` | 计算 `current_pct` 时所用的当前价 |
| `high_price` | 该回撤所基于的 250 日高点价 |
| `threshold_price` | 跌到该价即达到 `threshold_pct` 开窗阈值 |
| `next_level_price` | 下一档位（如 -25%）对应的价格；无下一档时为 `null` |

补充要求：
- **价格与百分比必须同源同时刻**（用同一次取价计算），不得一个实时一个昨收；
- 取不到价时字段为 `null`，**不得用旧价冒充**；
- 保留现有 `price_session`，网站会一并展示；
- 币种统一 USD（与契约 §3.3 一致）。

### 验收

- [ ] 同一份输入下，`current_pct` 与 `current_price / high_price - 1` 数值自洽（容差 0.01 个百分点）
- [ ] `threshold_price` 满足 `threshold_price / high_price - 1 == threshold_pct`（同容差）
- [ ] 取价失败时四个字段为 `null`，快照其余部分正常生成
- [ ] **判定逻辑零变化**：同一输入下开窗/不开窗结论与改动前完全一致（用现有测试或对照跑证明）

---

## 需求 2：导出历史 PE 序列，用于估值分位可视化（中优先级）

### 背景

用户看到彭博终端的 GOOG 远期 PE 历史曲线（2020–2026，当前 22.65），希望自己的系统也能看到"当前估值在历史上的哪个位置"。

**调研结论**：Koyfin（用户在 X 上看到的平替）**没有公开 API** —— 官方说明其数据商限制开放 API，仅 Enterprise 套餐（$59/月）含 API，Plus $39/Pro $79 均无。**不建议采购。**

**更好的路径**：量化系统**已经在算** `stock_percentile`（个股 PE 分位）、`ndx_percentile`（纳指100 PE 分位）、`soxx_percentile` —— 网站界面上已显示「个股 PE 分位 20.40%」。能算出分位，说明手里已有历史 PE 序列。**把序列导出即可画曲线，零额外数据成本，且口径与开窗判定完全统一。**

### 要求

新增只读导出（形式二选一，你侧择优）：

**方案 A（推荐）**：`data/pe_history.json`，与现有 `data/drawdown_stats.json`、`data/event_study.json` 同目录同风格，供网站按契约 §4 只读消费：

```json
{
  "generated_at": "2026-07-22T12:44:00-04:00",
  "metric": "forward_pe",
  "lookback_years": 5,
  "symbols": {
    "GOOG": {
      "current": 22.65,
      "percentile": 20.40,
      "series": [
        {"date": "2021-07-22", "value": 26.31},
        {"date": "2021-07-23", "value": 26.12}
      ]
    }
  }
}
```

**方案 B**：在现有分析快照的每个 symbol 下增加 `pe_history: { current, percentile, series[] }`（若快照体积可控）。

补充要求：
- 明确标注 `metric` 是**远期 PE（forward）还是 TTM** —— 两者不可混用（彭博图用的是 BEst P/E，即远期）；
- 序列采样频率：日频最佳；周频亦可接受，请在 `generated_at` 同级注明频率字段；
- 覆盖范围：至少涵盖量化监控池内标的；无数据的标的可省略；
- `percentile` 的定义写进导出（例如"当前值在 lookback 期内的百分位，0=最低，100=最高"），避免网站误读方向。

### 验收

- [ ] 导出文件可被 `json.loads` 正常解析，含 `metric`、频率、`percentile` 定义说明
- [ ] 抽查一个标的：`percentile` 与 `series` 自行计算的分位一致（容差 1 个百分点）
- [ ] 与快照中已有的 `stock_percentile` 口径一致（同一标的同一时点数值相同）
- [ ] 文件体积可控（单文件 < 10MB；超出请改为按标的分片或降采样并说明）
- [ ] 未改动任何判定逻辑；未泄露密钥

---

## 需求 2 补充要求（2026-07-22 加强，优先级提升为高）

用户已确定两套估值基准规则，网站侧方案 `修复方案-V15-PE估值双基准与免费数据源.md` 依赖本导出。以下为**硬性**要求：

### 补充 1：口径必须显式标注，且不得混用（硬性验收项）

- 导出中 `metric` 字段**必须**明确是 `forward_pe`（远期，基于分析师一致预期）还是 `ttm_pe`（过去 12 个月实际）；
- 用户参照的彭博图使用的是 **BEst P/E = 远期 PE**，网站侧另一路数据源（Alpha Vantage `ForwardPE`）也是远期口径；
- **若你侧现有 `stock_percentile` / `ndx_percentile` / `soxx_percentile` 用的是 TTM 口径，请在回报中明确说明** —— 网站会把两种口径分开展示、禁止相减，口径不明会导致误导性对比；
- 同一个导出文件内**不得混装**两种口径；确需同时提供，请用两个独立 metric 字段或两个文件。

### 补充 2：覆盖指数标的（用于杠杆 ETF 的底层估值）

用户持有 TQQQ / SOXL / FNGU / SPXL / UDOW 等指数型杠杆 ETF，**这些 ETF 自身没有 PE**，须用底层指数估值。请在序列中覆盖（有哪些给哪些，缺的注明）：

| 指数 key | 对应 | 用于 |
|---|---|---|
| `NDX` | 纳斯达克 100 | TQQQ、QLD、TECL(近似) |
| `SOX` | 费城半导体 | SOXL |
| `SPX` | 标普 500 | SPXL、UPRO、SSO |
| `DJI` | 道琼斯 | UDOW |
| `FANGPLUS` | NYSE FANG+ | FNGU |

你侧已在算 `ndx_percentile`、`soxx_percentile`，说明 NDX / SOX 的序列已存在，优先导出这两个即可，其余尽力而为。

### 补充 3：序列必须覆盖 2025 年 4 月（关税冲击期），用于锚点计算

用户的指数估值规则是：**以 2025 年 4 月关税冲击期间的最低估值作为参考锚点**，当前估值接近该锚点时提示（"接近就可以，百分位不用很高"）。

因此：
- 序列的历史范围**必须覆盖 2025-04-01 ~ 2025-04-30**（越长越好，个股规则另需近 5 年）；
- 若该区间有数据缺口，请在导出中注明缺失日期，不要静默跳过；
- **外部参照**：公开报道称纳指100 forward PE 在 2025-04-08 跌至约 **21.6**（2022 年 11 月以来最低）。若你侧算出的该期间 NDX 最低值与此差异较大，请在回报中说明原因（口径差异 / 成分口径 / 数据源不同），**不必强行对齐**，但需让网站侧知道差异来源。

### 补充 4：个股序列需覆盖近 5 年

用户对个股（含 TSLA / MSFT / NVDA / GOOG 等大科技）的规则是**近 5 年 forward PE 均值**作为基准。请确保监控池内个股的序列至少覆盖 5 年（1825 天）；不足 5 年的标的请在导出中注明实际起始日期，网站会据此标注"样本仅 N 个月"。

### 补充验收

- [ ] `metric` 明确标注 forward / TTM；回报中说明现有 percentile 字段的口径
- [ ] NDX、SOX 序列已导出且覆盖 2025-04 全月
- [ ] 监控池个股序列覆盖近 5 年，不足者注明起始日期
- [ ] 数据缺口显式注明，不静默跳过
- [ ] NDX 在 2025-04 的最低值已回报，并说明与外部参照值（约 21.6）的差异原因（如有）

---

## 明确不做

- 不改任何阈值、信号规则、开窗判定逻辑（两个需求都是纯导出层）。
- 不采购 Koyfin / 彭博等付费数据源（本需求用系统已有数据即可满足）。
- 不新增下单能力。
- 导出内容中不得出现任何密钥、账户号、token。

## 交付后

请回报导出字段的最终结构（或直接给一份样例 JSON），网站侧会据此对接：
- 需求 1 交付后，网站的价格显示自动从"推算值（带 ~ 与偏差说明）"切换为量化真值；
- 需求 2 交付后，网站会新增估值历史曲线，展示当前 PE 在历史区间中的位置。
