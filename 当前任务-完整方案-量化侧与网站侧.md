# 当前任务（完整方案）：量化侧补数据 + 网站侧补展示

日期：2026-07-22
执行方：**同时具备两个仓库权限的 Codex**
涉及仓库：
- **量化系统**（数据真源）：`~/Projects/futu-assistant`（Python）
- **网站**（只读展示层）：`portfolio-tracker`（TypeScript）

> **本文件自包含，是本轮唯一任务书。** 它取代 `当前任务-网站侧-价格补齐与PE估值.md`、`修复方案-V14.1-*.md`、`修复方案-V15-*.md`、`需求单-量化侧-价格字段与估值序列.md` 四份文件（那些可删）。仓库里其他 `修复方案-V*.md` 是已完成的历史记录，本轮不需要读。
>
> 共 10 个 Phase：**Part A 量化侧（Phase 1–2）→ Part B 网站侧价格（Phase 3–5）→ Part C 网站侧估值（Phase 6–10）**。必须按此顺序：量化侧先产出数据，网站侧才能直接消费真值。

---

## 0. 两个仓库的边界（最重要，先读这一节）

### 0.1 角色不变

即使你现在能同时改两个仓库，**架构角色不变**：

- **量化系统是数据真源**，负责计算、判定、信号；
- **网站是只读展示层**，只消费量化产出的数据，**永远不自造判定结论**。

**禁止**因为"反正两边都能改"就把判定逻辑挪到网站、或让网站直接读量化内部状态。两者仍通过 **CLI 输出 / JSON 文件**这一层契约通信。

### 0.2 量化侧：允许什么、禁止什么

**允许（本轮仅限这些）**：
- ✅ 在现有导出结构中**纯新增**字段（如 `depth_window` 增加价格字段）；
- ✅ **新增**只读导出文件（如 `data/pe_history.json`）；
- ✅ 为新增导出编写测试。

**绝对禁止（违反即事故）**：
- ❌ 修改任何**阈值、信号规则、开窗/关窗判定逻辑、止盈剧本配置**；
- ❌ 修改 `config/rules.yaml`、`config/symbols.yaml` 的既有条目（**新增**字段亦须先说明理由，不确定就不动）；
- ❌ 读取或输出 `config/channels.yaml` 的内容（**密钥文件**：Bark key、IBKR Token、长桥 token）；
- ❌ 写 `store.sqlite`（**单写者原则**，写坏调度器算 P0；只读连接 `mode=ro` 可以）；
- ❌ 修改 `scheduler.py` 的调度行为、修改推送内容格式（本轮不涉及）；
- ❌ 让网站 `import` 量化系统的 engine 代码（架构耦合红线）。

**量化侧每个 Phase 必须证明**：
1. 该仓库自有测试套件**全部通过**（README/CI 里的测试命令，约 212 项）；
2. **判定零变化**：同一输入下开窗/不开窗结论与改动前**完全一致**（用现有测试或对照跑输出证明，把两次输出贴进报告）。

### 0.3 网站侧：只读展示层红线

- 不自造买入/卖出信号、不自造阈值、不自造成本、不自造价格；
- **允许**的文案：「距锚点 +3.7%」「已进入锚点区」「现价 $384.98」；
- **禁止**的文案：「建议买入」「可买」「触发买入」——买入开窗判定永远归量化系统（验收有 grep 断言）；
- 无数据时显示「暂无」，**绝不显示 0 / NaN / Infinity 冒充**。

### 0.4 全程通用规则

1. 每个 Phase：**先写会失败的测试并确认它真的失败（贴失败输出）** → 改实现变绿 → 该仓库测试全绿 → **单独 `git commit`** → **留验证报告** → 才进下一 Phase。
2. 两个仓库的提交**分开**，不要混在一个 commit 里。
3. 不新增付费数据源、不新增运行时依赖。
4. 任何密钥/token 只走环境变量或本机 600 权限文件，**绝不进仓库或提交历史**。
5. 中途不要停下来问方向；唯一允许暂停：部署时需要输入 SSH 密码。
6. 完工必须贴出：**两个仓库的 git log** + **网站三个 hash**（本地 dist / 线上 VPS / 线上 Pages，三者一致才算部署生效）。

---

## 1. 当前状态（已验收，无需返工）

网站侧上一轮（V13 + V14）已完成并部署：248 测试全绿，本地 = VPS = Pages = `index-fMVJS00k.js`。

价格推算部分**实现正确**，已逐项核实：

| 项 | 状态 | 证据 |
|---|---|---|
| 推算公式 | ✅ | `current_pct=-21.8, threshold_pct=-16.24, price=384.98` → 高点 492.30、阈值价 412.35，与手算一致 |
| 正负号稳健 | ✅ | 用 `Math.abs()`，量化给 `-21.8` 或 `21.8` 都正确 |
| 除零保护 | ✅ | `ZERO_EPSILON` 判断，无 NaN |
| 量化真值优先 | ✅ | 已预留读取 `depth.current_price / high_price / threshold_price`，**Part A 交付后自动切换** |
| `~` 与偏差说明 | ✅ | `derived` 时显示 |
| 25 分钟定时刷新 | ✅ | `QUANT_ANALYSIS_REFRESH_MS` + `visibilitychange` |

**所以 Part B 不是返工**，只补一个盲区。

---

# Part A：量化侧新增导出（Phase 1–2）

## Phase 1：`depth_window` 补价格字段

### 问题

网站从快照拿到的 `depth_window` 只有百分比，**没有任何价格字段**：

```
depth_window: { applicable, open, current_pct, threshold_pct, price_session,
                win_rate_60d, n, sample_insufficient, bear_included }
```

于是界面只能显示「当前回撤 21.80% / 阈值 16.24%」，用户不知道**跌到多少钱才算达标**。网站目前用行情代理现价反推，但与量化取价时段不同源，会有偏差且高点会漂移。

### 要求

`depth_window`（以及恐慌位 `panic` 对应结构，如适用）**新增**四个字段，**全部使用与 `current_pct` 完全相同的那一次取价**：

| 字段 | 含义 |
|---|---|
| `current_price` | 计算 `current_pct` 时所用的当前价 |
| `high_price` | 该回撤所基于的 250 日高点价 |
| `threshold_price` | 跌到该价即达到 `threshold_pct` 开窗阈值 |
| `next_level_price` | 下一档位（如 -25%）对应价格；无下一档时 `null` |

补充要求：
- **价格与百分比必须同源同时刻**（同一次取价计算），不得一个实时一个昨收；
- 取不到价时字段为 `null`，**不得用旧价冒充**；
- 保留现有 `price_session`；
- 币种统一 USD。

### 验收

- [ ] `current_pct` 与 `current_price / high_price - 1` 数值自洽（容差 0.01 个百分点）
- [ ] `threshold_price / high_price - 1 == threshold_pct`（同容差）
- [ ] 取价失败时四个字段为 `null`，快照其余部分正常生成
- [ ] 量化仓库自有测试全绿
- [ ] **判定零变化**：同一输入下开窗结论与改动前完全一致（贴两次输出对比）

提交（量化仓库）：`feat: expose depth window prices in analysis snapshot`

## Phase 2：导出历史 PE 序列

### 背景

用户要按 PE 估值判断买入时机，规则是**两套基准**（详见 Part C）：
- **个股**：近 5 年 forward PE 均值；
- **指数杠杆 ETF**：2025 年 4 月关税冲击期的最低估值锚点。

你侧**已经在算** `stock_percentile`、`ndx_percentile`、`soxx_percentile`（网站界面已显示「个股 PE 分位 20.40%」），说明历史 PE 序列已存在，导出即可。

### 要求

新增只读导出 `data/pe_history.json`（与 `data/drawdown_stats.json` 同目录同风格）：

```json
{
  "generated_at": "2026-07-22T12:44:00-04:00",
  "metric": "forward_pe",
  "frequency": "daily",
  "percentile_definition": "当前值在 lookback 期内的百分位，0=历史最低，100=历史最高",
  "lookback_years": 5,
  "symbols": {
    "GOOG": {
      "current": 22.65,
      "percentile": 20.40,
      "series_start": "2021-07-22",
      "series": [
        {"date": "2021-07-22", "value": 26.31},
        {"date": "2021-07-23", "value": 26.12}
      ]
    }
  }
}
```

**硬性要求**：

1. **口径必须显式标注**：`metric` 必须明确是 `forward_pe`（远期，基于分析师预期）还是 `ttm_pe`（过去 12 个月实际）。
   - 用户参照的彭博图用的是 **BEst P/E = 远期 PE**；
   - 网站另一路数据源（Alpha Vantage `ForwardPE`）也是远期口径；
   - **必须在报告中说明你侧现有 `stock_percentile` / `ndx_percentile` / `soxx_percentile` 用的是哪种口径**；
   - 同一文件内**不得混装**两种口径；确需同时提供，用两个 metric 字段或两个文件。

2. **必须覆盖指数标的**（用户持有指数杠杆 ETF，它们自身没有 PE）：

   | 指数 key | 对应 | 用于 |
   |---|---|---|
   | `NDX` | 纳斯达克 100 | TQQQ、QLD、TECL(近似) |
   | `SOX` | 费城半导体 | SOXL |
   | `SPX` | 标普 500 | SPXL、UPRO、SSO |
   | `DJI` | 道琼斯 | UDOW |
   | `FANGPLUS` | NYSE FANG+ | FNGU |

   NDX / SOX **优先**（你侧已在算这两个的 percentile），其余尽力而为，缺的在报告里注明。

3. **必须覆盖 2025 年 4 月**（关税冲击期）：
   - 序列历史范围**必须包含 `2025-04-01 ~ 2025-04-30`**；
   - 该区间有数据缺口须**显式注明**，不要静默跳过；
   - **外部参照**：公开报道称纳指100 forward PE 在 **2025-04-08 跌至约 21.6**（2022 年 11 月以来最低）。若你算出的该期间 NDX 最低值与此差异较大，**在报告中说明原因**（口径差异 / 成分口径 / 数据源不同），**不必强行对齐**，但要让网站侧知道差异来源。

4. **个股序列覆盖近 5 年**（1825 天）；不足的标的注明实际 `series_start`。

5. 文件体积 < 10MB；超出改为按标的分片或降采样并说明。

### 验收

- [ ] 文件可被 `json.loads` 解析，含 `metric`、`frequency`、`percentile_definition`
- [ ] 抽查一个标的：`percentile` 与 `series` 自行计算的分位一致（容差 1 个百分点）
- [ ] 与快照中已有的 `stock_percentile` 口径一致（同标的同时点数值相同）
- [ ] NDX、SOX 序列已导出且覆盖 2025-04 全月
- [ ] 监控池个股序列覆盖近 5 年，不足者注明起始日期
- [ ] **已回报**：NDX 在 2025-04 的最低值，以及与外部参照（约 21.6）的差异原因（如有）
- [ ] 量化仓库自有测试全绿；**判定零变化**（贴对比输出）
- [ ] 未触碰 `config/channels.yaml`；未写 `store.sqlite`

提交（量化仓库）：`feat: export historical pe series for valuation basis`

---

# Part B：网站侧价格补齐（Phase 3–5）

## Phase 3：为量化监控池标的批量拉取报价

### 问题

`ConditionLookup.tsx` 的 `depthQuotePrice(holdings, symbol)` **只遍历 `holdings`（用户持仓）**：

```ts
function depthQuotePrice(holdings: readonly Holding[], rawSymbol: string): number | null {
  for (const holding of holdings) {
    if (!holding.quote) continue;
    // ...只在持仓里匹配
  }
  return null;   // 未持有 → null → source='unavailable' → 价格整块不显示
}
```

于是用户**未持有**但在量化监控池里的标的（**AVGO、MU、AAPL**）拿不到价格 —— **而「深度买入窗口」最有价值的场景，恰恰是发现还没买的标的的买入机会。**

补救路径已实测（用现有网关，零新增成本）：
```
GET http://67.215.255.196:8788/api/quotes?symbols=AVGO,MU,AAPL
→ AVGO $396.81 · MU $959.48 · AAPL $325.89   全部正常
```

> **注意**：Part A Phase 1 交付后，`depth_window` 会带真实价格，此路径退化为**兜底**（量化字段缺失时用）。两者都要保留。

### 要求

1. 新建 `src/monitoredQuotes.ts`（纯逻辑）+ `App.tsx` 调用：
   - 量化快照就绪时，取 `Object.keys(snapshot.symbols)` 作为监控池清单；
   - **排除**已在持仓中且已有 `quote` 的标的、现金等价物（`isCashEquivalent`）、以及**已有量化真实价格的标的**（Part A 交付后无需再查）；
   - 用已有行情代理批量请求 `/api/quotes?symbols=A,B,C`；
   - 网关单次上限 50 个（`.slice(0, 50)`），超出**分批**；
   - 结果存 state：`Map<symbol, price>`。
2. **节流**：每 25 分钟最多一次（与量化快照刷新对齐）；结果 + 时间戳缓存到 localStorage（key `portfolio-tracker:monitored-quotes-v1`），未过期直接复用；行情源未配置 → 跳过，不报错。
3. 请求失败 → 保留上次缓存，UI 照常显示，**不新增红色报错**。

**测试**：60 个标的 → 分 2 批（50+10）；已持有且有 quote 的不进清单；已有量化价格的不进清单；缓存 24 分钟不发请求、26 分钟发请求；失败时返回旧缓存不抛异常。

提交（网站仓库）：`feat: fetch quotes for monitored symbols outside holdings`

## Phase 4：`depthQuotePrice` 支持持仓外报价

1. 改签名 `depthQuotePrice(holdings, monitoredQuotes, rawSymbol)`：
   - **优先级 1**：持仓报价；**优先级 2**：`monitoredQuotes`；都无 → `null`；
2. `ConditionLookup` 增加 prop `monitoredQuotes`，`App.tsx` 传入；
3. **不改** `buildDepthPriceView` 与推算公式（已验证正确）。

**测试**：持有 NVDA → 用持仓报价；未持有 AVGO 但 monitoredQuotes 有 → 算出高点/阈值价；都无 → `unavailable`；**量化真值存在时优先于两者**（`source='quant'`）。

提交（网站仓库）：`feat: use monitored quotes for non-held depth windows`

## Phase 5：说明「反推高点会漂移」

真实的 250 日高点是**固定值**，但用现价反推出的高点**会随现价变化**：

| 时刻 | 现价 | 反推高点 |
|---|---|---|
| 12:44 ET | $384.98 | ~$492.30 |
| 20:00 ET | $396.81 | ~$507.43 |

**修法（纯文案，不改计算）**：`source === 'derived'` 时，在现有偏差说明后追加：

> 高点为反推值，会随现价与量化回撤的更新时差而小幅变动；量化系统提供真实高点后此处将改为固定值。

`source === 'quant'` 时**不显示**该句（Part A 交付后大部分标的走这条）。

**测试**：`derived` 含"会随…变动"；`quant` 不含。

提交（网站仓库）：`docs: explain derived high price drift`

---

# Part C：网站侧 PE 估值双基准（Phase 6–10）

## C0. 用户规则

**两类标的，两套基准**：

| 类别 | 代表标的 | 基准 | 判断方式 |
|---|---|---|---|
| **个股 / 个股杠杆** | GOOG、MSFT、NVDA、TSLL(→TSLA)、MSFU(→MSFT)、NVDL(→NVDA) | **近 5 年 forward PE 均值** | 当前相对均值偏离多少 |
| **指数杠杆 ETF** | TQQQ(→NDX)、SOXL(→SOX)、SPXL(→SPX)、UDOW(→DJI)、FNGU(→FANG+) | **2025 年 4 月关税低点的指数 PE 锚点** | 距锚点还差多少，**接近即提示** |

要点：
- 指数杠杆 ETF **自身没有 PE**，必须映射到底层指数；
- 锚点是「历史极低估值」，用户明确说"接近就可以，百分位不用很高" → 用**相对距离**判断。

## C0.1 数据源

**主来源：Part A Phase 2 导出的 `data/pe_history.json`**（历史序列 + 当前值 + 分位）。

**补充来源：Alpha Vantage OVERVIEW**（免费返回 ForwardPE，本机实测 demo key）：
```
{'Symbol':'IBM', 'TrailingPE':'18.83', 'ForwardPE':'17.42', 'PEGRatio':'2.05'}
```
免费层约 **25 请求/日**。用于量化序列未覆盖的标的、以及交叉校验。

**已调研排除**：Yahoo `quoteSummary` 需 crumb 认证（实测 `Invalid Crumb`）；Koyfin 无公开 API（Enterprise $59/月）；StockAnalysis Pro（$5.27/月）无 API；FMP / EODHD（$19–20/月）为已有数据付费。

**降级路径（必须保留）**：量化序列缺某标的 → 用 Alpha Vantage 当前值 + 手动录入锚点（Phase 9）；两者都无 → 显示「暂无」。

## Phase 6：标的 → 估值基准映射（`src/valuationBasis.ts`）

复用已有 `src/leverageMap.ts`（`underlying` 非 null = 个股型，null = 指数型）：

```ts
export type BasisKind = 'stock_5y_mean' | 'index_anchor';
export interface ValuationBasis {
  kind: BasisKind;
  peSymbol: string;      // 实际查 PE 的代码
  indexKey?: string;     // 指数型才有
  approximate?: boolean; // 近似映射标记
}
export function resolveValuationBasis(symbol: string): ValuationBasis | null
```

规则：
- `leverageMap.underlying` 非 null（TSLL→TSLA、MSFU→MSFT、NVDL→NVDA、AAPU→AAPL、NVDU→NVDA）→ `stock_5y_mean`，`peSymbol` = underlying；
- `underlying` 为 null 的指数型 → `index_anchor`，映射：
  ```
  TQQQ→NDX, QLD→NDX, SPXL→SPX, UPRO→SPX, SSO→SPX,
  UDOW→DJI, SOXL→SOX, TECL→NDX(approximate), TNA→RUT, FNGU→FANGPLUS
  ```
  `approximate: true` 必须在 UI 标注「近似基准」；
- 不在 leverageMap 的普通个股/ETF → `stock_5y_mean`，`peSymbol` 为自身；
- 现金等价物 → `null`。

**测试**：TQQQ→`index_anchor`+NDX；TSLL→`stock_5y_mean`+TSLA；GOOG→`stock_5y_mean`+GOOG；SGOV→null；TECL 标 approximate。

提交（网站仓库）：`feat: map symbols to valuation basis kinds`

## Phase 7：PE 数据接入（量化序列优先 + Alpha Vantage 兜底）

1. 新建 `src/peData.ts`：
   ```ts
   export interface PeSnapshot { symbol: string; forwardPe: number|null; trailingPe: number|null; source: 'quant'|'alphavantage'; fetchedAt: string; }
   export async function fetchQuantPeHistory(url: string): Promise<PeHistoryPayload>   // 读 Part A 的 pe_history.json
   export async function fetchForwardPe(symbol: string, apiKey: string): Promise<PeSnapshot>  // Alpha Vantage 兜底
   ```
2. **量化序列读取**：与现有量化快照读取方式一致（同源网关路径），解析并校验 `metric` 字段；**若 `metric` 是 `ttm_pe`，UI 必须标注为 TTM，不得与 Alpha Vantage 的 forward 值混用**。
3. **Alpha Vantage**：端点 `https://www.alphavantage.co/query?function=OVERVIEW&symbol={S}&apikey={K}`；
   - 返回值是**字符串**，需转数字；`"None"` / `"-"` / 空串 → `null`；
   - 响应含 `Note` 或 `Information` = 免费层额度用尽 → 抛可识别错误，UI 显示「今日免费额度已用完，明日恢复」，**不得静默失败**；
   - Key 来源：复用 `settings.quoteApiKey`（当 `quoteProvider === 'alphavantage'`），否则设置页新增独立 `peApiKey`（password，仅存 localStorage）；
   - **每日缓存**：每标的每天最多 1 次，结果 + 日期存 localStorage（`portfolio-tracker:pe-cache-v1`）。

**测试**：`"17.42"`→17.42；`"None"`→null；限流响应→抛限流错误；同日二次调用走缓存；量化序列存在时不调用 Alpha Vantage。

提交（网站仓库）：`feat: read pe history from quant with alpha vantage fallback`

## Phase 8：基准计算（纯函数 `src/peBasis.ts`）

```ts
export interface PePoint { date: string; value: number }
export interface StockBasisResult { mean5y: number|null; current: number|null; deviationPct: number|null; sampleMonths: number; }
export interface AnchorBasisResult { anchorPe: number|null; anchorDate: string|null; current: number|null; gapPct: number|null; zone: 'at_anchor'|'near_anchor'|'far'|'unknown'; }

export function computeStock5yMean(series: PePoint[], current: number|null): StockBasisResult
export function computeIndexAnchor(series: PePoint[], current: number|null, window: {start:string; end:string}, manualAnchor?: number): AnchorBasisResult
```

**个股 5 年均值**：
- 取最近 5 年（1825 天）的点求算术平均；
- `deviationPct = (current - mean5y) / mean5y * 100`（负数 = 低于均值）；
- 样本不足 24 个月 → 仍计算，`sampleMonths` 如实返回，UI 标注「样本仅 N 个月，参考价值有限」。

**指数锚点**：
- 默认窗口 `2025-04-01 ~ 2025-04-30`（可配置，见 Phase 9）；
- 锚点 = 窗口内序列**最低值** + 对应日期；
- `manualAnchor` 传入时**优先使用**；
- `gapPct = (current - anchorPe) / anchorPe * 100`（正数 = 当前比锚点贵）；
- 分档（阈值可配置）：`≤5` → `at_anchor`；`≤15` → `near_anchor`；否则 `far`；缺数据 → `unknown`；
- **除零保护**：锚点 ≤ 0 或 current ≤ 0 → `unknown`，无 NaN / Infinity。

**测试（数值逐个对上）**：
- 5 年均值 25.0、当前 20.0 → `deviationPct = -20.00`；
- 锚点 21.6、当前 22.5 → `gapPct = 4.17` → `at_anchor`；
- 当前 24.5 → `gapPct = 13.43` → `near_anchor`；当前 30 → `far`；
- `manualAnchor` 优先于序列计算值；
- 空序列 / 锚点为 0 → `unknown` 且无 NaN。

提交（网站仓库）：`feat: compute 5y mean and tariff-anchor valuation basis`

## Phase 9：设置项（锚点窗口 / 手动锚点 / 阈值）

设置页新增「估值基准」分组：
1. **锚点窗口**：两个日期输入，默认 `2025-04-01` / `2025-04-30`，说明：「用于取该区间内指数估值的最低点作为参考锚点（2025 年 4 月关税冲击期间估值处于历史低位）」；
2. **手动锚点**：按指数（NDX / SOX / SPX / DJI / FANGPLUS）各一个可选数字输入，说明：「量化系统序列未覆盖该指数时可手动填入；填了以手动值为准」；
3. **接近阈值**：两个数字，默认 `5` / `15`（%），说明：「距锚点 ≤5% 视为已进入锚点区，≤15% 视为接近」；
4. 持久化到 `AppSettings`，`loadSettings` 兼容缺省。

**测试**：缺省值正确；手动锚点写入后 `computeIndexAnchor` 使用它；阈值改 3/10 后分档随之变化。

提交（网站仓库）：`feat: configurable valuation anchor window and thresholds`

## Phase 10：估值卡片展示

按 `resolveValuationBasis` 类型分别渲染：

**个股型**：
```
GOOG · 远期 PE 22.65
5 年均值 24.80 · 当前低于均值 8.7%
[────●──────]   ← 位置条：均值居中，当前值标点
数据：量化系统（远期 PE）· 序列起始 2021-07-22
```

**指数型**：
```
TQQQ · 基准指数 NDX · 远期 PE 22.4
2025-04 锚点 21.6（2025-04-08）· 距锚点 +3.7%
🟢 已进入锚点区
[▓▓▓▓▓▓▓▓░░]   ← 距离条：锚点为 0 点
数据：量化系统 · 锚点：序列自动计算
```

要求：
- 每个数字**必须标注来源**（量化系统 / Alpha Vantage / 手动录入）**和口径**（远期 PE / TTM PE）；
- **两种口径的数字禁止画在同一条上、禁止相减**；
- `approximate` 显示「近似基准」标记；
- `at_anchor` 绿、`near_anchor` 琥珀、`far` 灰；
- 分位条用纯 CSS/SVG，**不引依赖**；
- **文案红线**：禁止「建议买入」「可买」「触发」等动作词。

**测试**：个股型渲染均值与偏离；指数型渲染锚点、日期、分档色；口径标注存在；grep 断言不含「建议买入」。

提交（网站仓库）：`feat: render dual-basis valuation cards`

---

## 2. 部署与总验收

1. 量化侧：该仓库测试全绿；确认 `positions-status` 与分析快照正常生成
2. 网站侧：`npm run build` && `npm test`
3. `bash deploy/deploy-us-vps.sh`
4. `git push origin main`（网站仓库），等 Pages Actions 完成

**总验收清单**：

**量化侧**
- [ ] Phase 1、2 各自的失败测试输出 + 通过输出，贴进报告
- [ ] 量化仓库自有测试套件全绿（约 212 项）
- [ ] **判定零变化**：同一输入下开窗结论与改动前完全一致（贴两次输出对比）
- [ ] `depth_window` 四个价格字段自洽（见 Phase 1 验收）
- [ ] `pe_history.json` 含 `metric`/`frequency`/`percentile_definition`；NDX、SOX 覆盖 2025-04 全月
- [ ] **已回报** NDX 在 2025-04 的最低值及与外部参照（约 21.6）的差异原因
- [ ] **已回报** 现有 percentile 字段用的是 forward 还是 TTM 口径
- [ ] 未触碰 `config/channels.yaml`；未写 `store.sqlite`；未改任何阈值/信号/判定逻辑

**网站侧**
- [ ] Phase 3–10 各自的失败测试输出 + 通过输出，贴进报告
- [ ] `npm test` 全绿（新增 ≥ 28 用例）；`npm run build` 通过
- [ ] **三个 hash 一致**（本地 dist / 线上 VPS / 线上 Pages，贴出三个）
- [ ] `/api/health` 正常
- [ ] **线上实测**：打开未持仓的监控池标的（AVGO 或 MU），深度买入窗口显示现价/高点/阈值价
- [ ] Part A 交付后，价格显示为 `source='quant'`（**无 `~`、无偏差说明、无漂移说明**）
- [ ] 个股型与指数型估值卡各构造一个用例，数值与来源标注正确
- [ ] 全仓 grep：不含「建议买入」「可买」「触发买入」
- [ ] 无数据处显示「暂无」，无 NaN / Infinity / 0 冒充
- [ ] Alpha Vantage 限流时有明确提示，不静默失败
- [ ] 每个 Phase 独立 commit；两个仓库提交分开；无未提交改动

---

## 3. 明确不做

**量化侧**
- 不改任何阈值、信号规则、开窗/关窗判定、止盈剧本配置
- 不改 `scheduler.py` 调度行为、不改推送格式
- 不触碰 `config/channels.yaml`、不写 `store.sqlite`
- 不新增下单能力

**网站侧**
- 不自造买入信号（买入开窗永远归量化系统）
- 不改 `depthPrice.ts` 推算公式
- 不接入付费数据源（Koyfin / FMP / EODHD / StockAnalysis 已排除）
- 不混用远期 PE 与 TTM PE
- 不硬编码锚点数值（21.6 仅作验收参照）
- 不为监控池标的高频轮询（严格 25 分钟节流）
- 不重构组件结构、不新增 Tab

**两侧共同**
- 不让网站 `import` 量化 engine 代码（架构耦合红线）
- 不把判定逻辑挪到网站
- 不把密钥写进任何仓库或提交历史

---

## 4. 后续（本轮不做）

若用户希望「接近锚点」成为**正式买入推送**（Bark 通知），须在**量化侧**作为新信号实现（含阈值配置、影子期、回测验证），**网站侧不得自造**。届时另行提需求。
