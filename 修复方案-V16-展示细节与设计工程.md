# 修复方案 V16 · 展示细节错误、口径自洽与设计工程

> 审查日期：2026-07-30
> 审查对象：`http://67.215.255.196:8788/`（VPS 部署）
> 审查方式：真实线上站 DOM/网络实测 + 本地源码对照
> 设计标准：`.agents/skills/apple-design/SKILL.md`、`.agents/skills/review-animations/STANDARDS.md`（emilkowalski/skills，已在 `skills-lock.json` 锁定）
> 执行者：Codex

---

## 0. 前置校验（已完成，Codex 无需重做但需知情）

| 项 | 值 | 结论 |
|---|---|---|
| 线上 JS hash | `assets/index-D3H2-eDV.js` | 与本地 `dist/assets/index-D3H2-eDV.js` **一致** |
| 线上 CSS hash | `assets/index-DE93B0RA.css` | 与本地 `dist/` 一致 |
| git | `main` @ `8d65f07`，working tree clean | 本文件所有 `file:line` 引用即线上代码 |
| 浏览器 console | 无任何 log / error | 所有问题都是"静默错误"，不会自己冒出来 |

**铁律（沿用 V13/V15，不得违反）**

1. 网站只读展示。禁止自造卖出阈值、成本、剧本、买入结论。
2. 量化系统是唯一裁决方。本文件里凡标注 **【量化侧】** 的，网站不得自行"修好文案"，只能记入 §5 需求单。
3. 估值页禁止出现"建议买入/可买/触发"等动作词。

**执行纪律（用户 CLAUDE.md 要求）**

- 每个 P0/P1 项 **先写失败测试**（vitest），跑一次看到红，再写最小实现变绿。
- 最小 diff。每一行改动都要能对应到本文件的某一条。不做顺手清理。
- 完工验收：`npm test` 全绿 + `npm run build` + 本地 hash 与双端线上 hash 三者一致（沿用 V11/V12/V14 验收法）。

---

## 1. P0 · 功能性 Bug（可复现，先修这些）

### P0-1 资产占比饼图：窗口变窄后**永久空白**，并造成 732px 横向溢出

**位置** `src/components/AllocationChart.tsx:62-63`

**复现步骤（已实测）**

1. 在 ≥640px 宽的窗口打开 `总览`
2. 把窗口拖窄到 <520px（等价于手机横屏→竖屏旋转）
3. `资产占比` 卡片变成一个 288px 高的空白洞
4. 再把窗口拖回 1280px —— **仍然是空白**，只有刷新页面才恢复

**实测证据**

```
// 拖窄后
{"innerWidth":400,"hostW":342,"hasLegend":false,"hasDetailList":false}
// 容器 innerHTML 为空
{"cls":"min-h-72 w-full min-w-0 sm:h-96","h":288,"w":342,"childCount":0,"html":""}
// 拖回 1280 后仍然空
{"step":"back to 1280 (no reload)","hostW":1078,"hostH":384,"childCount":0}
```

中间态还会撑爆页面宽度（recharts 把 legend 按 14 项单行自然宽度写成内联 `width:1030px`，svg 被顶到 1078px）：

```
{"innerWidth":375,"docW":1107}            // 横向溢出 732px
{"cls":"recharts-legend-wrapper","w":1030,"style":"...width: 1030px..."}
```

**根因** 组件用 `useRef` + `ResizeObserver` 手动量尺寸，再用 `{size.width > 1 && size.height > 1 && (...)}` 当渲染闸门。一旦某次回调量到 0（宽度断点切换时 `sm:h-96` 失效 + 宽度骤变，同一帧内 legend 卸载 / 明细表挂载又改变容器高度，形成观察循环），`size` 被钉死在 `Math.max(1, 0) = 1`，闸门永久为假。recharts 自带 `ResponsiveContainer` 就是为解决这件事存在的。

**修法**

- 删掉 `containerRef` / `size` / `ResizeObserver` 整套手动测量，改用 `<ResponsiveContainer width="100%" height={...}>` 包 `<PieChart>`。
- `isNarrow` 不要再从 JS 测量推导，改用 `window.matchMedia('(max-width: 519px)')` + `useSyncExternalStore`，或直接用 CSS（宽屏渲染 `<Legend>`、窄屏渲染明细表，两者都挂上、用 Tailwind `hidden`/`sm:block` 切换，交由 CSS 决定）。CSS 方案更稳，且天然没有"测量失败"这个状态。
- 无论选哪种，`<Legend>` 必须加 `wrapperStyle={{ maxWidth: '100%' }}`，把 recharts 的内联 `width` 钳住，防止再次撑爆。

**验收**

- 新增测试：在 jsdom 里以窄宽度渲染 → 断言渲染出明细列表；以宽宽度渲染 → 断言渲染出 legend；**两种宽度都断言容器非空**。
- 手测：1440 → 375 → 1440 拖动，饼图任何时刻都不空白；`document.documentElement.scrollWidth === window.innerWidth` 恒成立。

---

### P0-2 「最终裁决」条 782px / 手机 1424px，**挂在全部 6 个标签页上方**

**位置** `src/App.tsx:562-566`（`DecisionStatusBar` 在 6 个 `tab === '...'` 判断之外）

**实测证据**

```
1280px 视口：statusBarH = 782      （比 800px 视口还高）
 375px 视口：statusBarH = 1424     （约 1.75 个手机屏）
点「持仓」后：tabContentTop = 970  → 「截图导入持仓」在 1280×800 屏上就在首屏之外
```

这正是用户截图里的现象：`持仓` 高亮着，但整屏都是 28 行买入裁决，真正的持仓内容被推到 y≈890 以下。手机上要划过约两屏才看得到持仓表。

**为什么是 bug 而不是设计** 28 条 `不买` 是"买入"域的信息。在 `设置`、`计算器`、`估值`、`持仓` 四个页面它与当前任务完全无关，却占满首屏。apple-design §16「简洁不是极简」＋「Wayfinding：每屏都要回答我在哪」都被违反：任何 tab 打开都长一个样。

**修法**

1. `DecisionStatusBar` 只在 `tab === 'dashboard'` 渲染（移进 dashboard 那个 `<section>`）。
2. 默认折叠成一行摘要（见 P0-4 的新 headline），28 条明细收进 `<details>`；`条件查询` 页已有完整的逐标的视图，这里不需要第二份全量清单。
3. 其它 5 个 tab 若需要"今天不放行"的存在感，只保留 headline 一行（≤1 行高），不带明细。

**验收**

- 测试：渲染 `tab='settings'` → 断言 `queryByLabelText('量化系统最终裁决')` 为 null（或高度断言 ≤1 行）。
- 手测 375px：点「持仓」后，`截图导入持仓` 标题在首屏可见（`getBoundingClientRect().top < 812`）。

---

### P0-3 「数据停留在 2026-07-28（**4 天前**）」—— 算错，实际 2 天

**位置** `src/components/ValuationCharts.tsx:97`、`src/components/ValuationPanel.tsx:106`

**实测证据（同一份 payload，三个不同的陈旧天数）**

```json
{
  "valuation_tab.ndx.stale_days": 4,     // ← 页面显示的就是这个
  "freshness.stale_days": 2,
  "freshness.max_stale_days": 2,
  "final_verdict.stale_days": 2,
  "as_of": "2026-07-28"                   // 今天 2026-07-30 → 2 天
}
```

后端自己给了两个互相矛盾的值，网站盲传了错的那个，而且这句话在同一页出现 **两次**（估值卡 + PE 走势图各一次）。

**修法**

- 不再直接印 `stale_days`。写一个 `src/staleDays.ts`：`staleDaysFrom(asOf, now)` 按日历日差算，纯函数好测。
- 两处都改成用它算；后端 `stale_days` 只在 `as_of` 缺失时兜底。
- 同一页两次重复：只在页面顶部的估值卡保留一次，PE 走势图那处改为无文案的 `⚠` 角标（或直接去掉，因为紧邻同一警告）。
- 同时记 §5【量化侧】：修 `valuation_tab.ndx.stale_days`。

**验收** 测试：`staleDaysFrom('2026-07-28', new Date('2026-07-30T08:49-04:00'))` === 2；组件断言渲染「（2 天前）」且全页只出现一次。

---

### P0-4 「今日结论」被单个 `UNDECIDABLE` 污染

**位置** `src/components/DecisionStatusBar.tsx:62-66`

**现象** 页面顶部大字写「今日结论：数据不足，无法判定」，可下面 28 行里有 **24 行都是明确的「不买」**，只有 FAS/SPXL/UDOW/UPRO 4 个因恐慌窗口缺数据无法判定。判断顺序把 `some(UNDECIDABLE)` 放在 `every(NO_BUY)` 之前，一个缺数据就把整天的结论抹成"没结论"。

用户看到的是"系统坏了"，实际是"24 个明确不买 + 4 个缺数据"。这直接损害信任，和 V10「压成红点『可卖』」是同一类错误。

**修法** 改成计数式 headline，不要单一标签：

```
今日结论：不买 24 · 无法判定 4        // 有 UNDECIDABLE 且其余全 NO_BUY
今日结论：条件完整 1 · 不买 27        // 有 BUY 时把 BUY 放最前
今日结论：数据不足，无法判定           // 仅当 verdicts 为空或全部 UNDECIDABLE
```

**验收** 测试覆盖四种组合：全 NO_BUY / NO_BUY+UNDECIDABLE 混合 / 含 BUY / 空数组。混合场景断言 headline 里同时出现「不买 24」和「无法判定 4」。

---

### P0-5 数据陈旧提示不显示（`data_stale=false` 但 `stale_days=2`）

**位置** `src/components/DecisionStatusBar.tsx:14`

```ts
const isStale = Boolean(staleVerdict || verdictFreshness?.data_stale || freshness?.data_stale);
```

只看布尔量。后端这份 payload 里 `data_stale: false` 而 `stale_days: 2` / `max_stale_days: 2`，于是**裁决数据落后 2 天，页面一个字都不提**，而 `买入判定 2026-07-30 08:33` 的时间戳还在暗示"这是今天的判断"。

**修法** `isStale = data_stale === true || (stale_days ?? 0) > 0 || (max_stale_days ?? 0) > 0`。显示文案沿用现有 `数据 {asOf}（落后 N 天）`。记 §5【量化侧】修布尔量。

**验收** 测试：`{data_stale:false, stale_days:2}` → 断言渲染「落后 2 天」。

---

### P0-6 金额缺千分位：同一屏 `$3281.60` 与 `$3,281.60` 并存

**位置** `src/components/ConditionLookup.tsx:451`（`` `$${value.toFixed(2)}` ``）、`src/components/AlertRulesPanel.tsx:213`（同款）

**证据（`条件查询` 同一屏）**

```
⚪ TQQQ · 市值 $20412.63 · FUTU / IBKR · 无      ← 选择器里，无千分位
⚪ NVDA · 市值 $20172.03 · ...
本族当前盈亏  市值 $3,281.60                     ← 下方卡片，有千分位
```

`$20412.63` 这种五位数不加分隔符极易误读一个数量级 —— 和 V10「`$332.00` 被读成股价」是同一类事故。

**修法** 这两处改用已有的 `formatDisplayMoney` / `format.ts` 里的金额格式化函数，不要再手写 `toFixed`。全仓再 grep 一遍 `` `$${ `` 确认没有第三处。

**验收** 测试断言 `$20,412.63`；grep 断言全仓无手写 `` `$${...toFixed `` 金额拼接。

---

### P0-7 `ⓘ` 提示在手机上**完全打不开**

**位置** `src/components/ConditionLookup.tsx:623`、`:647`

```tsx
<span className="ml-1 text-xs text-ink-muted" title="只在浮盈达到最低档后适用">ⓘ</span>
```

`title` 属性只在鼠标 hover 时出现。用户是**手机为主**的使用者（README/交接文档里明确），触屏永远触发不了 `title`，这两条关键限定条件（"修复期是市场维度，与你的成本无关"、"止盈阶梯只在浮盈达最低档后适用"）在手机上等于不存在。而这正是 V10/V12 反复强调不能丢的口径说明。

同时它不是 `<button>`：无法聚焦、无 44px 命中区、屏幕阅读器读作一个孤立字符。

**修法** 换成可点开的 `<button type="button">` + 受控 popover（`<details>` 或已有的展开模式），命中区 ≥44×44，`aria-label` 写明用途。图标用行内 SVG 或统一的信息图标组件，别用 `ⓘ` 字形（各平台字形宽度不一，且紧贴中文无空隙）。

**验收** 测试：点击该按钮后断言说明文本出现在 DOM 中（而非只存在于 `title`）。

---

### P0-8 「期权成本无量化来源」清单里混着正股，且 MSFU 重复出现

**位置** `src/components/SettingsPanel.tsx:17`、`:23`、`:55`

**证据（`设置` → 成本数据覆盖）**

```
期权成本无量化来源
  MSFT · IBKR      ← 正股
  MSFU · IBKR      ← 杠杆 ETF
  NKE  · IBKR      ← 正股
  NVDA · IBKR      ← 正股
  PDD  · IBKR      ← 正股
  MSFU · FUTU
  IGV  · FUTU      ← ETF
提示：期权成本量化系统未提供，请在「持仓 → 补充期权详情」上传期权详情页截图

量化成本覆盖不完整
  MSFU · IBKR · coverage=unavailable    ← MSFU 在上面已经列过一次
  MSFU · FUTU · coverage=unavailable
```

两个问题：

1. **分类错**：`option_no_source` 这一桶装进了 MSFT/NKE/NVDA/PDD/IGV 这些非期权持仓，然后给用户一条"去上传期权详情页截图"的指引 —— 按这个指引操作解决不了正股缺成本。这是 V12 遗留第 ③ 项「未知成本清单重复且文案对非期权持仓不准」，**至今未修**。
2. **重复**：MSFU·IBKR / MSFU·FUTU 同时出现在两个清单里，用户会以为有两个不同问题。

**修法**

- 分类逻辑加 `isOption(holding)` 判定（`assetClass.ts` 已有资产类别能力，直接复用，别新写）。非期权且缺成本 → 独立桶 `equity_no_source`，文案改为"请在持仓表手动补填买入价"。
- 两个清单去重：同一 `symbol+broker` 只出现在优先级最高的那一桶（建议 `option_no_source` > `coverage_partial`）。

**验收** 测试：喂入 `MSFT(正股,无成本)` + `MSFU(期权,无成本,coverage=unavailable)` → 断言 MSFT 出现在正股桶且文案不含「期权详情」；断言 MSFU 全页只出现一次。

---

### P0-9 机器码 `coverage=unavailable` 直接印给用户

**位置** `src/components/SettingsPanel.tsx:55`

```ts
? ` · coverage=${quantCost?.coverage ?? 'missing'}`
```

**修法** 建一张中文映射表：`unavailable → 券商未提供成本`、`partial → 成本不完整`、`missing → 无成本记录`。落库值本身不动。

**验收** 测试断言页面不含字符串 `coverage=`。

---

### P0-10 原始 ISO 时间戳裸露（4 处）

**位置**

| 文件行 | 页面显示 |
|---|---|
| `src/components/ValuationPanel.tsx:162` | `OpenD 最新价 · 2026-07-30T08:49:24.092951-04:00` |
| `src/components/ValuationPanel.tsx:95` | `丹居 · <as_of ?? generated_at>` ← 兜底会印 ISO |
| `src/components/ValuationPanel.tsx:117` | `CNN · <as_of ?? generated_at>` ← 同上 |
| `src/components/BuyPlanSection.tsx:155` | `量化快照 2026-07-30T08:49:24.092951-04:00` |
| `RegimeSection` 当前状态卡 | `2026-07-30T08:49:24.092951-04:00` |

带微秒和时区偏移的 ISO 串在同一个页面里和格式化好的 `2026-07-30 08:33`（`DecisionStatusBar.dateText()`）并存。

**修法** 把 `DecisionStatusBar.tsx:91` 的 `dateText()` 提到 `src/format.ts` 导出，全部 5 处统一调用。带 ET 时段的场合沿用 `条件查询` 已有的 `快照 2026-07-30 08:49 ET，8 分钟前` 格式（那处是对的，直接复用）。

**验收** 测试：grep 断言渲染结果不含 `T` + 微秒模式 `/\d{2}:\d{2}:\d{2}\.\d{6}/`。

---

### P0-11 原始浮点 `34.4857142857143` 裸露

**位置** `src/components/RegimeSection.tsx:23`

**页面原文**

```
你的计划：0/3 条满足 · 最近「第一枪」还差 CNN < 25（当前 34.4857142857143）、回撤 < -45%（当前 -29.00871742416146）
```

同一页上方刚刚才写着 `CNN 34.49`、`回撤 -11.22%`。

**根因** `verdict.plan_link.nearest.missing` 是后端已经拼好的**字符串数组**，未格式化：

```json
"missing": ["CNN < 25（当前 34.4857142857143）", "回撤 < -45%（当前 -29.00871742416146）"]
```

**修法（两段并行）**

- 网站侧兜底：`missing.join('、')` 之前跑一遍 `str.replace(/-?\d+\.\d{3,}/g, m => Number(m).toFixed(2))`，把长小数就地压到 2 位。这是纯展示层格式化，不改数值语义、不违反只读铁律。
- 记 §5【量化侧】：`missing` 应改为结构化字段（`{ metric, op, threshold, current }`），由网站负责格式化，不要下发拼好的句子。

**验收** 测试：喂入上面那个 `missing` 数组 → 断言输出含「当前 34.49」且不含 `34.4857`。

---

### P0-12 标题层级跳级 h1 → h3

**证据** `设置` 页 DOM：`H1:我的投资组合` → `H3:设置` → `H3:数据导入 / 导出`，全站无 `h2`。

**修法** 各 tab 的板块标题降为 `h2`，卡片内小标题用 `h3`。纯语义改动，视觉靠 Tailwind 字号维持不变。

**验收** 测试断言每个 tab 的 heading 序列无跳级。

---

## 2. P1 · 数据口径自洽（用户"心算对不上"就会再次不信任系统 —— V12 教训）

### P1-1 三张卡百分比加起来 124.75%，无任何说明

`src/components/Summary.tsx:143` `:147` `:150`

```
持仓市值      $127,978.79   92.81%          ← 1 - cashWeight（含现金类 ETF）
现金及等价物  $44,049.76    31.94%          ← 含现金类 ETF $34,130.52
期权权利金    $13,097.87    9.50% 的总资产
                            ─────
                            124.75%
```

现金类 ETF（SGOV 等 $34,130.52）**同时**被计入"持仓市值"和"现金及等价物"。数学上没错（92.81% = 1 − 纯现金占比），但三张卡并排放着就是让用户去加。

**修法** 不改数值，只补口径。`持仓市值` 的 `sub` 改为 `92.81%（含现金类 ETF $34,130.52）`，或在三卡下方加一行 `现金类 ETF 同时计入持仓市值与现金及等价物，两者不可相加`。选前者，字更少。

### P1-2 期权 Delta 两个数：`$0.00` vs `≈$154,804.50`

- `Summary.tsx:159`：`等效正股暴露` 里 `期权Δ $0.00`
- 同一页 `期权风险专区 · Delta 敞口 ≈$154,804.50`，附注 `Delta 为统一假设 0.5，非真实 Delta`

同一屏、同一个概念、差 15 万美元。原因是两处用了不同的 Delta 口径（网站等效暴露只算有真实 Delta 的，7 个缺 Delta 的不计入 → 0；风险专区用统一 0.5 假设 → 15 万）。`Summary.tsx:164` 的 `⚠ 7 个期权缺 Delta/标的价未计入` 解释了前者，但没解释后者为什么有数。

**修法** 两处都加口径后缀，并互相点名：`期权Δ $0.00（仅真实 Delta；7 个缺 Delta 未计入，见下方期权风险专区的 0.5 假设口径）`。数值一个都不动。

### P1-3 同一个数两种精度

`可用资金 $28,717`（`DecisionStatusBar.tsx:58`，`maximumFractionDigits: 0`）vs `可动用 $28,717.47`（`Summary.tsx:259`）。统一到 `formatDisplayMoney` 的两位小数。

### P1-4 `现金分母：已投现金合计 $93,827.72 · 100.00%`

`Summary.tsx:259`。分母除以自己恒等 100%，这一项是纯噪音。删掉 `invested_pct` 的显示，或改为对总资产的占比（68.04%）—— 建议直接删，信息量为零。

### P1-5 全站**五种**刷新周期，用户无法建立心智模型

| 文案 | 位置 |
|---|---|
| `盘中每 5 分钟、其他时段每 25 分钟自动更新` | `ConditionLookup.tsx:749` |
| `美股盘中每 35 分钟自动刷新` | `Summary.tsx:192` |
| `盘中每 35 分钟检查一次` | `AlertRulesPanel.tsx:160` |
| `Mac 量化系统每 45 分钟汇总…推送到 VPS` | `SettingsPanel.tsx:263` |
| `北京时间每天 7 点后自动刷新一次` | `SettingsPanel.tsx:396`（另 `:370` 重复一次） |

这五个数字各自都对（分别是量化快照 / 行情 / 提醒检查 / 券商汇总 / 日线兜底），但散在四个组件里，读者只会觉得系统在自相矛盾。

**修法** 建 `src/refreshCadence.ts` 单一常量表（每条带 `label` + `interval` + `scope`），五处文案全部由它生成。再在 `设置` 加一个只读的"刷新节奏"小表把五条一次讲清。改动只是把散落的字面量收敛到一处，不改任何实际定时器。

### P1-6 FNGU 现价两个值（相邻 tab）

```
计算器：  FNGU · 现价 23.44
计算器：  FNGU · 目标价 $33.00 / 上一交易日收盘 $24.16 @ 15:28 ET · 还需上涨 26.79%
```

同一个 tab 内 23.44 与 24.16 并存（NVDA 同样：193.82 vs 192.93）。情景计算器与提醒卡用了不同价格源。

**修法** 统一到同一份报价快照（`monitoredQuotes` / `depthQuotePrice` 已有能力）。若确实必须用两个源，则两处都标注来源与时间戳，让差异可解释。

### P1-7 「上一交易日收盘 @ 15:28 ET」—— 既不是上一交易日，也不是收盘

后端 payload 明确：

```json
{"price_session": "pre", "prices_at": "2026-07-30T08:49:24-04:00"}
```

`price_session = pre`（盘前），取价时刻 08:49 ET。而提醒卡写「上一交易日收盘 $24.16 @ **15:28 ET**」—— 15:28 ET 是常规时段内的时间点，不是收盘（16:00）。总览同时写着「上一交易日（未开盘）」，与后端的 `pre` 冲突。

这是 memory 里 `当前任务-盘前盘后夜盘取价.md` 的老问题在展示层的残留。

**修法** 时段标签一律由 `price_session` 驱动（`pre` → `盘前`、`regular` → `盘中`、`post` → `盘后`、无 → `休市`），时间戳直接印 `prices_at`。禁止把非 16:00 的时间点称作"收盘"。

### P1-8 「今日涨跌 $0.00 **+0.00%**」

`Summary.tsx:105`，`formatSignedPct` 对 0 也加正号。零变化不该有方向符号。`format.ts` 里 `formatSignedPct` 加一条：`value === 0 → '0.00%'`（同时检查 `合计情景盈亏 占总资产 +0.00%` 那处一并生效）。

### P1-9 同一数量在两个 tab 不同值：`24,875` vs `24,906`

- 总览：`仍超硬顶 24,875，不建议新增科技`
- 估值：`仍超硬顶 24,906，不建议新增科技`
- 同时 `科技：等效 93.04%/93.06%`、`现金 79.90%/79.91%` 也各差一点

原因是两个 tab 各自持有一份不同时刻拉取的快照。

**修法** 全站共用一份 `quantAnalysis` 快照（App 已经在 `App.tsx` 顶层持有），把还在各自 fetch 的地方改成读同一份 state；页面顶部标一次快照时间。**顺带**：这两句里的 `24,906` 缺 `$`，属【量化侧】文案（见 §5）。

### P1-10 PE 两个数混用：29.40 / 29.90

估值页同时出现 `PE 29.40`（5 年分位基准）、`实时估算 29.90（QQQ 盘前价）`，而顶部结论卡写 `估值已到位（PE 29.90）`，开枪计划表的条件判定写 `✓ NDX PE < 30 当前 29.40 · 已满足`。阈值是 30，两个值一个 29.40 一个 29.90 —— 这次没翻转结论，下次就会。

**修法** 明确一条：**闸门判定只用后端 `pe`（29.40），结论卡与提示文案也必须用同一个值**；`实时估算 29.90` 只作为参考行展示，且必须紧跟"估算值，不参与判定"标注（现在这行标注在，但结论卡引用了估算值）。

### P1-11 「5年分位 20.16% · 数据缺失」同一行自相矛盾

给了具体分位又说数据缺失。二者取一：有值就不说缺失，缺失就不给值（显示 `—`）。

### P1-12 「还差多少」里出现向上的目标且标「已到达」

```
距 PE 30分位线   已到达
PE 29.40 → 32.47（已到达）
QQQ $672.79 → 约 $742.90（已到达）
```

板块标题是"还差多少"（等待下跌），这一行的目标价却比现价**高** 10%。当前 PE 低于 30 分位线，所以"已到达"在数值上成立，但和"还差多少 / 等回调"的语境正好反向，读者会以为要涨到 742 才算到位。

**修法** 目标高于现价时不要复用"还差/已到达"这套下跌语义，改为 `当前已低于 30 分位线（分位线对应 PE 32.47）`，并把这一行从"还差多少"里移出，或加"已在区间内"分组。

### P1-13 「一键刷新全部」标题与按钮同名

`Summary.tsx:120`（卡片标题）与 `:136`（按钮文字）完全一样，四个字连着出现两次。标题改成说明性的（如 `刷新`）或直接去掉标题只留按钮 + 副说明。

### P1-14 「已启用 美国 VPS 中转：…（美国 VPS 中转），手机不再…」

`SettingsPanel.tsx` 顶部提示里"美国 VPS 中转"在一句话内出现两次（一次做主语，一次做括注）。删括注。

### P1-15 事故排查叙事被当成永久产品文案

```
当前默认切到智谱。你手机上的 Kimi 已经表现为 90 秒超时，说明不是图片压缩问题，优先用智谱更稳。
```

这是 V2 那次排查过程的口述记录，被固化进了 `设置` 页。它把一次性的历史事件写成了永久说明，且用第二人称诊断用户的设备。

**修法** 改为一句中性说明：`默认使用智谱（更稳定）。Kimi 作为备用，网络不佳时可能超时。`

### P1-16 等效仓位公式缺括号，读出来是错的

```
等效仓位 = 正股 + 杠杆 ETF×倍数 + 期权 Delta 折算后的总敞口 ÷ 总资产
```

按运算优先级读是 `正股 + ETF×倍数 + (Delta折算 ÷ 总资产)`。

**修法** `等效仓位 =（正股 + 杠杆 ETF × 倍数 + 期权 Delta 折算）÷ 总资产`。

### P1-17 锚点说明里多余空格

```
…当前不使用更深的历史/熊市极值作为基准。 手动锚点仅在对应指数填写后覆盖自动值。
```

句号后有一个半角空格。中文排版不用空格分句，改为换行或直接相接（建议拆成两个 `<p>`，因为是两件事）。

### P1-18 单位混用：`%` / `点` / `pp` 无区分

同一张开枪计划卡里：

```
○ NDX PE < 28    当前 29.40 · 还差 5.0%     ← 相对百分比
○ CNN < 20       当前 34.49 · 还差 14.5 点   ← 绝对点数
○ 回撤 < -35%    当前 -11.22 · 还差 23.8%    ← 百分点(pp)
```

三个"还差"三种单位，其中两个都写成 `%` 但含义不同（相对变化 vs 百分点）。百分点场景统一用 `pp`（全站已有 `pp` 用法，如 `已用满 10.0pp 借额`），相对变化保留 `%`。

### P1-19 开枪计划表里同一段上下文重复 3 次

三张计划卡（SOXL 第一枪 / TQQQ 第一枪 / TQQQ 第二枪）各自带着完全相同的三行：

```
当前仓位门额度：$0
等效敞口 209.94% · 实付现金 100.00%
科技：等效 93.06% · 现金 79.91%
本轮不建议新增：已用满 10.0pp 借额（上限 75%），仍超硬顶 24,906，不建议新增科技（科技超硬顶 75.00%）
```

这是账户级信息，不是计划级信息。**修法**：提到计划表标题下方渲染一次。（apple-design §16 简洁：每个元素都要挣得自己的位置。）另外 `ⓘ 按当前盈利基数估算` 在"还差多少"里也重复 3 次，同样上提。

### P1-20 视觉模型选项标注不齐

```
glm-4.6v-flash（推荐：免费/较快）
glm-4v-flash（轻量免费）
glm-5v-turbo（更强，可能更贵）
glm-4.6v（更强）                  ← 与上一条同为"更强"，无法区分
glm-4.1v-thinking-flash           ← 唯一没有括注的
```

**修法** 五条都给一句同结构的括注（`免费/快` `免费/轻量` `更强/更贵` `更强/免费额度低` `思维链/最慢`），或全部去掉括注只留模型名 + 一行统一说明。

### P1-21 三处裸露内网 URL

`http://67.215.255.196:8788/api/zhipu/chat/completions`（出现两次）、`http://67.215.255.196:8788/api/quotes`。既是 HTTP 明文，也是把基础设施细节写进消费级界面。

**修法** 正常态只说"已自动使用服务器转发"，完整 URL 收进 `<details>` 折叠（排障时才需要）。

### P1-22 空态写成「· 无」

`ConditionLookup.tsx:728`：`` `⚪ ${fallbackLabel} · 无` ``。17 个下拉项全部尾随「· 无」。**修法** 无内容就不拼这一段，别用「无」当占位。

### P1-23 「保存设置」在超长页面最底部，且无脏态提示

`设置` 页很长（AI 服务 + 模型 + 代理 + 等效仓位 + 量化同步 + 成本覆盖 + 估值 + 锚点 + 行情源 + 代理 URL），保存按钮只在最末尾，改了顶部一个字段要一路滚到底。`估值` tab 已经实现了 `valuationEditorDirty` 脏态 + 离开确认，`设置` 完全没有。

**修法** 保存条改为 `sticky bottom-0` 的半透明工具条（配合 §3 的材质规范），有未保存改动时才出现，显示"N 项未保存"。这一条同时补上了 apple-design §16「Grouping & mapping：控件要靠近它影响的东西」。

---

## 3. P2 · 设计工程（按 apple-design + review-animations 标准）

> 这些不是"审美建议"，每条都能对上 skill 里的具体条款。用户明确要求"按苹果设计的美感"来改。

### P2-1 ❗中文正文套 `font-mono`，字体族在行内断裂

**最影响观感的一条。** `tailwind.config.js:27` 的 mono 栈是 `"SF Mono", ui-monospace, Menlo, monospace` —— **全部不含 CJK 字形**。于是每一行"英文代码 mono + 中文回退到别的字族"，同一行里两种字重、两条基线、两种字宽。

用量统计（`grep -c font-mono`）：

```
ConditionLookup.tsx  38    Summary.tsx  24    HoldingsTable.tsx  21
OpportunityOverview.tsx 12  ValuationCard.tsx 10  RegimeSection.tsx 9 ...
```

用户截图里那 28 行裁决文字就是典型：中文正文被整段丢进 mono，读起来像终端输出，不像产品。

**违反** apple-design §15「默认用平台系统字体，它自带光学尺寸和字距表；没有理由不要覆盖」＋ §16-7「Craft：漂亮的排版建立信任」。

**修法** 一条规则贯彻到底：

- `font-mono` + `tabular-nums` **只包数字和代码**（`<span className="font-mono tabular-nums">$20,412.63</span>`）。
- 中文散文一律 `font-sans`。
- 需要数字对齐的表格列，在 `<td>`/列容器上加 `tabular-nums`，**不要**加 `font-mono`（`font-sans` 栈里的 `-apple-system` / `PingFang SC` 本身支持 `tabular-nums`）。

工作量集中在把 `className="font-mono ... text-xs"` 从段落容器下移到数字 span。建议按文件逐个改，每个文件一个 commit。

### P2-2 零阴影、零 backdrop-blur —— 四十张卡片完全同一层级

`grep -o "shadow-[a-z0-9/-]*"` 全仓 **0 命中**。`backdrop-blur` 唯一出现处是 `index.css:124` 的 `prefers-reduced-transparency` 保护规则 —— **保护的对象不存在，这条 CSS 是死代码**。

结果：`rounded-2xl border border-neutral/40 bg-surface-raised` 这一套被复制到几乎每一张卡上，总资产卡和一条脚注提示在视觉上一样重。

**违反** apple-design §12「材质厚度编码层级 / 大表面要读起来更厚：更强模糊 + 更深阴影」；也正中用户全局规则里 design-quality 的禁用项「uniform radius, spacing, and shadows across every component」。

**修法（最小可见改动，别做全站重设计）**

1. 定义三级表面 token，写进 `index.css`：
   - `--elev-1`：脚注/说明块 —— 只有 border，无阴影（现状）
   - `--elev-2`：常规卡 —— `box-shadow: 0 1px 2px rgb(0 0 0 / .04), 0 1px 1px rgb(0 0 0 / .02)`
   - `--elev-3`：英雄卡（总资产、今日涨跌、今日结论）—— `0 4px 16px rgb(0 0 0 / .08)` + 稍大 radius
   暗色主题下阴影换成更低不透明度 + 顶部 1px 亮边（`border-top: 1px solid rgb(255 255 255 / .06)`），符合 §12「亮边 = 材质接到光」。
2. `Summary.tsx` 的 `variant="hero"` 已经存在，把它接到 `--elev-3`；其余卡接 `--elev-2`；脚注类保持 `--elev-1`。
3. 顶部 header + nav 改成 `sticky top-0` + `backdrop-blur(20px)` + 半透明背景（§12「导航做成半透明层，内容在下面滚过」）。这同时解决了 6000px 长页面滚下去之后**看不到自己在哪个 tab**（§16 Wayfinding）。做完这条，`index.css:123-127` 那条死 CSS 才真正生效。

### P2-3 `prefers-reduced-motion` 把所有 transition 杀成 0.01ms

`src/index.css:108-121`

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;   /* ← 连颜色/不透明度都杀 */
  }
}
```

**违反** apple-design §14 明文：「减少动效不等于没有反馈 —— 它意味着更温和的、非前庭刺激的等价物。保留有助理解的不透明度/颜色变化」，且「主题明暗切换要缓动，避免亮度突跳」。现在开了减少动效后，深浅主题切换变成硬闪。

**修法**

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    scroll-behavior: auto !important;
    transition-property: opacity, color, background-color, border-color !important; /* 保留 */
    transition-duration: 150ms !important;
  }
  button:active:not(:disabled) { transform: none; }
}
```

即：**只砍位移/缩放，保留颜色与不透明度的短过渡**。

### P2-4 `.ui-enter` 在每次切 tab 播放，且用了不可中断的 keyframes

`src/index.css:92-106` + `App.tsx:570,609,625,641,657,673`（6 个 tab 面板全挂）

```css
.ui-enter { animation: ui-enter 260ms ease-out both; }  /* opacity + translateY(6px) */
```

**违反** review-animations/STANDARDS 的频次表：切 tab 属于「每天几十次」→ 判定「移除或大幅削减」。而且 `@keyframes` **不可中断**（STANDARDS「Interruptibility」）：连续点两个 tab，第二次从零重新播，第一次的进度被丢弃。

**修法** 去掉 tab 面板上的 `.ui-enter`（6 处）。它只该留在**真正新出现的内容**上 —— `BuyPlanSection.tsx:203` 那处展开是合理用法，保留。若一定要给 tab 切换一点交代，用 `transition` 驱动的 60–100ms 纯 opacity 淡入（可中断），不要位移。

### P2-5 Tab 指示器硬跳，没有连续性

`App.tsx:731`：`transition-colors` 只过渡颜色，紫色药丸在两个 tab 之间**瞬移**。

**违反** apple-design §7「空间一致性」＋ §8「向手势方向暗示」。

**修法** 药丸做成单独一个绝对定位层，用 `transform: translateX()` 在 tab 之间移动，`transition: transform 240ms cubic-bezier(0.23,1,0.32,1)`（这条曲线 `index.css` 里已经在用了，直接复用）。位移动画走 `transform` 走合成器，符合 §11。

### P2-6 `⌄` 有 rotate 无 transition

`RegimeSection.tsx:35`：`<span className="text-ink-muted group-open:rotate-180">⌄</span>` —— 状态切换时 180° 硬翻。

**修法** 加 `transition-transform duration-200 ease-out`。同时把 `⌄` 换成行内 SVG chevron（文字字形在不同平台垂直位置不一，旋转中心也不对）。全仓另一处 `⌄` 一并处理。

### P2-7 一个组件里三个断点：480 / 520 / 640

`AllocationChart.tsx`：`isNarrow = size.width < 520`（JS）、`sm:h-96` = 640（CSS）、`min-[480px]:grid-cols-2`（CSS）。三个数字，没有一个来自共享 token。

**违反** apple-design §16-7「Craft：每一个间距、时长、对齐值都是能被辩护的刻意选择，没有随机数」。

**修法** 收敛到 Tailwind 默认断点（`sm:640`）：窄屏判定用 `sm` 的反面，明细表两列也用 `sm:grid-cols-2`。配合 P0-1 一起改，正好是同一个组件。

### P2-8 字距没有尺寸阶梯

全仓 `tracking-*` 只有 12 处，且只有 `tracking-tight` / `tracking-wide` 两档，`我的投资组合`（`text-2xl sm:text-3xl`）和 `今日结论`（`text-sm`）用的是同一套默认字距。

**违反** apple-design §15「字距是尺寸相关的，绝不能一个值通用；大字要负字距，小字要略正」。

**修法** 在 `index.css` 加三条工具类并按字号套用：`.text-display { letter-spacing: -0.02em }`（≥text-2xl）、正文 `0`、`text-[10px]/text-xs` 的标签类 `+0.01em`。改动限于标题和小标签，不动正文。

### P2-9 `⚪` emoji 当状态图标（45 处）

`ConditionLookup.tsx:174-177,727-728`、`OpportunityOverview.tsx:199`。白色圆圈 emoji 在 iOS/Android/macOS 字形、大小、基线各不相同，且没有承载任何颜色语义 —— 28 个标的全是同一个白圈，扫视时得不到任何信息。

**修法** 换成 8px 的 `<span>` 圆点，颜色接语义 token（`不买` → `--neutral`、`无法判定` → `--trim`、`条件完整` → `--buy`），并加 `aria-hidden`（状态已在文字里）。这样列表一眼就能看出 4 个"无法判定"在哪。

### P2-10 `window.confirm` 当离开确认

`App.tsx:527-536`。系统级模态弹窗，和设计体系无关，无法定制文案层级，且在 iOS Safari 上外观突兀。

**违反** apple-design §16-2「Agency：确认对话框只留给真正破坏性、不可逆的操作，用多了人就闭眼点过」。离开一个编辑器不是不可逆操作 —— 正确做法是**自动存草稿**。

**修法** 优先：`估值` 编辑器改为切 tab 时把草稿存进 localStorage，回来自动恢复，去掉 confirm。若暂时不做草稿，至少换成应用内自绘的对话框（复用 P2-2 的 `--elev-3` 表面 + 蒙层）。

### P2-11 死 CSS

`index.css:123-127` 的 `prefers-reduced-transparency` 规则针对 `[class*='backdrop-blur']`，而全仓没有任何元素用 `backdrop-blur`。做完 P2-2 第 3 条（sticky 半透明导航）它会自动变成活代码 —— 所以**顺序上先做 P2-2，再回头确认这条**；如果决定不做半透明导航，就把这段 CSS 删掉。

---

## 4. 执行顺序建议

```
第 1 批（功能性，独立可测）
  P0-1 饼图  →  P0-2 裁决条位置  →  P0-4 headline  →  P0-5 陈旧判定
  验收：npm test 绿 + 1440/375 手测

第 2 批（格式化，纯展示层，可批量）
  P0-3 天数  P0-6 千分位  P0-10 ISO  P0-11 浮点  P0-9 coverage=
  做法：先把 format.ts 补齐（dateText / staleDaysFrom / money / signedPct），再逐处替换

第 3 批（口径与文案）
  P0-8 成本分类  P0-7 ⓘ 可点  P0-12 heading
  P1-1 ~ P1-23（P1-5 刷新周期表、P1-9 单快照 优先，其余按文件聚合改）

第 4 批（设计工程）
  P2-1 font-mono（最大观感收益，逐文件 commit）
  P2-3 reduced-motion  →  P2-4 去 ui-enter  →  P2-2 三级表面 + sticky 半透明导航
  P2-5 药丸位移  P2-6 chevron  P2-9 状态点  P2-7 断点  P2-8 字距
  P2-10 草稿替代 confirm   P2-11 死 CSS 收尾
```

## 4.1 全局验收协议（沿用 V11/V12/V14）

1. `npm test` —— 全部通过，新增测试数量 ≥ 本文件 P0/P1 条目数。
2. `npm run build` —— 记录 `dist/assets/index-*.js` hash。
3. 部署后确认 **本地 hash == VPS 站 hash == Pages 站 hash**。
4. 手测清单：
   - 1440 → 375 → 1440 拖动，饼图不空白，`scrollWidth === innerWidth` 恒成立
   - 375px 下逐个点 6 个 tab，每个 tab 的首个内容标题都在首屏内
   - `设置` 页改一个字段，sticky 保存条出现
   - 开系统"减少动效"，主题明暗切换是渐变不是硬闪
   - 全站搜索：无 `T\d\d:\d\d:\d\d\.\d{6}`、无 `coverage=`、无 `\d\.\d{4,}`、无无千分位的五位数金额

---

## 5. 【量化侧】需求单（网站不得自行改写，只能兜底）

> 以下全部是后端下发字符串/字段本身的问题。网站按铁律只做展示，最多做格式化兜底（已在 P0-11 说明）。请同步到量化仓库。

### Q-1 `final_verdict.symbols[].single_sentence` 理由重复

```json
"single_sentence": "不买：六关未通过：低位、形态；另有六关未通过：低位、形态"
```

同一条理由被拼了两遍（`blocking_layers` 里只有一条 `gates_six`）。28 个标的**全部**如此。而且一句话里三级冒号（`不买：` + `六关未通过：`），读起来断不开。

建议格式：`不买 · 六关未通过（低位、形态）`。

### Q-2 `基准 FNGS还差 0pp` —— 缺空格，且 `0pp` 与"不放行"矛盾

```json
"single_sentence": "不买：基准 FNGS还差 0pp（触发价 $71.57）"
```

两个问题：

1. `FNGS` 与 `还差` 之间缺空格（`TECL`/`SOXL` 同款）。
2. `gap_pp: 0` 但仍然 `failed` —— 真正的阻塞在均线：

```json
"reason": "以 FNGS 闭市价判定｜回撤 11.72% / 阈值 11.00%：✓｜均线触及 ✗（MA100 还差 0.75%、MA120 还差 2.71%、MA250 还差 2.94%）"
```

一句话摘要只报了已经满足的那一维（回撤 gap 0pp），把真正没过的那一维（均线）扔了。用户读到"还差 0pp"会认为马上就要开窗。这与 V10「压成红点丢掉 trigger/detail」是同一类错误，请在摘要里报**未通过的那一维**。

> 网站侧配套改动（本轮做）：`条件查询` / 最终裁决条不要只印 `single_sentence`，把 `layers[].reason` 的完整明细一并展示（数据已经在 payload 里，展示更多不违反只读铁律）。

### Q-3 金额缺 `$`

`sleeve_borrow.reason`：`已用满 10.0pp 借额（上限 75%），仍超硬顶 24,906，不建议新增科技`。`24,906` 无货币符号，与同句的 `75%` 并排容易被读成另一个百分数。

### Q-4 半角/全角标点混用

`认知参考,禁止据此机械调参;参数变更仍须影子期；历史统计不代表未来收益`

一句话里半角 `,`、半角 `;`、全角 `；` 三种都有。中文文案请统一全角。

### Q-5 陈旧天数三个值互相矛盾

| 字段 | 值 |
|---|---|
| `valuation_tab.ndx.stale_days` | **4** |
| `freshness.stale_days` | 2 |
| `freshness.max_stale_days` | 2 |
| `final_verdict.stale_days` | 2 |
| 对应 `as_of` | `2026-07-28`（相对 2026-07-30 = 2 天）|

请统一为 2，并说明 `valuation_tab.ndx.stale_days` 的 4 是怎么算出来的（疑似算进了非交易日或起算点差异）。

### Q-6 `data_stale` 布尔量与 `stale_days` 不一致

`final_verdict.data_stale = false` 但 `stale_days = 2`；`freshness.data_stale = false` 但 `stale_days = 2` / `max_stale_days = 2`。网站本轮改成"任一 > 0 即视为陈旧"来兜底（P0-5），但布尔量本身请修正。

### Q-7 `plan_link.nearest.missing` 下发未格式化浮点

```json
"missing": ["CNN < 25（当前 34.4857142857143）", "回撤 < -45%（当前 -29.00871742416146）"]
```

请改为结构化字段，由网站负责格式化：

```json
"missing": [
  { "metric": "CNN", "op": "<", "threshold": 25, "current": 34.4857142857143, "unit": "point" },
  { "metric": "drawdown", "op": "<", "threshold": -45, "current": -29.0087174, "unit": "pct" }
]
```

同时这也能解决 P1-18 的单位混用 —— 单位由 `unit` 字段决定，不再靠拼字符串。

### Q-8 恐慌窗口数据缺口

`FAS / SPXL / UDOW / UPRO` 四个标的 `无法判定：恐慌窗口数据暂缺`。这四个都是 3× 标的，恐慌窗口是它们的核心闸门。请确认是数据源缺失还是计算未覆盖 —— 只要有一个 `UNDECIDABLE`，用户看到的日度结论就会被拉低（网站本轮改成计数式 headline 缓解，但根因在数据）。

### Q-9 「丹居」数据源名待确认

估值页两处显示 `丹居 · 2026-07-28`、`丹居 · 2021-07-28 至 2026-07-28`。请确认这是量化侧有意的数据源标识，还是某个源名的误写。若是内部代号，建议改为用户能理解的来源说明（如 `量化系统 · Futu OpenD`，与同页 `原始源 Futu OpenD` 的写法一致）。

### Q-10 VIX 两个截止日

`VIX 波动率` 卡写 `Cboe · 2026-07-29`，`波动率与胜率研究` 写 `真实 VIX · 2018-01-02 至 2026-07-24`。同一指标两个截止日期（07-29 / 07-24）。

---

## 6. 附：本次审查的一手证据（供 Codex 复核）

```js
// 1) 饼图死态（拖窄后，容器空）
{"cls":"min-h-72 w-full min-w-0 sm:h-96","h":288,"w":342,"childCount":0,"html":""}
// 拖回 1280 仍空
{"step":"back to 1280 (no reload)","hostW":1078,"hostH":384,"childCount":0}

// 2) 中间态横向溢出
{"innerWidth":375,"docW":1107}
{"cls":"recharts-legend-wrapper","w":1030,"style":"position: absolute; width: 1030px; ..."}

// 3) 裁决条高度 / 内容起点
1280px: statusBarH=782   375px: statusBarH=1424
点「持仓」: tabContentTop=970

// 4) 后端 payload 关键片段
{"final_verdict":{"data_as_of":"2026-07-28","data_stale":false,"stale_days":2}}
{"freshness":{"stale_days":2,"max_stale_days":2,"price_session":"pre",
              "prices_at":"2026-07-30T08:49:24.092951-04:00"}}
{"valuation_tab.ndx.stale_days":4}
{"AAPL.single_sentence":"不买：六关未通过：低位、形态；另有六关未通过：低位、形态"}
{"regime_status.verdict_card.plan_link.nearest.missing":
   ["CNN < 25（当前 34.4857142857143）","回撤 < -45%（当前 -29.00871742416146）"]}

// 5) 设计体系用量
grep -o "shadow-[a-z0-9/-]*"  → 0 命中
grep -rn "backdrop-blur"      → 仅 index.css:124（死代码）
grep -c font-mono             → ConditionLookup 38 / Summary 24 / HoldingsTable 21 ...
grep -o "tracking-[a-z]*"     → 全仓 12 处，仅 tight / wide 两档
console                       → 无任何 log/error
```
