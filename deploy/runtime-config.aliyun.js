// Written into the deployed static folder. Keeping the URL relative means the
// same bundle continues to work after a future domain/HTTPS migration.
window.__PORTFOLIO_TRACKER_RUNTIME__ = {
  apiBaseUrl: window.location.origin,
  deploymentLabel: '阿里云中转',
};
