# 修复方案 V9：机会一览 + 高亮状态（不让用户一个个点）

> 撰写：Claude（审计大脑），2026-07-19
> 执行：Codex（唯一施工者）
> 授权/凭证/纪律沿用 V7/V8（网站）与交接8/9/10（量化，导出层可改）；channels.yaml 禁读。
> 用户核心不满一句话：**"哪些能买、哪些能卖，页面应该直接告诉用户并高亮出来——让用户一个个下拉去点，是浪费用户时间的错误产品理念。"**

---

## 1. 现状与问题（截图实证，2026-07-19）

- 条件查询/卖出窗口都是"先选标的再看结果"：买入侧 21 只、卖出侧十几个持仓，用户要逐个点完才知道今天有没有机会。
- 深度位卡信息全但**没有视觉状态**：「当前回撤 11.25% · 阈值 17.45% · 成功率 59.09%」是一行平淡文字，达标/接近/远离看不出差别，成功率不醒目。
- 下拉选项无任何状态标记，选之前全靠猜。

---

## 2. F1 今日机会一览（本期核心）

### 2.1 产品行为

条件查询 Tab 顶部新增「**今日机会一览**」面板（总览 Tab 顶部放同一组件的紧凑版），打开页面第一眼就是结论：

```
┌ 今日机会一览        快照 13:18 ET · 8 分钟前
│
│ 🟢 可买入（条件已满足）
│   TQQQ  深度位✓ 恐慌位✓  回撤 26.7%/阈值 21.7  60日胜率 66.7%(n=18)  →详情
│   （空时显示：今日没有满足条件的买入标的）
│
│ 🟡 接近买入条件（差距 ≤3 个百分点）
│   AMZN  回撤 15.2%/阈值 17.5  还差 2.3 点
│
│ 🔴 可卖出（持仓中有触发依据）
│   MSFT  知足常乐触发：自基准日 +31% vs QQQ +29%  →详情
│   FNGU  止盈阶梯：盈利 10%+ 档达标  →详情
│
│ ⚪ 其余 16 只今日无操作窗口 —— 耐心等待也是操作
└ 只提醒不下单；由你在券商 App 手动执行。
```

- 每行可点击 → 直接跳到该标的的详情卡（复用现有卡组件，下拉自动选中）。
- 空状态是产品态度：三组都空时显示大字「今日无操作窗口，耐心等待」。
- 下拉框选项加状态点：🟢 可买 / 🟡 接近 / 🔴 可卖（持仓）/ ⚪ 无。

### 2.2 数据口径（判定必须后端算，前端只渲染——延续 X-9 铁律）

- `site_export.py` 新增 `summary` 区块（量化侧导出层，属既有授权范围）：
  ```json
  "summary": {
    "buy_ready":   [{"symbol": "TQQQ", "kind": "etf3x", "reason": "深度位+恐慌位双开", "drawdown_pct": -26.7, "threshold_pct": -21.7, "win_rate_60d": 0.667, "n": 18}],
    "buy_near":    [{"symbol": "AMZN", "gap_pct": 2.3, "drawdown_pct": -15.2, "threshold_pct": -17.5}],
    "sell_ready":  [{"symbol": "MSFT", "trigger": "知足常乐", "detail": "自基准日 +31% vs QQQ +29%"}],
    "generated_at": "..."
  }
  ```
- 判定规则（全部复用现有引擎结论，**不新造规则**）：
  - `buy_ready`：3 倍 = 恐慌窗口深度位∧恐慌位双开（armed/firing）；个股 = 自身深度位成立。
  - `buy_near`：距深度阈值 ≤ `summary.near_gap_pct`（rules.yaml 新键，默认 3）。
  - `sell_ready`：仅持仓族——修复完成状态、知足常乐触发、补涨收敛 active、止盈阶梯达标（+50% 默认止盈含在内），任一成立即入列，`trigger` 写明是哪条。
  - 影子期未转正的依据带 `"shadow": true`，前端渲染「（观察期）」角标——延续 X-5。
- 前端零判定逻辑：只按 summary 渲染分组与排序（买入按"超出阈值深度"降序，接近按差距升序）。

---

## 3. F2 高亮状态系统（回撤/阈值/成功率醒目化）

对深度位卡（个股+3 倍统一）：

1. **状态徽章**（颜色 + 文字，色弱可辨）：
   - 深度位达标 → 绿色大徽章「✓ 已达标」；
   - 距阈值 ≤3 点 → 琥珀徽章「接近 · 还差 2.3 点」；
   - 远离 → 灰色「未达标」。
2. **回撤 vs 阈值**：大号数字 + 进度条（当前回撤/阈值，达标后条变绿且尾部显示超出幅度）；替代现在的一行小字。
3. **成功率**：大字号显示「60 日胜率 59.09%」+ 小字 `n=22`；`n<20` 时灰显并强制加「样本不足」标签（诚实规则不因高亮而弱化）；胜率不用红绿渲染（它不是涨跌，是统计），用中性强调色。
4. 恐慌位行同样加 ✓/✗ 徽章 + 触发时段标注（沿用 triggered_session 字段）。
5. 主题适配：现有深/浅两套主题都要调（截图两种都在用）；对比度 ≥ WCAG AA；手机 390px 宽零横向溢出。
6. 百分比统一 2 位小数（延续 V8 A4-5，一览面板同样适用）。

---

## 4. 施工顺序与验收

| 步 | 内容 | 验收 |
|---|---|---|
| 1 | 量化侧 `site_export` summary 区块（TDD：mock 引擎状态断言分组正确） | pytest 先红后绿；`buy_ready/near/sell_ready` 判定全来自现有引擎结论，grep 无新阈值硬编码 |
| 2 | 前端一览组件 + 下拉状态点 + 跳转联动 | npm test 快照用例：三组渲染、空态文案、shadow 角标、点击跳转选中 |
| 3 | 高亮状态系统（徽章/进度条/大字成功率） | 快照测试：达标/接近/远离三态、n<20 灰显、两主题、390px 无溢出 |
| 4 | 总览 Tab 紧凑版一览 | 与条件查询数据同源同快照 |
| 5 | 回归 + 部署 | 两仓库测试不低于现基线（量化 291+/网站 186+）；V7 部署流程；手机+桌面实机验收 |

---

## 5. 易错点（继承 V7 W、V8 X、交接9 Y、交接10 W 全部有效；本期新增）

- **V-1 判定别搬到前端**：一览的"可买/可卖/接近"三个判定全部由 site_export 输出。前端出现任何 `drawdown >= threshold` 之类的比较 = 违规返工（X-9 的老教训，别再犯）。
- **V-2 卖出一览只扫持仓族**：不持有的标的永远不进 sell_ready；现金类（SGOV）两侧都过滤（X-7 延续）。
- **V-3 一览与详情同一份快照**：禁止一览用新快照、详情用旧快照造成两处结论打架；组件共享同一次 fetch。快照过期黄条对一览同样生效。
- **V-4 高亮不得越过诚实线**：样本不足的胜率禁止绿色/大字强调，必须灰显+标注；影子期依据必须带角标。醒目 ≠ 夸大。
- **V-5 空态别做成 bug 样式**：三组全空是常态（大多数交易日就该无操作），空态文案是设计的一部分，不是 fallback。
- **V-6 排序稳定**：同组内排序键明确（买入=超阈深度降序，接近=差距升序，卖出=触发类型固定顺序），避免每次刷新行序乱跳。
- **V-7 下拉状态点的性能**：状态从已加载的 summary 内存取，不要为每个选项发请求。
- **V-8 summary 的 near_gap_pct 进 rules.yaml**：默认 3，零魔法数字门禁在量化仓库守着。
