# 需求单：量化侧 V16 裁决文案与陈旧天数

> 本文只记录量化系统下发字段和文案的问题。网站保持只读展示，不自行重算或改写量化结论。

## Q-1 `final_verdict.symbols[].single_sentence` 理由重复

当前示例：

```json
"single_sentence": "不买：六关未通过：低位、形态；另有六关未通过：低位、形态"
```

同一理由被拼接两遍，且一句中出现三级冒号。建议输出为：`不买 · 六关未通过（低位、形态）`。

## Q-2 `基准 FNGS还差 0pp` 缺空格且摘要与阻塞原因矛盾

当前示例：

```json
"single_sentence": "不买：基准 FNGS还差 0pp（触发价 $71.57）"
```

- `FNGS` 与 `还差` 之间缺少空格，TECL、SOXL 也有同类问题。
- `gap_pp: 0` 但状态仍为 `failed`；实际未通过项是均线触及，而摘要只展示已经满足的回撤维度。

请让摘要展示真正未通过的维度，避免“还差 0pp”被误解为即将放行。网站已只读展示 `layers[].reason` 明细，不替量化侧重写摘要。

## Q-3 金额缺少货币符号

`sleeve_borrow.reason` 示例：

```text
已用满 10.0pp 借额（上限 75%），仍超硬顶 24,906，不建议新增科技
```

请给 `24,906` 补充 `$` 或明确币种，避免与百分数混淆。

## Q-4 中文文案半角与全角标点混用

当前示例：

```text
认知参考,禁止据此机械调参;参数变更仍须影子期；历史统计不代表未来收益
```

请统一为中文全角标点。

## Q-5 陈旧天数三个值互相矛盾

| 字段 | 当前值 |
|---|---:|
| `valuation_tab.ndx.stale_days` | 4 |
| `freshness.stale_days` | 2 |
| `freshness.max_stale_days` | 2 |
| `final_verdict.stale_days` | 2 |
| 对应 `as_of` | `2026-07-28` |

相对 `2026-07-30` 应为 2 天。请统一，并说明 `valuation_tab.ndx.stale_days = 4` 的计算口径。

## Q-6 `data_stale` 与 `stale_days` 不一致

- `final_verdict.data_stale = false`，但 `stale_days = 2`。
- `freshness.data_stale = false`，但 `stale_days = 2`、`max_stale_days = 2`。

网站目前用“任一陈旧天数大于 0 即视为陈旧”兜底；请修正量化侧布尔字段。

## Q-7 `plan_link.nearest.missing` 下发未格式化浮点字符串

当前示例：

```json
"missing": [
  "CNN < 25（当前 34.4857142857143）",
  "回撤 < -45%（当前 -29.00871742416146）"
]
```

建议改为结构化字段：

```json
"missing": [
  { "metric": "CNN", "op": "<", "threshold": 25, "current": 34.4857142857143, "unit": "point" },
  { "metric": "drawdown", "op": "<", "threshold": -45, "current": -29.0087174, "unit": "pct" }
]
```

这样网站可按 `unit` 统一格式化，避免百分比和百分点混用。

## Q-8 四个 3× 标的恐慌窗口数据缺失

`FAS`、`SPXL`、`UDOW`、`UPRO` 当前均为：

```text
无法判定：恐慌窗口数据暂缺
```

请确认是数据源缺失还是计算覆盖不完整。任一 `UNDECIDABLE` 都会降低日度结论的完整性。

## Q-9 「丹居」数据源名待确认

估值页收到的来源文案包括：

- `丹居 · 2026-07-28`
- `丹居 · 2021-07-28 至 2026-07-28`

请确认“丹居”是否为有意的用户可见来源名。若为内部代号，建议替换为用户能理解、且与同页一致的来源说明。

## Q-10 VIX 两个截止日期不一致

- `VIX 波动率`：`Cboe · 2026-07-29`
- `波动率与胜率研究`：`真实 VIX · 2018-01-02 至 2026-07-24`

请说明两个数据集的刷新节奏或统一截止日期，避免用户误认为同一指标的数据相互矛盾。

