# 投资组合网站项目交接与量化系统对接说明

> 更新时间：2026-07-15  
> 用途：把本文件直接交给其他 AI，帮助其快速定位源码、理解当前产品状态，并在不破坏既有链路的前提下继续开发。  
> 安全说明：本文不包含任何 SSH 密码、API Key 或同步 Token。

## 1. 一分钟结论

这是一个个人投资组合网站，已经形成以下完整链路：

```text
IBKR + 长桥 + 富途
        ↓
Mac 量化系统 futu-assistant（持仓真源，只读）
        ↓ positions-status CLI
Mac 定时推送脚本（每 45 分钟）
        ↓ 带 Token 的 POST
美国 VPS 网关与最新快照存储
        ↓ GET 最新快照
React 投资组合网站（电脑和手机共用同一份持仓）
```

目前三券商持仓聚合、跨设备同步、持仓总览、情景计算器、截图 AI 导入、期权等效仓位、杠杆 ETF、行情刷新和多币种显示已经完成。网站不是交易系统，不向券商下单；它只读取、展示和分析数据。

继续开发时，最重要的是区分：

1. **网站源码**：可以修改，主仓库是 `portfolio-tracker`。
2. **量化系统**：当前属于只读真源，未经新授权不得修改。
3. **VPS**：部署网站、网关和最新快照；不要触碰其他项目的进程、端口或 Nginx。

## 2. 所有关键位置

| 类型 | 位置 | 用途 | 处理规则 |
|---|---|---|---|
| 网站源码仓库 | `/Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker` | React 前端、网关、同步脚本、测试、部署脚本 | 本项目主要工作目录，可以修改 |
| GitHub 仓库 | `https://github.com/c670752068-cell/portfolio-tracker` | 远端源码和 GitHub Actions | 推送 `main` 会部署 Pages |
| GitHub Pages 入口 | `https://c670752068-cell.github.io/portfolio-tracker/` | 公共入口，目前会按运行时配置进入正式站 | 用于外部访问与兜底 |
| 正式网站 | `http://67.215.255.196:8788/` | 当前实际使用的 VPS 版本 | 手机和电脑应访问同一入口 |
| Mac 量化系统 | `/Users/wuhuazhou/Projects/futu-assistant` | IBKR、长桥、富途聚合持仓的只读真源 | **默认只读，不得擅自修改** |
| 量化 CLI | `/Users/wuhuazhou/Projects/futu-assistant/.venv/bin/futu-assistant positions-status` | 输出网站使用的聚合持仓 JSON | 允许调用 |
| 量化接入契约 | `/Users/wuhuazhou/Documents/Codex/2026-07-09/wo-me/outputs/网站接入指南_量化系统持仓数据契约.md` | 规定网站与量化系统的安全边界 | **硬禁区条款优先级最高** |
| 当前持仓审计 | `/Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker/持仓同步与情景计算审计-2026-07-15.md` | 最近一次 25 条持仓逐项核对、IGV/MSFU 问题和修复结论 | 新 AI 应优先阅读 |
| 历史方案 | 仓库根目录下 `修复方案.md`、`修复方案-V2.md` 至 `修复方案-V6-量化系统接入与情景计算器.md` | V1–V6 的产品决策和验收要求 | 本机可读；属于本地文档，GitHub 克隆可能没有 |
| VPS 信息 | `/Users/wuhuazhou/Desktop/搭建服务器计划/VPS信息.md` | SSH 等部署资料 | 含敏感信息；只在获准部署时读取，禁止复制进聊天、代码或 Git |
| 本机同步 Token | `~/.portfolio-sync-token` | Mac 推送快照到 VPS 的认证 | 必须保持权限 `600`，禁止提交 |
| 同步日志 | `~/Library/Logs/portfolio-sync.log` | 定时推送成功/失败记录 | 可用于排错，不应提交 |

## 3. 网站仓库应该读取哪些文件

### 3.1 新 AI 的最短阅读顺序

如果时间有限，按以下顺序读：

1. 本文件。
2. `README.md`：功能和部署说明。注意开头“纯前端”是历史描述；当前实际是“React 前端 + VPS 网关 + Mac 推送器”。
3. `持仓同步与情景计算审计-2026-07-15.md`：当前数据核对结论。
4. `修复方案-V6-量化系统接入与情景计算器.md`：当前量化接入架构和验收标准。
5. 外部的 `网站接入指南_量化系统持仓数据契约.md`：量化系统边界。
6. `src/types.ts`、`src/App.tsx`、`src/quantSync.ts`：核心数据结构、应用状态和同步合并逻辑。
7. `src/metrics.ts`、`src/scenario.ts`、`src/leverageMap.ts`：金额、等效仓位和情景计算。
8. `sync/push-positions.mjs`、`sync/com.portfolio.sync.plist`：Mac 定时推送。
9. `deploy/aliyun-gateway.mjs`、`deploy/deploy-us-vps.sh`、`deploy/runtime-config.us-vps.js`：VPS 网关和部署。
10. 对应的 `*.test.ts` / `*.test.tsx`：它们是现有产品规则的可执行说明。

所有相对路径均以此目录为根：

```text
/Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker
```

### 3.2 目录与文件职责

```text
portfolio-tracker/
├── src/                              # React/TypeScript 产品源码
│   ├── App.tsx                       # 主状态、Tab、量化同步、行情刷新、导入总编排
│   ├── types.ts                      # 持仓、期权、量化快照、设置等数据契约
│   ├── storage.ts                    # 浏览器 localStorage 读写与备份
│   ├── runtimeConfig.ts              # 正式站/Pages/本地环境的 API 地址解析
│   ├── quantSync.ts                  # 量化快照映射、增强信息保留、净值对账
│   ├── metrics.ts                    # 总资产、流动性、盈亏、Delta/等效暴露计算
│   ├── scenario.ts                   # 正股、杠杆 ETF、期权的情景计算
│   ├── leverageMap.ts                # MSFU 等杠杆产品的已知倍数与底层映射
│   ├── marketData.ts                 # 股票/ETF 行情刷新
│   ├── kimi.ts                       # 智谱/Kimi 截图识别、提示词和结果标准化
│   ├── importMerge.ts                # 完整组合导入/期权详情补充的安全合并
│   ├── analyzer.ts                   # 本地风险规则
│   ├── allocation.ts                 # 资产占比聚合
│   ├── assetClass.ts                 # 现金类 ETF 等资产分类
│   ├── exchangeRates.ts              # 汇率获取和缓存
│   ├── displayCurrency.ts            # 仅显示层的币种换算
│   ├── valueHistory.ts               # 浏览器内组合价值历史
│   └── components/
│       ├── Summary.tsx               # 总览卡片和同步状态
│       ├── HoldingsTable.tsx         # 持仓表
│       ├── ScenarioCalculator.tsx    # 情景计算器 Tab
│       ├── ImageImportPanel.tsx      # 截图导入
│       ├── SettingsPanel.tsx         # AI、行情、显示货币等设置
│       ├── AnalysisPanel.tsx         # 分析结果
│       ├── AllocationChart.tsx       # 资产占比图/手机明细
│       ├── RiskList.tsx              # 风险提示
│       └── CashEditor.tsx            # 手动现金
├── sync/
│   ├── push-positions.mjs            # 调 CLI、校验 Token、向 VPS 推送快照
│   ├── extractJson.mjs               # 从 CLI 混合输出中提取首个完整 JSON
│   └── com.portfolio.sync.plist      # macOS launchd，每 2700 秒执行一次
├── deploy/
│   ├── aliyun-gateway.mjs            # 静态站、AI、行情、持仓快照和导入留档网关
│   ├── deploy-us-vps.sh              # 当前美国 VPS 部署脚本
│   ├── deploy-aliyun.sh              # 历史/备用部署脚本
│   ├── runtime-config.us-vps.js      # 正式站同源 API 配置
│   ├── runtime-config.aliyun.js      # 阿里环境配置
│   └── portfolio-tracker.nginx.conf  # Nginx 参考配置
├── public/runtime-config.js          # GitHub Pages 运行时入口配置
├── .github/workflows/deploy.yml      # GitHub Pages 自动部署
├── docs/AI_IMPORT_DEPLOYMENT_ANALYSIS.md
├── 持仓同步与情景计算审计-2026-07-15.md
├── package.json
└── README.md
```

以下目录不是源码，不应手工修改或交给 AI 逐文件阅读：

- `node_modules/`：本机依赖。
- `dist/`：`npm run build` 自动生成的部署产物。

## 4. 数据源与真值优先级

### 4.1 三券商实际持仓

最高优先级真源是：

```text
/Users/wuhuazhou/Projects/futu-assistant
```

网站只允许通过下面的 CLI 获取聚合结果：

```bash
/Users/wuhuazhou/Projects/futu-assistant/.venv/bin/futu-assistant positions-status
```

它聚合 IBKR、长桥和富途。网站不能自己重新登录三家券商，也不能把三个券商的连接逻辑复制进前端。

### 4.2 量化系统只读边界

在没有用户另行明确授权的前提下，其他 AI 对 `futu-assistant` 只能：

- 调用 `positions-status` CLI。
- 阅读 `data/*.json`。
- 阅读 `README.md`、`docs/ARCHITECTURE.md`、`docs/ERRORS.md` 等文档。

禁止：

- 修改量化仓库任何文件。
- 从网站代码直接 import 量化系统内部 Python 模块。
- 读取 `config/channels.yaml`。
- 写入 `store.sqlite`。
- 把网站变成券商下单入口。

涉及量化系统的工作完成后必须验证：

```bash
git -C /Users/wuhuazhou/Projects/futu-assistant status --short
```

输出应为空。

### 4.3 量化研究数据

可以只读接入的现有研究结果包括：

```text
/Users/wuhuazhou/Projects/futu-assistant/data/drawdown_stats.json
/Users/wuhuazhou/Projects/futu-assistant/data/event_study.json
/Users/wuhuazhou/Projects/futu-assistant/data/backtest_results.json
/Users/wuhuazhou/Projects/futu-assistant/data/valuation_context.json
```

这些适合未来放到网站的风险/研究页面，但网站应消费导出的 JSON，不应调用量化引擎内部代码。

### 4.4 前端本地数据

浏览器还会保存用户自己的设置和增强信息：

```text
portfolio-tracker:portfolio-v1
portfolio-tracker:settings-v1
portfolio-tracker:portfolio-backup-v1
portfolio-tracker:value-history-v1
```

这些在浏览器 `localStorage` 中。AI Key、行情 Key、期权截图补充的 Greeks 等不会自动上传 GitHub。

## 5. 当前端到端架构

### 5.1 Mac 定时推送

- `sync/com.portfolio.sync.plist` 每 `2700` 秒，即 45 分钟运行一次。
- `sync/push-positions.mjs` 调用 `positions-status`。
- Token 从环境变量或 `~/.portfolio-sync-token` 读取；文件权限必须是 `600`。
- 脚本把快照 POST 到：

```text
http://67.215.255.196:8788/api/portfolio/positions
```

- 失败会等待 30 秒重试一次，并写入 `~/Library/Logs/portfolio-sync.log`。

### 5.2 VPS 网关与快照

当前 VPS 约定位置：

```text
/opt/portfolio-tracker-web                         # 静态网页
/opt/portfolio-tracker-gateway/portfolio-gateway.mjs
/opt/portfolio-tracker-data/positions/latest.json # 最新三券商快照
/opt/portfolio-tracker-data/imports                # 截图导入留档（受控）
```

当前服务由 PM2 进程 `portfolio-tracker` 运行，端口为 `8788`。

持仓接口：

- `POST /api/portfolio/positions`：必须带 `PORTFOLIO_SYNC_TOKEN`。
- `GET /api/portfolio/positions`：前端读取最新快照；当前为了电脑/手机直接同步而开放读取。

其他同源接口：

- `/api/quotes`：公开行情代理。
- `/api/zhipu/chat/completions`：智谱视觉请求中转。
- `/api/kimi/chat/completions`：Kimi 请求中转。

网关不应永久保存用户的 AI Key；Key 由浏览器在单次请求里提供。

### 5.3 前端收到快照后

核心逻辑在 `src/quantSync.ts`：

- 量化快照中的 `option` 类型优先于杠杆映射，防止 MSFU 期权被误判成杠杆 ETF。
- 已知杠杆 ETF 通过 `src/leverageMap.ts` 统一倍数；MSFU 当前按 **2×** 处理。
- `net_liquidation - 所有持仓市值` 形成现金/负债/应计/时差的净值对账调整，保证总资产与券商聚合净值一致。
- 同步时替换旧的 `quant-sync` 和 `image-import` 持仓，但保留明确的手动条目。
- 之前通过截图补充的期权 Greeks、成本等增强字段会尽量按标的匹配并保留；多个同标的期权无法唯一匹配时会要求重新补充。

## 6. 当前持仓数据契约

`positions-status` 当前给网站的核心字段如下。示例均为虚构值：

```json
{
  "as_of": "2026-07-15T12:00:00Z",
  "currency": "USD",
  "net_liquidation": 100000,
  "broker": "AGGREGATED",
  "position_count_by_broker": {
    "IBKR": 10,
    "LONGPORT": 3,
    "FUTU": 5
  },
  "positions": [
    {
      "broker": "FUTU",
      "symbol": "IGV",
      "asset_type": "option",
      "qty": 2,
      "market_value": 3800
    }
  ]
}
```

网站端对应类型定义位于 `src/types.ts`：

- `QuantPosition`
- `QuantPositionsPayload`
- `QuantPositionsSnapshot`
- `Holding`
- `OptionDetails`
- `PortfolioState`

当前契约能可靠表达：券商、代码、资产类型、数量、市值、净资产和快照时间。

当前契约**不能完整表达**：

- 稳定的账户 ID / 持仓 ID / 期权合约 ID。
- 期权行权价、到期日、Call/Put、Delta、Gamma、Theta、Vega、IV。
- 成本价、成本总额、券商报告盈亏。
- 同一标的多个不同期权合约的唯一对应关系。

因此“手机和电脑看到同一份原始持仓”已经完成，但“所有期权详情和浏览器私有增强字段跨设备同步”仍是下一阶段工作。

## 7. 当前已经完成并验证的产品状态

截至本文生成时，仓库 `main` 最新相关提交为：

```text
73c4f5b fix: reconcile synced derivatives and scenarios
909d002 docs: audit synced holdings and scenario calculations
fae9436 fix: share quant portfolio across devices
25f3f34 fix: preserve quant snapshot values during quote refresh
b768e2c feat: scenario calculator for price targets with greeks decay
f2c7fb7 feat: quant system positions sync with enrichment-preserving merge
ce54aae feat: mac-side positions pusher with launchd template
e8ff0f2 feat: authenticated portfolio positions store on gateway
```

最近一次正式验收确认：

- 聚合快照共 25 条：IBKR 17、长桥 3、富途 5。
- IGV 包含长桥的 72 股普通 ETF，以及富途的 2 张 IGV Call；两者不是重复或凭空生成。
- MSFU 按 2× 杠杆 ETF 处理；MSFU 期权保持期权类型。
- 手机与电脑从同一个 VPS 快照读取三券商持仓。
- 情景计算器支持普通股票/ETF、杠杆 ETF 和期权，并能把期权按底层标的归族。
- 期权 Delta 等效股数和等效美元暴露已经实现。
- 总览支持 USD/CNY/HKD/JPY/EUR/GBP 显示货币；内部计算仍以 USD 为基准。
- 截图 AI 导入支持“完整组合替换式导入”和“只补充期权详情”；后者绝不删除非期权持仓或现金。
- 支持现金类 ETF、资产占比、风险扫描、每日行情、今日涨跌和 JSON 备份。
- 最近一轮本地验证：20 个测试文件、127 个测试通过，构建和 lint 通过；手机尺寸和桌面浏览器无控制台错误。

如需逐条看持仓核对证据，不要重复猜测，请直接读取：

```text
/Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker/持仓同步与情景计算审计-2026-07-15.md
```

## 8. 已知限制和技术债

### P0：期权增强信息尚未完全跨设备

原始持仓已经跨设备，但期权 Greeks、行权价、到期日、成本等通常来自某一台浏览器的截图补充。新手机第一次打开时可能只有期权市值和数量，缺少 Delta 的期权会暂时不计入等效暴露。

### P0：正式接口仍是 HTTP，读取端当前未做用户认证

当前 `GET /api/portfolio/positions` 是为了自用的手机/电脑同步而开放。它与量化契约建议的“公开暴露必须认证”存在安全差距。更深度结合前应增加：

- 域名与 HTTPS。
- 读取端的安全会话、短期 Token 或其他不把长期密钥暴露在前端的认证方式。
- 同时保证手机访问体验不退化。

### P0：缺少稳定合约标识

仅靠 `broker + symbol + asset_type` 无法唯一表示同一券商、同一标的的多个不同期权合约。继续扩展前应在上游导出契约中增加稳定 `position_id` / `contract_id`。

### P1：同步周期是 45 分钟，不是实时

这是当前有意选择，足够用于组合分析，且不会让券商和网站产生不必要负载。不要未经需求把它改成高频轮询。

### P1：README 的“纯前端”描述过时

当前已经有 Mac 推送器和 VPS 网关。后续可单独更新 README，但不要因为文档过时而重写稳定架构。

### P1：历史方案是本机文档

V1–V6 方案在本机仓库根目录可见，但被本地 Git 排除规则忽略，远程 GitHub 克隆不一定包含。给远程 AI 工作前，应连同本文件和必要方案一起提供。

## 9. 下一阶段深度对接的推荐顺序

以下是对现有架构的自然延伸，不要求当前立即实施：

### 9.1 扩展只读导出契约

需要单独获得“可以修改量化系统导出层”的明确授权后，在量化系统的公开导出/CLI 契约增加：

- `position_id`：跨快照稳定。
- `broker`、脱敏 `account_id`。
- 股票/ETF 的 `cost_basis`、`unrealized_pnl`。
- 期权的 `underlying`、`contract_id`、`option_type`、`strike`、`expiration`、`multiplier`。
- 可用时提供 `delta/gamma/theta/vega/iv/underlying_price`。
- `captured_at`、字段来源和置信度。

网站仍然只消费 JSON；不要 import 量化系统内部代码。

### 9.2 建立受认证的服务器端增强层

把用户在网页补充的期权详情按稳定 `position_id` 保存到受认证的 VPS 数据层，从而让手机和电脑共享同一份增强信息。需要设计版本、过期策略、冲突合并和删除逻辑。

### 9.3 账户级与券商级对账

在总组合之外增加：

- 按 IBKR、长桥、富途查看净值、现金和持仓。
- 按账户核对持仓市值、现金/负债/应计与净资产。
- 对跨券商重复标的只在“风险暴露”中聚合，原始持仓表仍保留券商行。

### 9.4 接入量化研究结果

只读消费 `data/*.json`，新增回撤、事件研究、回测和估值页面。必须在 UI 上清楚区分：

- 实际持仓事实。
- 行情快照。
- 模型推演/情景估算。
- 历史回测和研究结论。

## 10. 开发、测试与部署

### 10.1 本地开发

```bash
cd /Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker
npm install
npm run dev
```

### 10.2 每次修改后的最低验收

```bash
cd /Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker
npm test
npm run build
npm run lint
git status --short
```

涉及同步或金额计算时，还必须验证：

- 持仓条数和三券商分布没有丢失。
- 总资产与 `net_liquidation` 对账。
- IGV 普通 ETF 与 IGV 期权没有混淆。
- MSFU 普通持仓是 2×，MSFU 期权仍是期权。
- 同一标的不同券商行在原始持仓表中没有被错误合并。
- 手机和桌面访问同一正式网址后显示相同服务端快照。
- 浏览器控制台无错误。

### 10.3 检查同步链路

```bash
node /Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker/sync/push-positions.mjs
tail -n 20 ~/Library/Logs/portfolio-sync.log
curl -sS http://67.215.255.196:8788/api/health
curl -sS http://67.215.255.196:8788/api/portfolio/positions
```

注意：不要把接口返回的完整私人持仓贴到公开位置。

### 10.4 部署

- GitHub Pages：提交并推送 `main`，由 `.github/workflows/deploy.yml` 自动部署。
- 正式 VPS：先 `npm run build`，再使用 `deploy/deploy-us-vps.sh`。
- 部署所需 Token/密码只从环境变量或本机安全文件读取，绝不写入仓库。
- 不要触碰 VPS 上与本项目无关的进程、端口和 Nginx。

## 11. 敏感信息规则

其他 AI 必须遵守：

- 不要把 `/Users/wuhuazhou/Desktop/搭建服务器计划/VPS信息.md` 的内容写入代码、文档、聊天记录或提交历史。
- 不要打印 `~/.portfolio-sync-token` 的内容。
- 不要读取或提交浏览器 localStorage 中的 AI Key。
- 不要在测试 fixture 中使用真实持仓、真实账号、真实 Token 或真实服务器密码。
- 文档和测试统一使用虚构值或脱敏值。
- 修改后执行敏感信息自查，再提交。

## 12. 可直接发给下一位 AI 的任务提示词

```text
请先完整阅读：
1. /Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker/项目交接说明-投资组合网站与量化系统.md
2. /Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker/持仓同步与情景计算审计-2026-07-15.md
3. /Users/wuhuazhou/Documents/Codex/2026-07-09/wo-me/outputs/网站接入指南_量化系统持仓数据契约.md

网站工作目录：
/Users/wuhuazhou/Desktop/股票Dashboard/portfolio-tracker

量化真源：
/Users/wuhuazhou/Projects/futu-assistant

量化仓库当前只读。只允许调用 positions-status CLI、读取 data/*.json 和文档；禁止修改文件、禁止 import 内部代码、禁止读取 config/channels.yaml、禁止写 store.sqlite。

请先用现有测试和审计文档确认当前行为，再做修改。使用测试先行；完成后运行 npm test、npm run build、npm run lint，并验证手机/桌面、三券商条数、净资产对账、IGV 普通 ETF/期权分类和 MSFU 2×。任何密码、API Key、Token 不得进入文件或 Git。
```

## 13. 建议下一位 AI 使用的工作方法

- 修复具体 bug：使用严格的复现、最小化、假设、修复和回归测试流程。
- 改计算或同步：测试先行，先写失败测试再实现。
- 做架构深化：先阅读当前审计、V6 方案和量化契约，再评估边界。
- 做网页验收：同时测试桌面和手机视口，检查控制台错误。

不要重新推翻已经验证的同步架构；新工作应在现有“量化 CLI → Mac 推送 → VPS 快照 → 前端消费”链路上增量演进。
