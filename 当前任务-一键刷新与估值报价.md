# 当前任务：一键全量刷新 + 估值卡片显示股价

日期：2026-07-23
执行方：**同时具备两个仓库权限的 Codex**
涉及仓库：
- **量化系统**（数据真源）：`~/Projects/futu-assistant`（Python）
- **网站**（只读展示层）：`portfolio-tracker`（TypeScript，含 VPS 网关与 Mac 同步脚本）

> **本文件自包含，是本轮唯一任务书。** 仓库里其他 `修复方案-V*.md`、`当前任务-*.md` 均为已完成的历史记录，本轮不需要读。
>
> 共 8 个 Phase：**Part A 量化侧（Phase 1）→ Part B 触发链路（Phase 2–4）→ Part C 网站展示（Phase 5–8）**。按顺序执行。

---

## 0. 边界与全程规则（先读）

### 0.1 两个仓库的角色不变

- **量化系统是数据真源**：负责计算、判定、信号、推送；
- **网站是只读展示层**：只消费量化产出的数据，**永远不自造判定结论**。

即使两个仓库都能改，**禁止**把判定逻辑挪到网站、禁止网站 `import` 量化 engine 代码。两者仍只通过 **CLI 输出 / JSON 文件 / HTTP 契约**通信。

### 0.2 量化侧：允许什么、禁止什么

**本轮允许**：
- ✅ **新增**一个 CLI 命令，用于"立即执行一次现有检查流程"（Phase 1）；
- ✅ 为新增命令写测试。

**绝对禁止**：
- ❌ 修改任何**阈值、信号规则、开窗/关窗判定、止盈剧本配置**；
- ❌ 修改**推送内容格式、推送去重逻辑、分级规则**（新命令必须复用现有推送路径，不得另起一套）；
- ❌ 修改 `scheduler.py` 的**定时调度行为**（新命令是"额外的手动入口"，不替代、不改变原有定时逻辑）；
- ❌ 读取或输出 `config/channels.yaml` 内容（**密钥文件**）；
- ❌ 写 `store.sqlite`（**单写者原则**；只读连接 `mode=ro` 可以）；
- ❌ 新增下单能力。

**量化侧必须证明**：
1. 该仓库自有测试套件**全部通过**（约 212 项）；
2. **判定与推送零变化**：同一输入下，新命令产出的开窗结论与推送内容，与原有定时流程**完全一致**（贴对比输出）。

### 0.3 网站侧红线

- 不自造买入/卖出信号、不自造阈值、不自造成本、不自造价格；
- **禁止**文案：「建议买入」「可买」「触发买入」（验收有 grep 断言）；
- 无数据显示「暂无」，**绝不显示 0 / NaN / Infinity 冒充**；
- 推导值（如"锚点对应股价"）**必须标注为推导并写明假设**。

### 0.4 通用规则

1. 每个 Phase：**先写会失败的测试并确认它真的失败（贴失败输出）** → 变绿 → 该仓库测试全绿 → **单独 commit** → **留验证报告** → 进下一 Phase；
2. 两个仓库的提交**分开**；
3. 不新增付费数据源、不新增运行时依赖；
4. 密钥只走环境变量或本机 600 权限文件，**绝不进仓库或提交历史**；
5. 中途不要停下来问方向；唯一允许暂停：部署输 SSH 密码；
6. 完工贴出：**两个仓库 git log** + **网站三个 hash**（本地 dist / 线上 VPS / 线上 Pages，三者一致才算部署生效）。

---

## 1. 当前状态（已核实）

### 1.1 已完成（无需返工）

上一轮 10 个 Phase 全部落地（`ee48897`…`d1dd43d`），PE 估值双基准、监控池报价、深度窗口价格推算均已实现并部署。

### 1.2 本轮要解决的两个问题

**问题 A：刷新入口分散，且无法触发量化重算**

现有**三个**互相独立的刷新按钮：

| 位置 | 按钮 | 作用范围 |
|---|---|---|
| `Summary.tsx:143` | 「手动刷新」 | 只刷行情报价 |
| `Summary.tsx:168` | `onRefreshQuant` | 只拉量化持仓 |
| `ConditionLookup.tsx:542` | 「刷新快照」 | 只拉量化分析快照 |

**更关键的是**：这三个按钮都只是**重新读取 VPS 网关上已有的数据**，而网关上的数据由 Mac 端脚本推送：

```
Mac (launchd 每 2700 秒 = 45 分钟)
  └─ sync/push-positions.mjs
       ├─ 跑 `positions-status`  → POST /api/portfolio/positions
       └─ 跑 `site-export`       → POST /api/portfolio/quant-analysis
```

所以**网站点任何按钮，都无法让量化系统重新计算** —— 最坏情况数据已经 45 分钟没更新，用户点刷新拿到的还是旧数据。用户看到特斯拉盘中跌 6% 想查当下状态，**现在做不到**。

**问题 B：估值卡片完全不显示股价**

`ValuationCard.tsx` 中 grep `现价|股价|price|Price` **零命中**。当前只显示：

```
TSLA · 远期 PE 22.65
5 年均值 24.80 · 低于均值 8.7%

TQQQ · 基准指数 NDX · 远期 PE 22.4
锚点 21.6（2025-04-08）· 距锚点 +3.7%
```

用户不知道**现在多少钱**，也不知道**跌到多少钱估值才到锚点**。

---

# Part A：量化侧新增手动触发命令（Phase 1）

## Phase 1：新增 `refresh-now` CLI 命令

### 要求

新增一个 CLI 子命令（名称建议 `refresh-now`，你侧可按现有命名习惯调整并在报告中说明），行为：

1. **立即执行一次**现有的完整检查流程：重新取价 → 重算开窗/关窗判定 → 按**现有规则和现有去重逻辑**发送推送；
2. **完全复用**现有代码路径 —— 不得复制一份判定逻辑、不得新写推送格式；
3. 输出一行 JSON 结果到 stdout，供 Mac 脚本判断执行情况：
   ```json
   {"ok": true, "checked_at": "2026-07-23T10:32:11-04:00", "notifications_sent": 2, "symbols_checked": 18}
   ```
4. **推送去重必须沿用现有逻辑** —— 用户可能短时间内多次触发，同一信号不得重复轰炸手机；
5. 执行超时上限 180 秒；超时返回 `{"ok": false, "error": "..."}`，不得挂死。

### 验收

- [ ] 命令可独立执行并返回合法 JSON
- [ ] **判定零变化**：同一输入下，本命令产出的开窗结论与原定时流程**完全一致**（贴对比输出）
- [ ] **推送零变化**：推送内容、格式、分级与原有一致（贴一条实际推送样例）
- [ ] **去重有效**：连续执行两次，第二次不重复推送已推过的信号（贴两次输出）
- [ ] 量化仓库自有测试全绿（约 212 项）
- [ ] 未修改 `scheduler.py` 的定时调度行为
- [ ] 未触碰 `config/channels.yaml`；未写 `store.sqlite`

提交（量化仓库）：`feat: add refresh-now command for manual check`

---

# Part B：反向触发链路（Phase 2–4）

**架构**（网站在 VPS、量化在 Mac，Mac 无公网入口，只能由 Mac 主动轮询）：

```
用户点「一键刷新」
  → 网站 POST /api/refresh-request        （网关写待办标志）
  → Mac 轮询脚本（每 60 秒）读到标志
  → 执行 refresh-now + positions-status + site-export
  → 推送发出 + 新数据 POST 回网关 + 清除标志
  → 网站轮询到 pushed_at 变化 → 界面更新
```

最大延迟 ≈ 60 秒 + 量化计算时间。

## Phase 2：网关新增刷新请求端点

在 `deploy/aliyun-gateway.mjs` **纯新增**（既有逻辑一行不动）：

1. `POST /api/refresh-request`（需 `Authorization: Bearer <PORTFOLIO_SYNC_TOKEN>`）：
   - 写 `/opt/portfolio-tracker-data/refresh-request.json`：`{"requested_at": ISO, "status": "pending"}`；
   - **节流**：若已存在 `pending` 且 `requested_at` 距今 < 60 秒 → 返回 `202` 并附 `{"throttled": true, "requested_at": ...}`，不覆盖；
   - 返回 `{"ok": true, "requested_at": ...}`。
2. `GET /api/refresh-request`（同 token）：返回当前标志内容；无文件 → `{"status": "idle"}`。
3. `POST /api/refresh-request/complete`（同 token）：Mac 端执行完毕后调用，写入 `{"status":"done","completed_at":ISO,"result":{...}}`。
4. 未配置 `PORTFOLIO_SYNC_TOKEN` → 这组端点整体 404（与现有 `/api/imports` 一致）。

**测试**：无 token → 404；错 token → 401/404；POST 后 GET 返回 pending；60 秒内二次 POST 返回 throttled；complete 后 GET 返回 done。

提交（网站仓库）：`feat: add refresh request endpoints on gateway`

## Phase 3：Mac 同步脚本支持轮询与按需执行

改 `sync/push-positions.mjs`（**保留现有 45 分钟全量推送行为**，新增轮询模式）：

1. 新增运行模式参数（如 `--watch`）：每 **60 秒** GET `/api/refresh-request`；
2. 读到 `status === "pending"` 时：
   - 执行 `refresh-now`（Phase 1 的新命令）；
   - 再执行现有的 `positions-status` + `site-export` 并 POST 上传；
   - 调用 `POST /api/refresh-request/complete` 上报结果；
3. **并发保护**：本次执行未结束时，忽略新的 pending（避免叠加执行）；
4. 任何一步失败 → 上报 `{"status":"done","result":{"ok":false,"error":"..."}}`，**不得让脚本崩溃退出**；
5. 新增 launchd 配置（或改现有 plist）让 `--watch` 常驻；**原有 2700 秒全量推送保留**作为兜底。

**测试**（对纯逻辑部分）：pending → 触发执行序列；执行中再来 pending → 忽略；失败 → 上报 error 且不抛出；idle → 不执行。

提交（网站仓库）：`feat: watch mode for on-demand quant refresh`

## Phase 4：网站统一「一键刷新」按钮

1. **合并三个按钮**为一个主按钮，放在总览页顶部显眼位置，文案：**「一键刷新全部」**；
   - 原有三个按钮**保留但降级**为次要样式（或收进「更多」），**不删除**（部分场景仍需单独刷新）。
2. 点击后依次执行：
   - `POST /api/refresh-request`（触发 Mac 重算 + 推送）；
   - 立即刷新行情报价、量化持仓、量化分析快照（读现有数据，先给即时反馈）；
   - **轮询** `GET /api/refresh-request`，每 5 秒一次，最多 3 分钟；
   - 检测到 `status === "done"` → 再拉一次全部数据 → 显示完成。
3. **按钮状态机**（必须有，让用户知道进行到哪一步）：
   ```
   一键刷新全部 → 已请求量化重算… → 正在计算（约 1 分钟）… → 已更新（12:34:56）
   ```
4. **节流**：60 秒内不可重复点击（按钮置灰 + 倒计时）；
5. **失败处理**：
   - 网关返回 throttled → 显示「刚刚已有刷新请求，正在处理中」；
   - 超时 3 分钟未完成 → 显示「量化系统未在 3 分钟内响应，可能 Mac 端未运行；已显示现有最新数据」，**不清空界面**；
   - 无同源网关（GitHub Pages 入口）→ 按钮禁用并注明「一键刷新仅在 VPS 入口可用」。
6. **推送说明**：按钮下方小字注明「刷新会让量化系统重新检查一次，若有符合条件的标的会照常推送到手机」。

**测试**：点击后按顺序发起请求；状态机文案随阶段变化；60 秒内二次点击被拦截；throttled/超时/无网关三种情况文案正确；不清空既有数据。

提交（网站仓库）：`feat: unified one-tap refresh with quant recompute`

---

# Part C：估值卡片显示股价（Phase 5–8）

## C0. 用户规则（本轮明确）

> 「不能直接给一个图表但是没有告诉价格……我要知道它在这个价格的时候，它在历史上的那个 PE 估值到底是什么情况……就以 2025 年 4 月份川普加关税的那个为最低的标准就行了，**不需要熊市标准，现在还达不到熊市**，只要接近川普那会儿的 PE，或者说把川普当时的 PE 设为最低点就可以了。」

**明确两点**：
1. 每张估值卡片**必须显示该标的的当前股价**；
2. 锚点**固定使用 2025 年 4 月窗口**的最低 PE，**不引入更深的历史低点/熊市极值**（现有设置项默认值 `2025-04-01 ~ 2025-04-30` 即符合，不要改）。

## Phase 5：估值卡片显示当前股价

1. `ValuationCard` 新增 prop `currentPrice: number | null` 与 `priceSource: 'holding' | 'monitored' | 'unavailable'`；
2. 数据来源复用已有的 `depthQuotePrice(holdings, monitoredQuotes, symbol)`（持仓报价优先，其次监控池报价）；
3. 卡片首行改为**代码 + 股价 + PE** 同行显示：
   ```
   TSLA  $402.15  ·  远期 PE 22.65
   ```
4. 股价走 V4 的显示货币层（`formatDisplayMoney`）；
5. 无报价 → 显示「股价暂无」，**不显示 0**。

**测试**：有持仓报价 → 显示该价；仅监控池有 → 显示该价；都无 → 「股价暂无」；显示货币切 CNY → 股价随之换算。

提交（网站仓库）：`feat: show current price on valuation cards`

## Phase 6：推导「估值回到基准时对应的股价」

这是本轮的**核心价值**：用户想知道"跌到多少钱，估值才回到那个水平"。

**公式**（PE = 股价 / 每股收益，短期内 EPS 视为不变）：
```
基准对应股价 = 当前股价 × (基准 PE / 当前 PE)
```

1. 新纯函数 `src/peTargetPrice.ts`：
   ```ts
   export interface PeTargetPrice { targetPrice: number | null; gapPct: number | null; }
   export function computePeTargetPrice(currentPrice: number|null, currentPe: number|null, basisPe: number|null): PeTargetPrice
   ```
   - 三者任一为 null / ≤ 0 → 返回 `{targetPrice: null, gapPct: null}`（**不得产生 NaN / Infinity**）；
   - `gapPct = (targetPrice - currentPrice) / currentPrice * 100`（负数 = 还需下跌）。
2. **个股型卡片**追加一行：
   ```
   PE 回到 5 年均值 24.80 对应股价 ~$440.20（需上涨 9.5%）
   ```
3. **指数型卡片**追加一行（用基准指数的 PE 与其代理 ETF 的价格）：
   ```
   NDX 回到 2025-04 锚点 21.6 对应 QQQ ~$512.30（需下跌 12.4%）
   ```
   指数代理 ETF 映射：`NDX→QQQ, SPX→SPY, SOX→SMH, DJI→DIA, RUT→IWM, FANGPLUS→（无代理，标注不可推算）`
4. **必须标注假设**（固定小字）：
   > 按当前每股收益不变推算；实际 EPS 会随财报变化，仅供参考。

5. **杠杆 ETF 的特殊处理（重要）**：TQQQ / SOXL / FNGU 等杠杆 ETF**不得**用倍数反推其锚点价格 —— 每日重置带来路径依赖，简单倍数推算是错的。卡片上：
   - 显示该杠杆 ETF 自己的**当前股价**（✅）；
   - **不显示**其"锚点对应股价"，代之以小字：「杠杆 ETF 因每日重置存在路径依赖，不推算目标价；请参考上方基准指数的目标价」。

**测试（数值逐个对上）**：
- 现价 400、当前 PE 30、基准 PE 21.6 → `targetPrice = 288.00`，`gapPct = -28.00`；
- 现价 400、当前 PE 20、基准 PE 24.8 → `targetPrice = 496.00`，`gapPct = +24.00`；
- 任一入参为 0 或 null → 返回 null，无 NaN；
- 杠杆 ETF → 不产出 targetPrice，出现路径依赖说明。

提交（网站仓库）：`feat: derive price level implied by valuation basis`

## Phase 7：锚点规则收口（不引入熊市极值）

1. 确认设置项默认锚点窗口为 `2025-04-01 ~ 2025-04-30`（现有默认值，**不要改**）；
2. 锚点卡片说明文案统一为：
   > 锚点 = 2025 年 4 月关税冲击期间的最低 PE。当前不使用更深的历史/熊市极值作为基准。
3. **不得**新增"历史最低 PE""熊市锚点"等第二套基准 —— 用户明确说不需要；
4. 若量化序列在锚点窗口内无数据 → 显示「锚点窗口内无数据，可在设置手动录入」，不回退到全历史最低值。

**测试**：默认窗口值正确；窗口内无数据时不回退到全历史最低；文案包含"不使用更深的历史/熊市极值"。

提交（网站仓库）：`fix: pin anchor to april 2025 window only`

## Phase 8：估值卡片可读性收尾

1. 卡片信息顺序固定为：**股价 → 当前 PE → 基准 PE → 距离 → 目标股价 → 数据来源与口径**；
2. 每个数字标注来源（量化系统 / Alpha Vantage / 手动录入）与口径（远期 PE / TTM PE）；
3. 手机窄屏（< 520px）时纵向堆叠，不得横向溢出；
4. **文案红线复查**：grep 断言不含「建议买入」「可买」「触发」。

**测试**：渲染顺序正确；375px 宽度无横向溢出；grep 断言通过。

提交（网站仓库）：`feat: finalize valuation card layout`

---

## 2. 部署与总验收

1. 量化侧：该仓库测试全绿；`refresh-now` 可用
2. 网站侧：`npm run build` && `npm test`
3. `bash deploy/deploy-us-vps.sh`（网关新端点生效，需提供 `PORTFOLIO_SYNC_TOKEN`）
4. Mac 端：安装 `--watch` 模式 launchd 配置并启动
5. `git push origin main`，等 Pages Actions

**总验收清单**：

**量化侧**
- [ ] Phase 1 失败测试输出 + 通过输出已贴
- [ ] 量化仓库自有测试全绿（约 212 项）
- [ ] **判定零变化 + 推送零变化**（贴对比输出与一条推送样例）
- [ ] 连续执行两次，第二次不重复推送（贴输出）
- [ ] 未改 `scheduler.py` 调度行为；未触碰 `channels.yaml`；未写 `store.sqlite`

**链路**
- [ ] 无 token 时刷新端点 404；有 token 时 POST→GET→complete 全流程正常
- [ ] **端到端实测**：网站点「一键刷新全部」→ 60 秒内 Mac 执行 → 网站显示「已更新」→ 数据 `pushed_at` 确实变新（贴时间戳前后对比）
- [ ] 60 秒内二次点击被节流
- [ ] Mac 端未运行时，网站 3 分钟后显示超时提示且**不清空数据**

**网站侧**
- [ ] Phase 2–8 各自失败测试 + 通过输出已贴
- [ ] `npm test` 全绿（新增 ≥ 24 用例）；`npm run build` 通过
- [ ] **三个 hash 一致**（贴出三个）
- [ ] `/api/health` 正常
- [ ] 估值卡片显示：**股价 + 当前 PE + 基准 PE + 目标股价**（个股型与指数型各截一例）
- [ ] 杠杆 ETF 卡片**不显示**目标价，显示路径依赖说明
- [ ] 锚点窗口默认 `2025-04-01 ~ 2025-04-30`，无数据时不回退全历史最低
- [ ] 全仓 grep：不含「建议买入」「可买」「触发买入」
- [ ] 无数据处显示「暂无」，无 NaN / Infinity / 0
- [ ] 375px 宽度无横向溢出
- [ ] 每 Phase 独立 commit；两仓库提交分开；无未提交改动

---

## 3. 明确不做

**量化侧**
- 不改阈值、信号规则、开窗判定、止盈剧本配置
- 不改推送内容格式、去重逻辑、分级规则
- 不改 `scheduler.py` 定时调度行为
- 不触碰 `config/channels.yaml`、不写 `store.sqlite`
- 不新增下单能力

**网站侧**
- 不自造买入信号（买入开窗永远归量化系统）
- 不删除现有三个独立刷新按钮（降级保留）
- 不为杠杆 ETF 推算锚点目标价（路径依赖）
- 不引入熊市/历史极值作为第二套锚点
- 不接入付费数据源；不新增运行时依赖
- 不重构组件结构、不新增 Tab

**两侧共同**
- 不让网站 `import` 量化 engine 代码
- 不把判定逻辑挪到网站
- 不把密钥写进任何仓库或提交历史

---

## 4. 后续（本轮不做）

若「一键刷新」的 60 秒轮询延迟仍不满足需求，可考虑在 Mac 端加内网穿透（Tailscale 等）实现真正的即时触发 —— 属基础设施变更，须用户单独决定。
