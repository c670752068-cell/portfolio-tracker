# 当前任务：盘前 / 盘后 / 夜盘价格取价修正

日期：2026-07-23
执行方：**同时具备两个仓库权限的 Codex**
涉及仓库：
- **量化系统**（数据真源）：`~/Projects/futu-assistant`（Python）
- **网站**（只读展示层）：`portfolio-tracker`（TypeScript，含 VPS 网关）

> **本文件自包含，是本轮唯一任务书。** 仓库里其他 `修复方案-V*.md`、`当前任务-*.md` 均为历史记录，本轮不需要读。
>
> 共 6 个 Phase：**Phase 1 诊断定责 → Phase 2–3 网关取价 → Phase 4 量化侧时段 → Phase 5–6 网站展示**。Phase 1 的结论决定 Phase 4 是否需要执行。

---

## 0. 问题（用户实测）

2026-07-23 **09:20 ET（盘前时段）**，微软实际盘前价约 **$388**，但网站「深度买入窗口」显示：

```
当前回撤 29.16%  阈值 15.10%
现价 $390.34 · 高点 $551.05 · 阈值价 $467.81
取价时段：盘中
```

**两处错误**：
1. 现价 $390.34 是**昨日收盘价**，不是盘前实时价；
2. 时段标注「盘中」，而当时是**盘前**。

点「刷新快照」也无效 —— 因为取到的仍是同一个昨收价。

---

## 1. 已实测的事实（直接采信，不要重新排查）

### 1.1 Yahoo **能**取到盘前/盘后价，只是网关没要

网关当前代码 `deploy/aliyun-gateway.mjs:516`：

```js
const url = `https://query1.finance.yahoo.com/v8/finance/chart/${yahooSymbol}?range=5d&interval=1d`;
const price = asNumber(meta.regularMarketPrice) ?? lastNumber(result?.indicators?.quote?.[0]?.close);
```

两个问题：**没有 `includePrePost` 参数**、**日线粒度**、且优先取 `meta.regularMarketPrice`（= 上一个正常交易时段的收盘价）。

**实测对比**（2026-07-23 09:20 ET，MSFT）：

| 请求 | 结果 |
|---|---|
| `?range=5d&interval=1d`（现状） | `regularMarketPrice: 390.34` ← **昨收**，`hasPrePostMarketData: true` 但数据未返回 |
| `?range=1d&interval=1m&includePrePost=true` | 分钟序列最后一点 **09:20 ET = $388.00** ✅ |
| `?range=1d&interval=5m&includePrePost=true` | 最后一点 **09:21 ET = $387.97** ✅，响应 4KB（1m 为 14KB） |

**结论：改参数即可拿到盘前价，推荐 `interval=5m`（体积小 3.5 倍，价格一致）。**

### 1.2 取价方法（重要）

- **不能**用 `meta.regularMarketPrice` —— 它永远是正常交易时段的价；
- **不能**用 `meta.preMarketPrice` / `meta.postMarketPrice` —— chart 端点实测返回 `None`（这两个字段只在需要 crumb 认证的 `quote` 端点有，而该端点实测返回 `Invalid Crumb`，**不可用**）；
- **正确做法**：取 `timestamp[]` 与 `indicators.quote[0].close[]` 中**最后一个非 null 的点**，其 timestamp 即取价时刻。

### 1.3 时段判断（Yahoo 直接给）

`meta.currentTradingPeriod` 返回三个窗口（实测，ET）：

```
pre      04:00 - 09:30
regular  09:30 - 16:00
post     16:00 - 20:00
```

用取价点的 timestamp 落在哪个窗口，即可判定 `pre` / `regular` / `post`；三者之外（20:00–04:00）即**夜盘时段**。

### 1.4 时段定义规范（**本轮唯一标准，全部代码以此为准**）

#### （1）标准交易日时段表（时间一律为 **ET / America/New_York**）

| 时段 | 代码值 | 起 | 止 | 说明 |
|---|---|---|---|---|
| 夜盘 | `overnight` | 20:00 | 次日 04:00 | 跨零点；**Yahoo 不覆盖**（见 1.5） |
| 盘前 | `pre` | 04:00 | 09:30 | |
| 盘中 | `regular` | 09:30 | 16:00 | |
| 盘后 | `post` | 16:00 | 20:00 | |

**边界规则统一为「左闭右开」**：`09:30:00` 属于 `regular`，`09:29:59` 属于 `pre`；`16:00:00` 属于 `post`。

#### （2）半日市（提前收市）时段表

半日市当天 **13:00 收盘**，时段相应变化：

| 时段 | 起 | 止 |
|---|---|---|
| 盘前 | 04:00 | 09:30 |
| 盘中 | 09:30 | **13:00** |
| 盘后 | **13:00** | **17:00** |
| 夜盘 | 17:00 | 次日 04:00 |

#### （3）休市日

- **周末**：周六、周日全天 `closed`；
- **法定假日**：全天 `closed`；
- `closed` 时**不得**显示任何时段徽标，只显示「休市 · 上一交易日收盘价 $X」。

#### （4）2026 年美股假期与半日市（**兜底表，非权威**）

**全天休市**：
```
2026-01-01 元旦
2026-01-19 马丁·路德·金日
2026-02-16 总统日
2026-04-03 耶稣受难日
2026-05-25 阵亡将士纪念日
2026-06-19 六月节
2026-07-03 独立日（7/4 为周六，提前至周五）
2026-09-07 劳动节
2026-11-26 感恩节
2026-12-25 圣诞节
```

**半日市（13:00 收盘）**：
```
2026-11-27 感恩节次日
2026-12-24 平安夜
```

> ⚠️ **此表仅作兜底，且会在 2027 年过期。**
> **判定优先级必须是**：
> 1. **Yahoo 的 `meta.currentTradingPeriod`**（它自带假期与半日市信息）—— 优先用；
> 2. **分钟序列为空** → 判定为 `closed`（这是最可靠的休市信号）；
> 3. 上述两者不可用时，才用本地时段表 + 假期表；
> 4. **若当前日期年份超出本地假期表覆盖范围**，不得报错，降级为「仅按周末 + 时段表判断」并在日志中提示需更新假期表。
>
> **禁止**自建一套交易日历去覆盖数据源的判断。

#### （5）夏令时 / 冬令时（**最容易出错的地方**）

- 美东夏令时 **EDT = UTC-4**（3 月第二个周日 – 11 月第一个周日），冬令时 **EST = UTC-5**；
- **绝对禁止硬编码 UTC 偏移**（如 `-4` 或 `-5`）、禁止用 `getTimezoneOffset()` 推算、禁止依赖服务器本地时区；
- **必须**使用 IANA 时区 `America/New_York`，通过 `Intl.DateTimeFormat` 取 ET 的年/月/日/时/分/星期。
- 上表中的时段边界（04:00 / 09:30 / 16:00 / 20:00）是 **ET 本地时间**，全年不变 —— 夏冬令时只影响它对应的 UTC 时刻。

#### （6）参考实现（**已实测通过，可直接采用**）

```js
function etParts(date) {
  const f = new Intl.DateTimeFormat('en-US', {
    timeZone: 'America/New_York', year:'numeric', month:'2-digit', day:'2-digit',
    hour:'2-digit', minute:'2-digit', hourCycle:'h23', weekday:'short',
  });
  const p = Object.fromEntries(f.formatToParts(date).map(x => [x.type, x.value]));
  return {
    date: `${p.year}-${p.month}-${p.day}`,
    minutes: Number(p.hour) * 60 + Number(p.minute),
    weekday: p.weekday,
  };
}
const M = (h, m = 0) => h * 60 + m;

function classifySession(date, { holidays = new Set(), halfDays = new Set() } = {}) {
  const { date: d, minutes, weekday } = etParts(date);
  const weekend = weekday === 'Sat' || weekday === 'Sun';
  const closeMin = halfDays.has(d) ? M(13) : M(16);
  const postEnd  = halfDays.has(d) ? M(17) : M(20);
  if (weekend || holidays.has(d)) return 'closed';
  if (minutes >= M(4)    && minutes < M(9, 30)) return 'pre';
  if (minutes >= M(9, 30)&& minutes < closeMin) return 'regular';
  if (minutes >= closeMin&& minutes < postEnd)  return 'post';
  return 'overnight';
}
```

#### （7）自查清单（**必须逐条写成测试用例，实测输出已验证如下**）

| # | 输入（UTC） | 期望 ET | 期望 session | 验证要点 |
|---|---|---|---|---|
| 1 | `2026-07-23T13:20:00Z` | 07-23 09:20 EDT 周四 | `pre` | 盘前 |
| 2 | `2026-07-23T14:00:00Z` | 07-23 10:00 EDT 周四 | `regular` | 盘中 |
| 3 | `2026-07-23T20:30:00Z` | 07-23 16:30 EDT 周四 | `post` | 盘后 |
| 4 | `2026-07-24T02:00:00Z` | 07-23 22:00 EDT 周四 | `overnight` | 夜盘（注意 UTC 日期已跨天，ET 仍是 23 日）|
| 5 | `2026-07-25T14:00:00Z` | 07-25 10:00 EDT 周六 | `closed` | 周末 |
| 6 | `2026-01-15T14:35:00Z` | 01-15 09:35 **EST** 周四 | `regular` | **冬令时**：同为 14:35Z，夏令时是 10:35 盘中、冬令时是 09:35 盘中 |
| 7 | `2026-11-26T15:00:00Z` | 11-26 10:00 EST 周四 | `closed` | 感恩节假期 |
| 8 | `2026-11-27T19:00:00Z` | 11-27 14:00 EST 周五 | `post` | **半日市**：13:00 已收盘，14:00 属盘后（非半日市时应为 regular）|
| 9 | `2026-12-24T17:30:00Z` | 12-24 12:30 EST 周四 | `regular` | **半日市**：13:00 前仍是盘中 |

**补充边界用例（也要测）**：
- `09:29:59 ET` → `pre`；`09:30:00 ET` → `regular`（左闭右开）；
- `15:59:59 ET` → `regular`；`16:00:00 ET` → `post`；
- `19:59:59 ET` → `post`；`20:00:00 ET` → `overnight`；
- 半日市 `12:59:59` → `regular`；`13:00:00` → `post`；`16:59:59` → `post`；`17:00:00` → `overnight`。

### 1.5 夜盘的边界（必须诚实告知）

**Yahoo chart 端点不覆盖 20:00–04:00 ET 的夜盘（overnight）交易。** 该时段行情由券商提供 —— 量化系统用的**富途 OpenD 有夜盘行情**，且网站代码里 `sessionLabel` 本就有 `overnight: '夜盘'` 分支。

因此分工：

| 时段 | 数据来源 |
|---|---|
| 盘前 04:00–09:30 ET | ✅ Yahoo（本轮 Phase 2 修） |
| 盘中 09:30–16:00 ET | ✅ Yahoo |
| 盘后 16:00–20:00 ET | ✅ Yahoo（本轮 Phase 2 修） |
| **夜盘 20:00–04:00 ET** | ❌ Yahoo 无 → **只能靠量化侧富途 OpenD** |

---

## 2. 全程规则

### 2.1 量化侧允许 / 禁止

**允许**：修正**取价时段选择逻辑**（让快照使用当前时段的真实价格）、修正 `price_session` 标注。

**绝对禁止**：
- ❌ 修改任何**阈值、开窗/关窗判定规则、信号规则、止盈剧本配置**；
- ❌ 修改推送格式、去重逻辑、分级规则；
- ❌ 修改 `scheduler.py` 定时调度行为；
- ❌ 读写 `config/channels.yaml`（密钥）；
- ❌ 写 `store.sqlite`（只读 `mode=ro` 可以）。

> **注意**：修取价会让回撤百分比数值变化（因为价格变了），这是**预期内的正确变化**，不算"判定逻辑变更"。但必须证明：**给定同一个价格输入，开窗判定结论不变**。

### 2.2 网站侧红线

- 只读展示层，不自造价格、不自造判定；
- 无数据显示「暂无」，**绝不显示 0 / NaN**；
- 推导值必须标注。

### 2.3 通用

1. 每个 Phase：**先写会失败的测试并确认失败（贴失败输出）** → 变绿 → 该仓库测试全绿 → **单独 commit** → **留验证报告**；
2. 两仓库提交分开；
3. 不新增付费数据源、不新增运行时依赖；
4. 密钥绝不进仓库；
5. 中途不停下来问方向；唯一允许暂停：部署输 SSH 密码；
6. 完工贴：两仓库 git log + 网站三个 hash（本地 dist / 线上 VPS / 线上 Pages）。

---

## Phase 1：诊断定责（必须先做，结论决定后续）

截图中价格**未带 `~` 符号**，按 `src/depthPrice.ts` 逻辑推断为 `source === 'quant'`（量化真值），但**必须实测确认**。

**任务**：
1. 读取 VPS 网关上的量化分析快照原始 JSON（`GET /api/portfolio/quant-analysis`，带 token），检查 MSFT 的 `depth_window`：
   - 是否存在 `current_price` / `high_price` / `threshold_price` 字段？值是多少？
   - `price_session` 字段的值是什么？
2. 同时请求网站行情代理 `GET /api/quotes?symbols=MSFT`，记录其返回价格；
3. **在报告中明确回答**：截图上的 $390.34 来自量化快照，还是网站行情代理？

**分支**：
- 若来自**量化快照** → Phase 4 必须执行（量化侧取价时段有误）；
- 若来自**网站行情代理** → Phase 2–3 即可解决，Phase 4 视量化侧实际情况决定（仍需检查其 `price_session` 是否准确）；
- **两者都错**的可能性存在 → 两边都修。

提交：无（诊断报告即可，写进 Phase 2 的 commit message 或单独文档）

---

## Phase 2：网关取价支持全时段（网站仓库）

改 `deploy/aliyun-gateway.mjs` 的 `fetchYahooQuote`：

1. URL 改为：
   ```js
   `https://query1.finance.yahoo.com/v8/finance/chart/${yahooSymbol}?range=1d&interval=5m&includePrePost=true`
   ```
2. **取价逻辑**：从 `timestamp[]` 与 `indicators.quote[0].close[]` 取**最后一个非 null 点**作为当前价，其 timestamp 为取价时刻；
   - 若分钟序列为空（如休市日）→ 回退到 `meta.regularMarketPrice`，并标注时段为 `regular`（或 `closed`）；
3. **时段判定**：严格按 **§1.4 的时段定义规范**执行，优先级为 —— ①`meta.currentTradingPeriod` 的窗口 → ②分钟序列为空则 `closed` → ③本地时段表 + 假期表兜底。时区一律用 IANA `America/New_York`，**禁止硬编码 UTC 偏移**。参考实现见 §1.4(6)；
4. **返回结构新增字段**（纯新增，不改既有字段）：
   ```json
   {
     "symbol": "MSFT", "price": 388.00,
     "session": "pre",                       // pre | regular | post | overnight | closed
     "priceTime": "2026-07-23T13:20:00Z",    // 取价时刻
     "regularMarketPrice": 390.34,           // 保留正常时段价，供对照
     "previousClose": 397.75, ...
   }
   ```
5. **`previousClose` 口径**：盘前/盘后时应对照**上一个正常交易日收盘价**（现有 `chartPreviousClose` 即可），不要用盘前价自比；
6. **NASDAQ 备用源**：保持现状不动（它只有正常时段价），但当主源成功时优先用 Yahoo 的全时段价。

**测试**：
- **§1.4(7) 自查清单的 9 个场景 + 全部边界用例，逐条写成测试**（这是本 Phase 的硬性要求）；
- 分钟序列最后点为盘前时刻 → `session='pre'`，price 取该点；
- 序列为空 → `session='closed'`，回退 `regularMarketPrice` 并标注为上一交易日收盘价，不抛异常；
- close 数组含 null 尾部 → 正确跳过取到最后有效值；
- 假期表年份过期（如构造 2028 年日期）→ 不报错，降级为周末+时段表判断。

提交（网站仓库）：`feat: fetch pre/post market prices from yahoo`

---

## Phase 3：网站消费与展示时段（网站仓库）

1. `src/marketData.ts` 的 `fetchProxyQuotes` 解析新增的 `session` / `priceTime` 字段，写入 `QuoteSnapshot`（`types.ts` 相应扩展，字段可选以兼容旧数据）；
2. `src/monitoredQuotes.ts` 同样透传；
3. **深度买入窗口卡片**（`ConditionLookup.tsx` 的 `DepthHighlight`）：
   - 价格行后追加时段徽标：`现价 $388.00（盘前 09:20 ET）`；
   - 徽标文案映射：`pre→盘前`、`regular→盘中`、`post→盘后`、`overnight→夜盘`、`closed→休市`；
   - **当网站行情的时段与量化快照的 `price_session` 不一致时**，显示提示：
     > 量化快照取价为{量化时段}，当前行情为{网站时段}；回撤百分比基于量化取价计算。
4. **总览页行情同步**文案同样带上时段。

**测试**：`session='pre'` → 显示「盘前」；不一致时出现提示；旧数据无 session 字段 → 不崩溃、不显示徽标。

提交（网站仓库）：`feat: show price session on depth window`

---

## Phase 4：量化侧取价时段修正（量化仓库，视 Phase 1 结论执行）

**仅当 Phase 1 确认量化快照的价格或 `price_session` 有误时执行。**

1. 检查量化系统生成分析快照时的取价逻辑：
   - 快照生成于盘前（如 09:08 ET）时，是否取到了**盘前实时价**？
   - `price_session` 是否如实反映取价时刻所处时段（含 `overnight`）？
2. 修正为：**取价时刻所在时段的真实价格**，并如实标注 `price_session`；
   - 富途 OpenD 支持盘前/盘后/夜盘行情，优先使用；
   - 取不到当前时段价时，**如实回退并标注**为上一时段价（例如 `price_session: "regular"` + 注明是收盘价），**不得冒充**当前时段价。
3. `depth_window` 的 `current_price` 与 `current_pct` **必须来自同一次取价**（此前需求已确立，本轮复查）。

**验收**：
- [ ] 盘前时段生成的快照，其 `current_price` 为盘前价，`price_session='pre'`
- [ ] 夜盘时段生成的快照，`price_session='overnight'` 且价格为夜盘价
- [ ] `current_pct` 与 `current_price / high_price - 1` 自洽（容差 0.01 个百分点）
- [ ] **判定零变化**：给定同一价格输入，开窗结论与改动前完全一致（贴对比输出）
- [ ] 量化仓库自有测试全绿（约 212 项）
- [ ] 未改阈值/信号/剧本配置；未改 scheduler 调度；未触碰 channels.yaml；未写 store.sqlite

提交（量化仓库）：`fix: use session-aware price for analysis snapshot`

---

## Phase 5：全时段端到端验证

在**至少两个不同时段**各做一次实测并贴出证据：

1. **盘前**（04:00–09:30 ET）：网站显示的现价与 Yahoo 分钟序列最后值一致，时段标「盘前」；
2. **盘后**（16:00–20:00 ET）或**夜盘**（20:00–04:00 ET）：同上，时段标注正确；
   - 夜盘若量化侧未能提供 → **如实记录该限制**，网站显示「夜盘价格暂无（数据源不覆盖）」，不得显示昨收冒充。

**每次实测贴出**：网站截图或页面文本 + 同一时刻的 `curl` 原始返回对照。

提交：无（验证报告）

---

## Phase 6：休市与异常兜底

1. **周末 / 节假日**：按 §1.4(3)(4) 判定 `closed` → 显示「休市 · 上一交易日收盘价 $X」，**不显示任何时段徽标**；
2. **半日市**：按 §1.4(2) 的时段表处理（13:00 收盘、盘后 13:00–17:00）；
3. **假期表过期**：当前年份超出 §1.4(4) 覆盖范围时**不得报错**，降级为「周末 + 时段表」判断，并在日志提示需更新假期表；
4. **单标的取价失败**：不影响其他标的（现有 `failedSymbols` 机制保留）；
5. **时区**：一律 IANA `America/New_York`，**禁止硬编码 UTC 偏移、禁止 `getTimezoneOffset()` 推算、禁止依赖服务器本地时区**（VPS 时区未必是 ET）；
6. **缓存**：Phase 2 的取价结果沿用现有缓存/节流策略，不得因分钟级请求高频轰炸 Yahoo。

**测试**：
- §1.4(7) 的 9 个场景全部通过（若 Phase 2 已覆盖则复用，不重复写）；
- 空序列 → 休市文案且无时段徽标；
- 半日市 14:00 → `post`（非半日市同一时刻应为 `regular`，两者都要断言）；
- 构造 2028 年日期（假期表未覆盖）→ 不抛异常；
- 冬令时用例（`2026-01-15T14:35:00Z` → 09:35 EST `regular`）必须通过，证明未硬编码 UTC-4。

提交（网站仓库）：`fix: handle market closed and timezone edge cases`

---

## 3. 部署与总验收

1. 量化侧（若执行 Phase 4）：仓库测试全绿
2. 网站侧：`npm run build` && `npm test`
3. `bash deploy/deploy-us-vps.sh`（网关改动需重新部署）
4. `git push origin main`，等 Pages Actions

**总验收清单**：
- [ ] **Phase 1 诊断结论已明确回答**：$390.34 来自量化还是网站行情代理
- [ ] 各 Phase 失败测试输出 + 通过输出已贴
- [ ] `npm test` 全绿（新增 ≥ 14 用例）；`npm run build` 通过
- [ ] 量化侧（若改）测试全绿 + **判定零变化**对比输出
- [ ] **三个 hash 一致**（贴出三个）
- [ ] `/api/health` 正常
- [ ] **盘前实测**：`curl "http://67.215.255.196:8788/api/quotes?symbols=MSFT"` 返回的 price 与 Yahoo 分钟序列最后值一致，且 `session` 字段正确（贴两边输出对照）
- [ ] **盘后或夜盘实测**：同上（夜盘若无数据源，如实记录限制）
- [ ] 深度买入窗口卡片显示时段徽标（盘前/盘中/盘后/夜盘）
- [ ] 量化时段与网站时段不一致时有提示
- [ ] 休市日显示「休市 · 上一交易日收盘价」，不显示任何时段徽标
- [ ] **§1.4(7) 自查清单 9 个场景 + 全部边界用例，逐条有对应测试且全部通过**（贴测试输出）
- [ ] **冬令时用例通过**（`2026-01-15T14:35:00Z` → 09:35 EST `regular`），证明未硬编码 UTC 偏移
- [ ] **半日市用例通过**（`2026-11-27T19:00:00Z` → 14:00 EST `post`）
- [ ] 全仓 grep 确认：时段判断处**无** `UTC-4`/`UTC-5`/`-14400`/`-18000`/`getTimezoneOffset` 等硬编码偏移
- [ ] 假期表年份过期时降级而非报错（构造 2028 年日期验证）
- [ ] 无数据处显示「暂无」，无 NaN / 0 冒充
- [ ] 每 Phase 独立 commit；两仓库提交分开；无未提交改动

---

## 4. 明确不做

**量化侧**
- 不改阈值、开窗判定、信号规则、止盈剧本配置
- 不改推送格式、去重、分级
- 不改 `scheduler.py` 调度行为
- 不触碰 `channels.yaml`、不写 `store.sqlite`

**网站侧**
- 不自造价格、不用昨收冒充当前时段价
- 不接入付费数据源、不新增运行时依赖
- 不改 `depthPrice.ts` 推算公式
- 不重构组件结构、不新增 Tab

**共同**
- 不让网站 `import` 量化 engine 代码
- 不把判定逻辑挪到网站
- 密钥不进仓库

---

## 5. 已排除的方案（不要再尝试）

| 方案 | 排除原因 |
|---|---|
| Yahoo `quoteSummary` 端点取 `preMarketPrice` | 实测返回 `Invalid Crumb`，需认证，不可靠 |
| chart 端点的 `meta.preMarketPrice` / `postMarketPrice` | 实测为 `None`，该端点不填这两个字段 |
| `interval=1m` | 可用但响应 14KB（5m 仅 4KB 且价格一致），批量请求时浪费带宽 |
| 付费数据源（Koyfin / FMP / EODHD） | 此前已调研排除；Yahoo 免费即可满足盘前盘后 |

## 6. 后续（本轮不做）

若夜盘（20:00–04:00 ET）价格在量化侧也无法取得，可考虑：
- 富途 OpenD 的夜盘行情订阅配置（量化侧调查）；
- 或在网站明确标注该时段不可用。

属数据源能力问题，须先由量化侧确认富途实际支持情况后再决定。
